# ── Credentials & Secrets (NEVER COMMIT) ─────────────────────────────────────
.env
.env.*
!.env.example
*.json
!package.json
!package-lock.json
!composer.json
!composer.lock
!scripts/*.json
!includes/components/*.json
*credential*
*password*
*secret*
google-credentials.json
google-indexing-setup.txt
.ssh-deploy/
.ssh/
*.key
*.pem
id_rsa*
id_ed25519*

# ── Deploy & Indexing Scripts (contain or use secrets) ───────────────────────
deploy*.py
upload*.py
*-upload.py
submit-bing-indexing.py
submit-google-indexing.py
submit-google-indexing-log.txt
submit-bing-indexing-log.txt
setup-google-indexing.ps1
ftp*.php
test-ftp*.php

# ── IndexNow (contains secret key) ───────────────────────────────────────────
submit-indexnow.php
3714fa0124eb471586614d7b875b96fe.txt

# ── IDE / Editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.sublime-*

# ── Local Development ─────────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
node_modules/

# ── Temporary & Generated ─────────────────────────────────────────────────────
*.tmp
*.temp
*.log
tmp_*.txt
cache-test.php
clear-cache.php
debug-*.php
fix-*.php

# ── WordPress Blog (managed separately) ──────────────────────────────────────
blog/

# ── OS Files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Backup Files ──────────────────────────────────────────────────────────────
*.bak
*.backup
*~

# ── Claude/AI tooling ─────────────────────────────────────────────────────────
.claude/

# ── Junk / Review folder ──────────────────────────────────────────────────────
junk-to-review-by-nasir/

# ── Separate repo copies ──────────────────────────────────────────────────────
github-repo/

# ── Text files (mostly internal docs) ────────────────────────────────────────
*.txt
!robots.txt
!LICENSE.txt
!ads.txt
!llms.txt

# ── Internal ops docs (contain operational context) ─────────────────────────
CLAUDE.md
.claude/
seo-audit-*/

# ── AI chat backend config (contains Groq API key) ──────────────────────────
ai-config.php

# ── Ephemeral session scripts ───────────────────────────────────────────────
__*.py
__*.txt
__*.php
*.zip
kbt.zip
microsoft-store/package/

# ── AI helper / diagnostic scripts (one-off debugging from session work) ────
diagnose-*.py
check-after-fix*.py
deep-diagnose.py
fpm-restart*.py
quick-check.py
last-try.py
status-check.py
uapi-probe.py
fix-pr-62.py
rebuild-*.py
inject-pro-locales.py
gsc-country-queries.py
gsc-localized-analysis.py
gsc-anchor-audit.py
gsc-coverage-audit.py
gsc-traffic.py
test-gsc-api.py
dev-to-publish.py
