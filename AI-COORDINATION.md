# AI Coordination — Shared State Between Claude Code and Codex

**Last updated:** 2026-05-29 (Claude, SEO/GEO audit + fixes deployed live — see entry below)

> **2026-05-29 (Claude) — SEO/GEO audit + fix waves DEPLOYED LIVE via SFTP.** Full multi-agent audit report at `seo-audit-2026-05-29/SEO-GEO-AUDIT-REPORT.md`. Shipped & verified live:
> - **Titles (CTR):** rewrote ALL "Free Open Source"-prefixed titles keyword-first — meta-config.php (51), 22 inline `$pageTitle` overrides, 19 intent-page `$intentPage['meta']['title']`. 0 "Free Open Source" titles remain. Homepage + DPI page (analyzer-first) + H1 template prefix ("Open Source & Free" → "Free").
> - **Brand cannibalization:** `/tools/keyboard-tester/` retitled to "N-Key Rollover & Anti-Ghosting Test" so the homepage owns "keyboard tester".
> - **hreflang:** added EN→localized slug aliases in `includes/head-common.php` ($__hreflang_en_aliases) — the #1 traffic page (DPI) went from 0→10 hreflang tags; homepage now emits clean directory URLs, no duplicates.
> - **Schema:** Organization `@id` added; duplicate microdata BreadcrumbList stripped (JSON-LD only) in `includes/components/breadcrumbs.php`; sameAs reconciled (added instagram+bluesky).
> - **GEO:** robots.txt explicit AI-crawler Allow groups (GPTBot/ClaudeBot/PerplexityBot/etc.) + llms.txt pointers; sitewide `<link rel="alternate" type="text/plain" ... llms.txt>`; ad-placeholder "PUT YOUR BRAND HERE" → "Advertise on KeyboardTester.click".
> - **.htaccess:** removed `Header append Vary: User-Agent` (cache-busting anti-pattern).
> - **config.php:** removed non-brand `x.com/HW_Tools` social link.
> - **Localized CJK meta fix:** removed the English fallback sentence that leaked into Korean/Japanese/Arabic meta descriptions (`render-localized-modern-tool-page.php` + `-legacy-page.php`).
> - **Re-submitted** 45 changed URLs to Google Indexing (45/45) + Bing WMT (45/45) + IndexNow.
> - **GA4 Data API:** `ga4-traffic.py` added (reads `GA4_PROPERTY_ID` from `.env`). **ACTION NEEDED (user):** enable Analytics Data API on project kbt-indexing-4659, add service account as GA4 Viewer, paste numeric property id into `.env`.
> - **DEFERRED (risk-managed, need dedicated pass):** content templating bug in `render-english-tool-page.php:130` (kbtTemplateSeoBlocks scrapes legacy `help/seo-content/*.php` instead of rendering full article — restoring ~38k words interacts with FAQPage schema via getToolFAQs on ~85 pages); localized hreflang dedup (renderer scrapes + head-common emits = 18-20 tags) and FAQ-scraper leak (`kbtLmlExtractFaqs` harvests the keyboard UI `<details>Detailed measurements</details>` as #1 FAQ on all 8 localized homepages); `/tools/keyboard-tester/` directory page still emits 0 hreflang.
> - **NOT code (user/host):** HTTP/2+full-page cache+PHP 8.x (host toggle), backlinks (Product Hunt/YouTube/embed widgets), Wikidata.
> - All edits php-linted; final health sweep = 18 key pages all HTTP 200. Changes deployed to live but NOT yet git-committed.


This file is the **single source of truth** when handing off work between AI agents working on KeyboardTester.click. Both Claude Code and Codex read this at the start of every session and update it before ending.

---

## 🧭 Current state of the project

**Site version:** v17.2.47 (April 28, 2026 — latest work: Codex blog homepage redesign)
**Roadmap:** 50/50 tools built, no pending tools
**Last major work:** Pro Diagnostics panel deployed to all 9 language keyboard testers; Codex redesigned/deployed the blog homepage with pinned GSC-performing articles

**Note on commit prefixes:** This file previously suggested `[claude]` / `[codex]` prefixes. The first joint commit (`79de12c`) predates the convention. Going forward, both agents should add the prefix.

**Note on DEV.to API key:** `DEVTO_API_KEY` is now in `.env` (gitignored). Verified no token value in commit history. Token can be rotated at https://dev.to/settings/extensions if ever exposed.

## 👥 Active agents

| Agent | Workspace | Memory location |
|-------|-----------|-----------------|
| Claude Code | `c:/xampp/htdocs/kbt` | `C:\Users\TECH OFFICE\.claude\projects\c--xampp-htdocs-kbt\memory\` |
| Codex | `c:/xampp/htdocs/kbt` | (TBD — Codex sets its own location) |

**Both agents share:** all files in the workspace, all credentials in `.env`, all gitignored credential files (`google-credentials.json`, `.ssh-deploy/kbt_deploy`).

---

## 🔄 Handoff protocol

### Before starting work
1. **Read this file first** (top to bottom)
2. Read [seo-audit-2026-04-26/SESSION-TRACKER.md](seo-audit-2026-04-26/SESSION-TRACKER.md) for last session's detailed state
3. Run `git log --oneline -20` to see recent commits
4. Check the "Active tasks" section below to know what's in flight

### During work
- If you start a non-trivial task (>15 min), add it to "Active tasks" with your agent name
- If you finish a task, move it to "Completed today" with date + agent
- If you make a discovery worth remembering, add it to "Lessons learned"
- Don't edit files another agent is currently editing — coordinate via this doc

### Before ending session
1. Update "Active tasks" — mark anything in-flight
2. Update "Last updated" at top with date + agent name
3. If new credentials/tokens were added, document in "Credentials inventory"
4. Commit changes with message prefixed `[claude]` or `[codex]`

---

## 🎯 Active tasks (in flight or queued)

**Status legend:** `[in-progress]` `[queued]` `[blocked]` `[awaiting-user]`

| Task | Owner | Status | Notes |
|------|-------|--------|-------|
| DEV.to article series (5 total, 1 published) | Claude | `[awaiting-user]` | Articles 2-5 ready to write; next publish ~Apr 30 |
| AlternativeTo.net submission | User | `[blocked]` | Account age <7 days, unlocks May 4 — text saved at `seo-audit-2026-04-27/alternativeto-submission-PENDING-2026-05-04.md` |
| Slant.co listing | User | `[blocked]` | Slant search returned 500; package ready at `seo-audit-2026-04-27/slant-submission-package.md` |
| GitHub awesome-list PR #62 | User | `[awaiting-merge]` | Checked 2026-05-24: `mathewlewallen/awesome-free-tools#62` is still open, non-draft, mergeable, and unchanged since the user's 2026-05-09 follow-up. GitHub still reports `mergeStateStatus=UNSTABLE` with no check runs, likely maintainer approval/check-suite state rather than a content conflict. Broader PR search now shows 18 open, 3 merged, and 5 closed-not-merged PRs mentioning KeyboardTester.click; `Bakumon/awesome-online-tools#29` merged on 2026-05-21. |
| GSC validation clicks (5 buckets) | User | `[done]` | Clicked Apr 27 |
| Localized SEO Phase 1C+ | Either | `[queued]` | Russian/Spanish optimization done; remaining: localized intro paragraphs for typing-rhythm/brightness pages |
| USA backlink outreach batch | User/Codex | `[paused]` | 29 validated contacts sent from `outreach@keyboardtester.click`; 15 delivery failures matched, 14 likely delivered/pending, 21 contacts remain on hold. Pause further cold email until deliverability/DKIM is reviewed. Master tracker: `seo-audit-2026-05-02/outreach/outreach-master-tracker-2026-05-02.md`. |
| Russian/localized outreach | Either | `[queued]` | Do not send yet. Wait 24-48h after USA batch, then build 15-20 verified Russian-language prospects. Use Russian only for Russian-language/relevant sites, unique subjects, sender as `KeyboardTester.click team`. |
| Advertiser prospecting | Either | `[queued]` | Separate from backlink outreach. Build commercial leads for page-specific sponsorships/media kit; do not ask advertisers for backlinks in the first pitch. 2026-05-19 check: the Man & Machine pitch sent 2026-05-17 was rejected by recipient spam filtering for both `sales@mmimd.com` and `info@mmimd.com`; do not resend by email unless deliverability is fixed or a different/manual contact route is approved. |
| WhatsApp outreach | Either | `[paused]` | Use only for highly relevant public business WhatsApp numbers; no automation, no blasts, no repeat messages without reply. |
| Local homepage design review | Codex/User | `[awaiting-user]` | Local-only homepage draft now uses a Microsoft-style dark hero treatment with Segoe-style white copy, H1 `Open Source & Free Keyboard Tester` displayed as two controlled lines, generated no-person laptop/headphones workspace image shifted right, slim 7-category band directly below the hero with thinner 2px color lines, wider Microsoft-like header rail, clean stretched header wordmark with no Open Source badge in the logo area, `Tools` moved into the former CTA slot while `Try Tools` is removed, live keyboard tester below the category band, full-width Microsoft-style tiles for the current top-performing tools using SEO-focused copy (`Mouse DPI Tester`, `Input Latency Test`, `Click Speed Test (CPS)`, `Mouse Button Test`) with aligned CTAs, refreshed SEO-aware guide/FAQ with Advanced Options and Pro Test wording plus desktop ad rails, flatter Microsoft-style homepage keyboard controls with theme-aware key-history styling, Microsoft-style rectangular `Back to top` floating button with thin SVG up arrow, Segoe UI text, and footer docking, keyboard-variant block removed, latest blog posts restored and restyled as rectangular cards, Home removed from header nav (logo remains the home link), remaining header controls balanced and dark-theme aware, dark footer GitHub/X icons fixed, and the Microsoft Store banner restyled as a rectangular block. No live deploy. Backup remains `C:\xampp\htdocs\kbt-backups\kbt-local-backup-20260515-093053`. |

---

## 📜 Completed today (rolling 24-48h log)

### 2026-05-28 (Codex)
- Fixed the Bing Webmaster Tools recommendation that many meta descriptions were too short. Live pre-fix crawl of `sitemap.xml` found 884 URLs HTTP 200, 0 missing descriptions, but 98 HTML pages under 150 visible characters, mostly localized tool pages. Updated the shared localized modern renderers from a 120-character expansion threshold to 150, extended the all-tools renderer fallback for Japanese/Korean pages, and lengthened 15 direct page/article descriptions (`meta-config.php`, `resources.php`, `advertise.php`, `latency-checker.php`, and 12 blog posts). Deployed 19 scoped files via SFTP/paramiko with remote PHP lint passing. Final live sitemap-wide crawl: 884/884 URLs HTTP 200, 883 HTML pages, 0 descriptions under 150 characters, 0 missing descriptions, 0 redirects, 0 PHP/runtime markers. Bing WMT API accepted 884/884 URLs in 2 batches with 0 failures; IndexNow returned HTTP 200 and reported URLs submitted successfully. The remaining Bing recommendation about inbound links is authority/backlink work, not a site-code error.
- Checked Bing Webmaster failing-URL export `C:\Users\TECH OFFICE\Downloads\keyboardtester.click_FailingUrls_5_28_2026.csv` containing 50 localized/tool URLs. Live Bingbot-style fetches returned HTTP 200 for all 50 with no redirects; all 50 were present in both live `sitemap.xml` and `sitemap.txt`; robots.txt allowed them; no noindex, X-Robots noindex, canonical mismatch, or PHP runtime-error markers were found. Bing WMT quota check returned daily 10000/monthly 40000, then `submit-bing-wmt.py --urls ...` accepted all 50 URLs with HTTP 200 and 0 failed. Follow-up `GetUrlInfo` showed Bing data and LastCrawledDate for all 50, with 0 robots blocks and 0 API errors. No site-code deploy was needed.
- Prepared a Medium-ready Markdown cross-post draft for the one-handed gaming keyboard buyer guide at `seo-audit-2026-05-26/medium-best-one-handed-gaming-keyboards-2026.md`. The draft is adapted from the live KBT article and DEV.to version for Medium formatting: no PHP/template code, no DEV.to front matter/liquid syntax, absolute live image URLs, official buy links, product-source section, a plain YouTube URL for Medium embedding, and a canonical/original article link back to `https://keyboardtester.click/blog/best-one-handed-gaming-keyboards-2026.php`. Spot-checked current official/vendor search snippets for key price/spec lines before preparing the draft. No live-site deploy and no IndexNow submission.

### 2026-05-26 (Codex)
- Reviewed the exported Core Web Vitals CSVs from `C:\Users\TECH OFFICE\Downloads\keyboardtester.click-core-web-vitals-2026-05-26`: mobile-only report, current active GSC issue was `LCP issue: longer than 2.5s (mobile)`, validation failed, 109 affected URLs; INP had 0 URLs. Because the May 22/May 24 fixes were already deployed but live Lighthouse still reproduced poor mobile LCP on representative tool templates, deployed a scoped shared fix. Added `includes/components/responsive-image.php`, updated the English/shared/intent/localized modern renderers to emit WebP hero `srcset` sources, added a mobile-specific 640px WebP hero source preference, changed localized legacy support stylesheets to async preload/onload, and added a 640px mobile source to `help/brief-mouse-test.php`. Deployed only the scoped renderer/helper/mouse-brief files via SFTP/paramiko with remote PHP lint clean. Verification: live HTML for affected routes shows WebP/mobile hero sources; Lighthouse mobile after fixes reported `/tools/keyboard-tester/` LCP 1.9s, `screentestindex.php` 1.8s, `webcamtesterindex.php` 2.3s, `mic-tester.php` 1.8s, `headphone_speaker_tester_index.php` 2.4s, `mouse-test.php` 2.3s, `languages/spanish/` 2.3s, and localized screen samples around 2.0-2.1s, all with TBT 0ms and CLS 0. IndexNow returned HTTP 200. Note: GSC/CrUX field status will still lag because it uses a rolling 28-day window.
- Created a local-only researched buyer-guide draft for `blog/best-one-handed-gaming-keyboards-2026.php`. The draft targets the long-tail cluster around `best one handed gaming keyboard`, `one handed gaming keyboard with joystick`, `gaming keypad with analog stick`, `one handed mechanical keyboard`, `left hand gaming keyboard`, and related comparison intent. It ranks 9 currently buyable keypads/one-hand boards: Azeron Cyborg II, Azeron Keyzen, Razer Tartarus Pro, Razer Tartarus V2, Redragon K585 DITI, Redragon K585 PRO Wireless, HORI TAC Mechanical, HORI TAC F14, and Koolertron 48-key programmable keypad. Used official/vendor pages for prices/specs/buy links and downloaded 9 product-specific WebP images into `blog/images/one-handed-gaming-keyboards/`. Added the post to `blog/posts-data.php` and regenerated local `sitemap.xml`/`sitemap.txt`. No live deploy and no IndexNow submission. Verification passed: PHP lint for article and `blog/posts-data.php`, local article HTTP 200, one H1, BlogPosting/BreadcrumbList/ItemList/FAQPage/VideoObject schema present, 9 product cards, 9 buy links, all image files present, blog index card present, sitemap includes the URL, and in-app browser desktop/mobile checks found no horizontal overflow.
- Reader-point-of-view audit pass completed locally on the same article. Improved the opening decision path with a `Start Here: Which One Should You Buy?` section, rewrote the search-intent section into a reader-facing ranking-method section, added per-product `Reader verdict` callouts, removed awkward `snapshot`/`draft` wording from visible and product-data copy, added a related guides/tests section, and tightened source wording. No deploy. Verification after edits: PHP lint clean, local article HTTP 200, one H1, 9 product cards, 9 reader verdict lines, 9 buy links, related guides present, no visible `draft` or `snapshot` wording, all local product images present, schema markers present, and in-app browser desktop/mobile checks found no horizontal overflow.
- Deployed the one-handed gaming keyboard buyer guide live and completed the requested follow-up fixes. Live URL: `https://keyboardtester.click/blog/best-one-handed-gaming-keyboards-2026.php`. Added the shared bottom `You might also like` related-post cards to the article. Fixed the `Check current price` / buyer CTA contrast issue by darkening `.buy-link`, protecting white CTA text from the global blog link override, narrowing the fix to direct `.tool-cta > a` buttons, updating the older crosshair CTA gradient, and bumping `global.min.css` from `v=4.0` to `v=4.1`. Also deployed accessible `#1e40af` fallback updates across older blog CTA/link snippets. Deployed 46 scoped files via SFTP/paramiko. Verification passed: remote PHP lint clean for critical files, live article HTTP 200 with `You might also like` and 3 related cards, live buyer buttons use the new style, live blog index/sitemap XML/sitemap TXT include the article, all 9 product images return HTTP 200 `image/webp`, live `global.min.css?v=4.1` contains the overrides, live browser contrast audit found 0 failures across 106 CTA/related/pagination/gradient elements in light and dark modes, and IndexNow returned HTTP 200 with the new article in the response.
- Published the DEV.to canonical cross-post for the one-handed gaming keyboard buyer guide from `dev-to-articles/08-best-one-handed-gaming-keyboards-2026.md`. DEV.to article ID `3757096`, public URL `https://dev.to/nasirazizawan/best-one-handed-gaming-keyboards-in-2026-keypads-thumbsticks-and-what-to-test-413e`. The post uses canonical URL `https://keyboardtester.click/blog/best-one-handed-gaming-keyboards-2026.php`, tags `gaming`, `hardware`, `keyboards`, `tutorial`, the live Azeron Cyborg II WebP as cover image, absolute live product image URLs, and DEV.to YouTube liquid embed `{% youtube mO8YzzTbdtE %}`. Verification passed: `dev-to-publish.py --list` reports the article as PUBLISHED, API detail returns HTTP 200 with the correct canonical URL/tags/title, and the public page returns HTTP 200 with the title, canonical URL, cover image marker, YouTube marker, and KeyboardTester link present. Product Hunt could not be posted programmatically because `.env` has no Product Hunt token and Product Hunt's own posting guidance requires a personal account and a direct product URL rather than a blog/article URL. Prepared a manual Product Hunt update package at `seo-audit-2026-05-26/producthunt-keyboardtester-update-package.md`, using `https://keyboardtester.click/` as the primary product URL and the new article URL only for first-comment/update context.
- Published a standalone first-person DEV.to essay from `dev-to-articles/09-ant-hill-problem-superintelligence.md` after the user requested a human, thought-provoking article based on the Stephen Hawking / ants / superintelligence analogy. DEV.to article ID `3757129`, public URL `https://dev.to/nasirazizawan/the-ant-hill-problem-why-superintelligence-does-not-need-to-hate-us-2hjo`. The post intentionally has no canonical URL, no cover image, no outbound links, no KeyboardTester promotion, and no source list, matching the user's "no link just an article" instruction. Tags: `ai`, `psychology`, `ethics`, `future`. Verification passed: `dev-to-publish.py --list` reports PUBLISHED, DEV.to API detail returns HTTP 200 with the expected title/tags and body Markdown containing `0` Markdown links and `0` bare HTTP URLs, and the public page returns HTTP 200 with the title and key phrase present.

### 2026-05-24 (Codex)
- Investigated the GA4 anomaly where homepage landing page `/` surged on 2026-05-22. Used the GA4 Data API for property `properties/537895037` with production hostname filters and compared server access logs from HostArmada. GA4 evidence: homepage sessions jumped from 39 on 2026-05-21 to 1,975 on 2026-05-22, then fell to 21 on 2026-05-23; 1,931 sessions were `(direct) / (none)`, nearly all had no referrer, and the spike compressed into GA hours 16-18. Event mix was synthetic: 5,790 `page_view`, 1,968 `session_start`, 1,965 `first_visit`, 3,333 `user_engagement`, but only 8 `scroll` events across ~1,967 users. Server logs showed abnormal crawler/headless activity during the corresponding server window, including `HeadlessChrome` and bot UAs, but only modest 4xx/5xx counts and no sustained May 23/May 24 GA recurrence. DNS check showed nameservers remain HostArmada (`ns1.dal1.hostarmada.net`, `ns2.dal1.hostarmada.net`), not Cloudflare. Conclusion: likely one-off synthetic/browser automation or GA spam-like traffic, not a confirmed breach or ongoing DDoS. Evidence files: `dirty-data/ga4-may22-homepage-spike-investigation-2026-05-24.json` and `dirty-data/server-logs-2026-05-24/server-log-may22-summary.json`.
- Follow-up deployed for the download page and shared footer only. Removed the duplicate blue `Get in Store app` CTA from the Microsoft Store card in `pages/download.php` because the official Microsoft Store badge already opens the Store listing; kept the badge visible as the single Store action. Restored compact desktop footer spacing in `includes/components/site-chrome.php` after the added Download link made the footer too tall, while preserving 44px footer link/contact tap targets on mobile. Deployed `pages/download.php` and `includes/components/site-chrome.php` via SFTP/paramiko. Verification passed: local and remote PHP lint clean; live download page HTTP/rendered checks confirmed no `Get in Store app` text, no `dl-store-product__button`, Store badge present, no sidebar, no PHP error markers, footer Download link present, desktop `min-height: 26px` and mobile `min-height: 44px` rules present.
- Follow-up deployed scoped mobile CWV/accessibility fixes after PageSpeed flagged mobile LCP/INP plus forced reflow, contrast, and touch-target issues. Moved the homepage LCP hero image preload before `head-common.php` and added a concrete `href` fallback for the 1440 WebP candidate; removed ad-slot geometry reads (`offsetParent`, iframe `offsetHeight`/`offsetWidth`, and related blocker probe layout reads) from `includes/head-common.php` and `includes/components/adsense-slot.php`; changed popular-tool category labels from low-contrast accent colors to dark gray/light gray; increased footer links/contact items to 44px minimum touch targets; and added a shared footer `Download` link to `/pages/download.php`. Deployed `index.php`, `includes/head-common.php`, `includes/components/adsense-slot.php`, `includes/components/site-chrome.php`, `includes/components/tool-popular-tools.php`, and `pages/download.php` via SFTP/paramiko. Verification passed: local and remote PHP lint clean; live homepage HTTP 200 with one H1, no PHP error markers, preload before common head, no `offsetParent` or ad iframe geometry-read markers, footer Download link present, 44px footer target rule present, and dark category text rule present; live browser confirmed footer Download target height 44px, no horizontal overflow, no ad geometry markers in source, and popular-card category contrast color applied. IndexNow returned HTTP 200. Note: CrUX/Core Web Vitals field assessment is a 28-day window, so GSC/PageSpeed field status will lag behind the deployed fixes.
- Refreshed and deployed `pages/download.php` to match the newer site chrome. The page now suppresses the old left sidebar, uses a page-scoped white Microsoft/Fluent-style header treatment, presents Microsoft Store as the recommended Windows install option with a compact product-card hero panel (`KeyboardTester.click`, category, Free, Windows 10/11, blue `Get in Store app` CTA, and official Store badge), keeps the existing live `/desktop/` installer and portable EXE URLs, removes the old fake aggregate rating schema, and uses cleaner source text with no mojibake icons. Deployed only `pages/download.php` via SFTP/paramiko. Verification passed: local and remote PHP lint clean; local and live HTTP 200 checks confirmed Microsoft Store/App Store URL presence, installer URL preservation, no sidebar markup, and no mojibake; in-app browser desktop/mobile checks confirmed one H1, five download cards, Store links visible, white header, Store product card, no sidebar, and no horizontal overflow; IndexNow endpoint returned HTTP 200.
- Published the DEV.to canonical cross-post for the Rapid Trigger article from `dev-to-articles/07-rapid-trigger-keyboard-settings-cs2-valorant.md` using `dev-to-publish.py` and the existing `.env` `DEVTO_API_KEY`. DEV.to article ID `3738185`, public URL `https://dev.to/nasirazizawan/rapid-trigger-keyboard-settings-for-cs2-and-valorant-start-fast-not-fragile-3bpk`. The post uses canonical URL `https://keyboardtester.click/blog/rapid-trigger-keyboard-settings-cs2-valorant.php`, tags `gaming`, `hardware`, `keyboards`, `tutorial`, DEV.to cover image from the live hero WebP, absolute live article/diagram image URLs, and DEV.to YouTube liquid embed `{% youtube 4ACFM3bwJVE %}`. Verification passed: DEV.to API list reports the article as published, API detail shows `published_at=2026-05-24T06:35:13Z` with the correct canonical URL/tags/cover image, public page returns HTTP 200 and contains the title, canonical article URL, cover image marker, YouTube marker, and Keyboard Tester tool link; all referenced live KeyboardTester images returned HTTP 200 as `image/webp`.
- Replaced the blog share button text labels with icon-only controls in the shared blog article component. Updated `includes/components/blog-article-ads.php` only: added inline SVG icons for native share, copy, Facebook, X, LinkedIn, Pinterest, WhatsApp, and email; kept `aria-label`/screen-reader labels and title tooltips; added brand-colored icon button styling for light/dark themes; and updated the JavaScript fallback so older custom posts also generate icon buttons. Deployed the single scoped file via SFTP/paramiko. Verification passed: local and remote PHP lint clean; live Rapid Trigger and custom fallback blog posts return HTTP 200 with icon SVGs and no old visible text-button markup; live Browser checks confirmed icon buttons, accessible labels, Pinterest links, and no console errors; no non-empty public `error_log` files found.
- Added live social share buttons to blog posts through the shared article helpers. Updated `includes/components/blog-article-ads.php` with `kbtRenderBlogShareButtons()`, share-bar styling, native share/copy-link behavior, direct Facebook/X/LinkedIn/Pinterest/WhatsApp/Email share links, and a JavaScript fallback that auto-mounts share bars on older custom blog posts that do not call the standard intro/footer helpers. Updated `includes/components/blog-template-enhancements.php` so standard posts render top and bottom share bars server-side; updated ignored article `blog/rapid-trigger-keyboard-settings-cs2-valorant.php` with explicit server-rendered top/bottom share bars. Deployed only those 3 scoped files via SFTP/paramiko. Verification passed: local and remote PHP lint clean; live HTTP 200 checks for Rapid Trigger, best mechanical keyboard, and best gaming mouse posts; live Browser check confirmed two share bars, Pinterest/Facebook links, custom-post fallback, and no console errors; IndexNow endpoint returned HTTP 200. No sitemap regeneration needed because no URLs changed.
- Deployed the approved keyboard tester keyup/release feedback and Rapid Trigger blog article live via SFTP/paramiko temp uploads plus `posix_rename()`. Uploaded 14 scoped files only: `assets/js/keyboard-tester-english.js`, `assets/js/keyboard-tester-english.min.js`, `tools/keyboard_tester_english.php`, `assets/css/localized-keyboard-visual.css`, `blog/rapid-trigger-keyboard-settings-cs2-valorant.php`, `blog/posts-data.php`, `sitemap.xml`, `sitemap.txt`, and six files under `blog/images/rapid-trigger-keyboard-settings/`. Regenerated `sitemap.xml`/`sitemap.txt` locally before deploy; sitemap now includes `https://keyboardtester.click/blog/rapid-trigger-keyboard-settings-cs2-valorant.php`. Remote verification passed: PHP lint clean for `tools/keyboard_tester_english.php`, the article, and `blog/posts-data.php`; live homepage, `/tools/keyboard-tester/`, `/languages/spanish/`, the article, blog index, sitemap XML, and sitemap TXT return HTTP 200 with no PHP error markers; all article images return HTTP 200; article has one H1, canonical, BlogPosting, FAQPage, and VideoObject schema; blog index shows the card; sitemap XML/TXT include the article; IndexNow endpoint returned HTTP 200 and included the article in the submitted URL list. Live headless Chrome/CDP test confirmed KeyA adds `is-pressed` on keydown, does not increment on repeat keydown, adds `is-released` on keyup, and returns to the persistent tested-key state. Archived a pre-existing root `error_log` containing only an old 2026-05-23 direct-request fatal from `kbt-blog-style.php`; final scan found no non-empty public `error_log` files.
- Updated the local keyboard tester key feedback only; no live deploy. In `assets/js/keyboard-tester-english.js` and regenerated `assets/js/keyboard-tester-english.min.js`, the shared desktop tester now tracks live held keys, adds an `is-pressed` visual state on keydown, ignores browser auto-repeat keydown events for the press counter/history while a key remains held, adds an `is-released` visual state on keyup, clears stale held states on blur/visibility change, and applies the same behavior to pointer/touch interaction on the virtual keys. Added matching `is-pressed`/`is-released` color styles in `tools/keyboard_tester_english.php` and `assets/css/localized-keyboard-visual.css` so English and localized shared testers show the release feedback. Verification passed: `node --check` for source and minified keyboard JS, `php -l tools/keyboard_tester_english.php`, local HTTP 200 checks for `/`, `/tools/keyboard-tester/`, and `/languages/spanish/`, and a headless Chrome/CDP key event test confirmed KeyA adds `is-pressed` on keydown, does not increment on repeat keydown, adds `is-released` on keyup, then returns to the persistent tested-key state.
- Created a local-only researched blog draft for `Rapid Trigger Keyboard Settings for CS2 and Valorant: Actuation, SOCD and Testing Guide` at `blog/rapid-trigger-keyboard-settings-cs2-valorant.php`. Research inputs included official Wooting Rapid Trigger/CS2 guidance, Valve CS2 input automation notes, RTINGS Hall Effect/Rapid Trigger explainers, Razer Snap Tap documentation, Riot VALORANT anti-cheat support wording, ProSettings gear stats, Reddit/community question patterns, public search results, YouTube oEmbed verification, and local GSC query evidence for actuation/Hall Effect/Snap Tap-related terms. Added local assets in `blog/images/rapid-trigger-keyboard-settings/` (hero plus Rapid Trigger/reset, SOCD/Snap Tap, and testing workflow diagrams) and a local blog registry card in `blog/posts-data.php`. Verification passed locally: PHP lint for the article and posts registry, article HTTP 200, one H1, FAQPage/VideoObject/BreadcrumbList schema present, no runtime error markers, no missing local image references, blog index card visible, desktop screenshot reviewed, and CDP mobile emulation reported `innerWidth=390`, `scrollWidth=390`, `post=390`.
- During user review, tightened the draft introduction by removing the broad "no longer a niche curiosity / search data and Reddit threads" sentence and replacing it with a more direct practical problem statement. Also replaced the outline-like "This guide is written..." paragraph with reader-facing copy about changing one movement setting at a time and rejecting profiles that cause accidental inputs. Re-audited the full draft for user-facing guide quality: removed the visible search/keyword/Search Console/Reddit-style section, replaced it with a practical "Before You Change the Numbers" tuning section, removed the BlogPosting keyword-list artifact, tightened meta/process wording, renamed sources to "References for Settings and Rules", and removed the now-unused ProSettings reference. Re-ran PHP lint and local HTTP checks successfully.
- No live deploy, no sitemap regeneration, and no IndexNow submission were performed because the user asked to approve the article locally first.

### 2026-05-23 (Codex)
- Prepared FixLens for Microsoft Store submission only, per user clarification that this is not a KeyboardTester.click website publishing task. Restored/kept the visible `http://keyboardtester.click` firm link inside FixLens after the user clarified that Store publication should still mention KeyboardTester.click as the company/firm link. Rebuilt `PC Registry Scanner/bin/FixLens.exe`, `bin/FixLens.Cli.exe`, and `dist/FixLensSetup.exe`; fixed the installer self-delete retry so uninstall launched from the install folder removes the full temp install directory. Verification passed: silent install to a temp path with spaces, installed `FixLens.Cli.exe --list-modules`, and silent uninstall cleanup. Created `PC Registry Scanner/microsoft-store-fixlens/` with versioned package `package/FixLensSetup-1.0.0.exe`, SHA-256 checksum, Store listing draft, Partner Center checklist, certification notes, privacy-policy draft, license terms draft, screenshot, box art, poster art, and logo. Current blocker for the EXE/MSI Store path: the installer is `NotSigned`; Partner Center login/name reservation and either trusted code signing for EXE/MSI or an MSIX package path are still required. No live site deploy.

### 2026-05-22 (Codex)
- Pulled a GA4 traffic summary for `properties/537895037` (`KeyboardTester.click`, timezone `Asia/Karachi`). Property was created 2026-05-14, so the first clean full window is 2026-05-15 through 2026-05-21. Production host `keyboardtester.click` had 1,404 sessions, 1,239 total users, 2,562 page views, 783 engaged sessions, 55.8% engagement rate, 44.2% bounce rate, and 2m41s average session duration; raw GA totals also included `localhost` noise of 47 sessions and 916 page views. Main channel was Organic Search with 1,029 sessions, led by `google / organic` with 991 sessions. On 2026-05-22 partial-day data showed an abnormal low-engagement spike; the latest pull during the check reached 728 production-host sessions, 726 users, 1,423 views, only 5 engaged sessions, 0.7% engagement rate, and 99.3% bounce rate. Most traffic was `Unassigned`/`(not set)` and Direct, with homepage `/` dominating. Treat 2026-05-22 as suspicious/incomplete until server logs or tomorrow's GA data confirm whether it was bot-like traffic, a campaign/spike, or tracking artifact.
- Updated the newly copied local desktop app under `PC Registry Scanner/` only. Added a real WinForms `About` page to the left navigation with product identity, author `Nasir A. Awan`, and clickable firm URL `http://keyboardtester.click`; added the same attribution to `PC Registry Scanner/README.md`. Rebuilt both `bin/PCRegistryScanner.exe` and `bin/PCRegistryScanner.Cli.exe` using process-scoped PowerShell execution-policy bypass because the direct script launch was blocked by local policy. Verification passed: GUI and CLI builds completed, and `PCRegistryScanner.Cli.exe --list-modules` returned module rows. No live site deploy.
- Follow-up fixed text clipping/visibility in the local `PC Registry Scanner` WinForms app after user screenshot showed About-page headings cut off at the current Windows scaling. Updated `src/Program.cs` with measured text-height helpers and applied larger/safer text bounds to the About page, category page headers, Findings header, Logs header, dashboard hero/card labels, category badges, side navigation rows, and module tiles. Rebuilt both GUI and CLI binaries; `PCRegistryScanner.Cli.exe --list-modules` smoke test passed. No live site deploy.
- Renamed the local desktop app branding to `FixLens` across visible WinForms UI, About page, exported report text/HTML, backup/action-log labels, manifest description/assembly name, README, UI audit doc, and build/run scripts. Build outputs now use `bin/FixLens.exe` and `bin/FixLens.Cli.exe`; scripts remove stale `PCRegistryScanner*.exe` binaries. Closed the old running `PCRegistryScanner.exe` instance and removed the stale old binary after rebuilding. Verification passed: `FixLens.Cli.exe --list-modules` returned module rows and only the two FixLens executables remain in `bin`. No live site deploy.
- Designed and integrated a custom FixLens logo for the local desktop app: blue-to-teal rounded square with a white diagnostic lens, check mark, and small sparkle. Added generator `PC Registry Scanner/generate-fixlens-logo.ps1`, source assets `src/FixLens-logo.svg`, `src/FixLens-logo-256.png`, and multi-size `src/FixLens.ico`. Updated `Program.cs` so the form loads the executable icon and the side rail/About card use a matching custom-drawn logo badge. Updated `build.ps1` and `Run.bat` to embed `src/FixLens.ico` with `/win32icon`, and documented logo regeneration in `README.md`. Rebuilt `bin/FixLens.exe` and `bin/FixLens.Cli.exe`; verification passed with `FixLens.Cli.exe --list-modules`, only FixLens executables in `bin`, and extracted executable icon reporting `32x32`. No live site deploy.
- Created a standalone Windows setup installer for the local FixLens desktop app. Added `PC Registry Scanner/installer/FixLensSetup.cs` and `PC Registry Scanner/build-installer.ps1`; the generated `PC Registry Scanner/dist/FixLensSetup.exe` embeds `FixLens.exe`, `FixLens.Cli.exe`, `FixLens.ico`, and `README.md`, installs per-user to `%LOCALAPPDATA%\Programs\FixLens`, creates optional desktop/Start Menu shortcuts, writes the HKCU uninstall entry, and supports silent install/uninstall switches. Verification passed: built the installer, installed to a temporary path with spaces, confirmed required files and uninstall registry metadata, ran `FixLens.Cli.exe --list-modules`, then uninstalled cleanly with folder and registry key removed. No live site deploy.
- Fixed the local FixLens dashboard category tabs so the full card surface is clickable, including the icon, title, status line, count line, and empty card area. Added composite click wiring plus Enter/Space activation support on the card shell in `PC Registry Scanner/src/Program.cs`, rebuilt `bin/FixLens.exe`, rebuilt `bin/FixLens.Cli.exe`, rebuilt `dist/FixLensSetup.exe`, silently refreshed the installed per-user copy under `%LOCALAPPDATA%\Programs\FixLens`, and verified both repo and installed `FixLens.Cli.exe --list-modules` still return the module catalog. No live site deploy.
- Follow-up fixed the four top dashboard metric cards after the user screenshot clarified those were the non-clickable tabs: `Issues` and `Safe fixes` now open filtered Findings views, `Cleanup` opens the Cleanup module page, and `Startup entries` opens the Performance module page. The full metric card surface is wired as a composite click target with keyboard activation support. Rebuilt `bin/FixLens.exe`, `bin/FixLens.Cli.exe`, and `dist/FixLensSetup.exe`; silently refreshed `%LOCALAPPDATA%\Programs\FixLens`; verified the installed CLI still lists modules; reflection UI smoke check confirmed card clicks route to `Findings`, `Findings`, `Cleanup`, and `Performance`. No live site deploy.
- Fixed and deployed the live homepage desktop Lighthouse performance issue reported 2026-05-22. Scoped changes: `assets/js/keyboard-tester-english.js` now avoids shrinking the reserved keyboard container height during initial scaling, regenerated `assets/js/keyboard-tester-english.min.js` from 50.8 KB to 24.9 KB, added `images/home/headphones-diagnostic-hero-1440.{webp,jpg}`, and updated `index.php` plus `help/brief-keyboard-tester.php` preload/picture srcsets so desktop Chrome selects the 1440 WebP instead of the 1792 WebP. Deployed with SFTP/paramiko temp uploads plus `posix_rename()`. Verification: local PHP lint and JS syntax checks passed; live homepage HTTP 200 contains the 1440 srcsets and fresh keyboard JS version; new 1440 image assets return HTTP 200; live browser probe at 1350x940 reported CLS `0`, one H1, no console errors, keyboard wrapper scaled without inline container height, and hero currentSrc `headphones-diagnostic-hero-1440.webp`; live Lighthouse desktop returned FCP `0.5s`, LCP `0.6s`, TBT `0ms`, CLS `0`, Speed Index `1.8s`, performance `0.97`; devtools-throttled Lighthouse showed residual CLS `0.007` from Google ad fonts only; IndexNow returned HTTP 200 and no non-empty live `error_log` files were present.

### 2026-05-21 (Codex)
- Ran a full live production health audit after the user asked whether the complete site is live and OK. Initial crawl of live `sitemap.xml` checked 882 URLs: all returned HTTP 200, 881 were HTML, JSON-LD parsed, H1/title/meta checks mostly passed, but it found one real duplicate canonical on `mouse-and-keyboard-test-tools.php`, one missing OG/Twitter image reference to `images/og-keyboard-mouse-tools.png`, and a non-empty live `/tools/error_log`. The CPU/GPU warning flags were false positives from visible tool safety copy, not PHP warnings. Fixed and deployed `mouse-and-keyboard-test-tools.php` so `head-common.php` does not add a second canonical and OG/Twitter image tags use the existing `images/shared/keyboard-and-mouse.png`. Also fixed direct legacy requests to `tools/keyboard_tester_{arabic,spanish,french,german,japanese,korean,portuguese,russian}.php` by adding direct-request 301 guards to the modern localized keyboard pages, while preserving include behavior. Archived the old `/tools/error_log` under `/home2/keyboar1/dirty-data-cpanel/2026-05-21/public-error-logs/`. Final rerun report saved at `seo-audit-2026-05-21/live-site-health-audit-2026-05-21-after-fixes.md`: 882/882 sitemap URLs HTTP 200, 375/375 discovered same-origin assets HTTP 200, 0 page errors, 0 bad assets, 0 PHP error markers, 0 JSON-LD parse errors, 0 title/canonical/H1/meta-description/noindex errors, legacy keyboard direct URLs all 301 to localized homes, HTTP and www canonical redirects pass, and no non-empty live `error_log` files remain.
- Checked the user's report that GSC External links was empty after showing 63 earlier in the day. Verified Search Console API access with `google-credentials.json`: service account still has `siteOwner` on `sc-domain:keyboardtester.click`; API exposes only `searchanalytics`, `sitemaps`, `sites`, `urlInspection`, and mobile-friendly testing resources, so the GSC Links report table cannot be pulled through the official API. Current API evidence does not show a site-wide Google issue: sitemaps list contains `sitemap.xml` and `sitemap.txt` with `errors=0` and `warnings=0`; latest available GSC performance day `2026-05-19` has `161` clicks / `12,779` impressions; 7-day GSC total for `2026-05-12..2026-05-19` is `1,243` clicks / `122,788` impressions / `1.01%` CTR / avg position `9.4`. GA4 MCP for `properties/537895037` also works and shows traffic still flowing since the new property start: daily sessions were 195, 207, 202, 233, 217, 215, and 127 partial today for `2026-05-15..2026-05-21`; referral sessions exist today and across the period, with top source/medium rows including `chatgpt.com / referral`, `facebook.com / referral`, `search.google.com / referral`, `claude.ai / referral`, `t.co / referral`, `scoop.it / referral`, and `worthytutors.com / referral`. Conclusion: treat the empty GSC Links UI as a property-selection/reporting/sample-refresh issue unless it persists for multiple days; first check the Domain property `keyboardtester.click` and try the UI export.
- Upgraded the live Mouse Drag Click Test tool at `mouse-drag-test.php` by changing the shared partial `tools/mouse_drag_tool.php` only, so English and all localized mouse-drag routes inherit the same functionality. Added button filtering (any/left/right), min-gap filtering for noisy switch chatter, avg click-gap and consistency metrics, filtered-click count, click path distance, technique classification, recent click event history, visual click dots, copy-summary, and CSV export. Deployed via SFTP/paramiko with remote PHP lint. Verification passed locally and live: English plus all 8 localized routes returned HTTP 200 with the new controls/analysis panel, no PHP error markers, rendered tool JavaScript passed `node --check` on every route, live browser interaction on `https://keyboardtester.click/mouse-drag-test.php` updated CPS/gap/consistency/summary and stopped cleanly with zero console errors, no non-empty live `error_log` files were found, and IndexNow returned HTTP 200 with `mouse-drag-test.php` included.
- Local-only homepage guide/FAQ standardization pass after user requested a consistent template before adding any new "You Ask, We Answer" section. Used two-agent workflow: one query/intent research agent for GSC/search/forum-style FAQ candidates, and one structure/design agent for reusable section rules. Updated `index.php` to use a balanced left guide plus exactly 7 FAQ items, updated the FAQ panel heading to the reusable wording `Quick answers` / `Common questions before you start`, updated `includes/schema.php` so homepage FAQ JSON-LD matches the visible 7 questions, and adjusted `assets/css/index-modern.css` plus regenerated `assets/css/index-modern.min.css` so the guide and FAQ columns render at equal height. No live deploy. Verification passed locally: homepage HTTP 200, PHP lint clean for `index.php` and `includes/schema.php`, one H1, 7 visible FAQ items, 7 FAQ schema items, schema questions matching visible FAQ questions, 7 guide steps, guide/FAQ word counts balanced at 346 vs 363 words, no horizontal overflow, and no browser console warnings/errors in the checked local render.
- Follow-up live deployed the homepage guide/FAQ standardization after user approval. Uploaded only `index.php`, `includes/schema.php`, `assets/css/index-modern.css`, and `assets/css/index-modern.min.css` via SFTP/paramiko temp uploads plus overwrite-safe rename; no sitemap file changes were needed. Remote PHP lint passed for `index.php` and `includes/schema.php`. Live verification passed: homepage HTTP 200, old `7 researched keyboard tester questions` heading absent, new `Quick answers` / `Common questions before you start` heading present, one H1, 7 visible FAQ items, 7 FAQ schema items, visible/schema questions matching, updated `index-modern.min.css` served with fresh filemtime query and HTTP 200, no PHP error markers, no non-empty live `error_log` files, and IndexNow submission returned HTTP 200 with the homepage included.
- Fixed the live `keyboard-sound-test.php` tool after user reported it was not working. Root cause was invalid inline JavaScript in the English page: several ternary operators had lost their `?`, so the browser stopped the tool script before the Start Listening button could run. Patched `keyboard-sound-test.php` to restore the valid ternaries for dB calculation, dB label output, audio context/analyser fallbacks, and peak-frequency formatting, then deployed only that file via SFTP/paramiko. Verification: local PHP lint clean; local rendered inline scripts passed `node --check` with zero syntax failures; remote PHP lint clean; live rendered inline scripts passed `node --check` with zero syntax failures; live page has one H1, Start Listening button present, fixed `dbRaw` expression present, broken ternary patterns absent, no PHP error markers, browser-load check reported no console warnings/errors, no non-empty live `error_log` files, and IndexNow returned HTTP 200 with `keyboard-sound-test.php` included.
- Follow-up checked the Keyboard Switch Sound Analyzer across all localized routes and added related functionality to English plus the shared localized renderer. New tool controls: adjustable detection threshold, room-noise calibration, live confidence score, recent-hit count, and last-5 detection history. Updated `keyboard-sound-test.php` for the English/modern-captured tool and `includes/lang-tool-pages/keyboard-sound-test.php` for `es`, `fr`, `de`, `pt`, `ar`, `ru`, `ja`, and `ko`; no wrapper page changes were needed. Deployed both files via SFTP/paramiko. Verification: local and remote PHP lint passed; local and live checks across English plus all eight localized routes returned HTTP 200, one H1, the new threshold/calibration/confidence/history controls present, no broken ternary patterns, no PHP error markers, and zero inline JS syntax failures. Browser smoke checks passed on English and Spanish with visible controls and no console warnings/errors. No non-empty live `error_log` files found. IndexNow returned HTTP 200 with English and localized keyboard sound URLs included.
- Drafted a local-only buyer-guide blog article at `blog/most-expensive-luxury-keyboards-top-7.php` targeting `most expensive luxury keyboards`, `most expensive keyboard`, and `luxury mechanical keyboard`. The draft uses official/current product pages for prices and buying links, adds seven WebP product photos under `blog/images/luxury-keyboards/`, includes a fast-answer price table, product-by-product buying notes, related KeyboardTester.click test links, source list, FAQPage, BlogPosting, BreadcrumbList, and VideoObject schema with an Adam Savage/Tested Norbauer video. Updated `blog/posts-data.php` so the article appears in the local blog registry. Initial draft stage had no live deploy, sitemap regeneration, or IndexNow submission. Verification passed locally: PHP lint clean for the article and `blog/posts-data.php`; local HTTP render returned 200 with one H1, four JSON-LD blocks, and zero missing local image references.
- Follow-up live deployed the luxury keyboards article after user approval. Regenerated `sitemap.xml` and `sitemap.txt` so the new article and seven image entries are included, then deployed the scoped files via SFTP/paramiko: article PHP, `blog/posts-data.php`, seven WebP images, and both sitemap files. Live verification passed: remote PHP lint clean for the article and blog registry; article HTTP 200 with one H1, one title, four JSON-LD blocks, canonical present, and no PHP error markers; `/blog/` shows the new card; all seven WebP images return HTTP 200 as `image/webp`; live XML/TXT sitemaps include the article; IndexNow submission returned HTTP 200 with the new URL included; no non-empty live `error_log` files found.

### 2026-05-20 (Codex)
- Follow-up refined the all-tools grid ad placement after the user observed that a one-cell Google ad makes the tool-card row look uneven. `includes/render-all-tools.php` now treats `emptyCells === 1` differently: the ad is moved to the next row with `data-ad-layout="next-row"` and spans the full current grid width, while 2- or 3-cell gaps still use the in-row `fill-gap` layout. Deployed the renderer with SFTP/paramiko. Verification: local and remote PHP lint passed; live source shows `next-row`, `adLayout`, and `adSpan`, with no old `two-row`, modal, or ad-open hooks; in-app browser verification on live all-tools showed one-empty-cell categories rendering `data-ad-layout="next-row"` and full-column span, two-empty-cell categories still rendering `fill-gap`, one H1, no horizontal overflow, and no modal/open hooks.
- Follow-up replaced the earlier two-row all-tools ad experiment with final-row gap-fill placement matching the user's design direction. `includes/render-all-tools.php` now calculates the actual rendered column count, checks each category's final-row empty cells, and shows exactly one grid ad only when there is a visible gap. The ad card gets `data-ad-layout="fill-gap"` plus `data-ad-span="1"`, `"2"`, or `"3"` so 7 tools fill one cell, 6 tools fill two cells, 5 tools fill three cells, and exact multiples show no in-grid ad. When ads are blocked, the same reserved gap slot shows the existing `PUT YOUR BRAND HERE` house fallback. Also removed the leftover hidden all-tools modal/form CSS, markup, and JS open hooks so ad inquiries route to `advertise.php` only. Deployed `includes/render-all-tools.php` via SFTP/paramiko. Verification: local and remote PHP lint passed; live source returns HTTP 200 with one H1, `fill-gap` and `emptyCells`, no `two-row`, no `data-kbt-ad-modal`, no `data-kbt-ad-open`, no `data-kbt-partner-open`, and no modal CSS marker; live `advertise.php?placement=all_tools_bottom&package=three_month` returns HTTP 200 with one H1 and working form prefill; in-app browser on the live all-tools page shows no horizontal overflow, no modal/open hooks, one gap-fill ad for categories with incomplete final rows, and zero visible grid ads for exact-full rows.
- Replaced first-party/local ad popup behavior with a dedicated `advertise.php` inquiry page and deployed it live with SFTP/paramiko. `includes/components/adsense-slot.php` now builds normal `advertise.php?placement=...&package=...` links for house ads and partner fallbacks, removes the local-ad modal/open attributes from rendered ad cards, and keeps the old modal function dormant for compatibility. `includes/render-all-tools.php` now turns all-tools sponsor package CTAs into normal page links, renders top and bottom sponsor blocks, and no longer shows local sponsor modal markup. Added `advertise.php`, wired it to the existing `api/send-ad-inquiry.php` endpoint with signed captcha tokens, package preselection, placement carry-through, and no exposed private SMTP/recipient config. Added the page to `$pages`, `generate-sitemap.php`, `submit-indexnow.php`, regenerated `sitemap.xml` and `sitemap.txt`, deployed all scoped files, and submitted IndexNow. Verification: local/remote PHP lint passed for deployed PHP files; live `advertise.php` returns HTTP 200 with one H1, form present, correct package/placement prefill, zero modal tags, and no private env/SMTP markers; live homepage returns HTTP 200 with 8 `advertise.php` house-ad links, 8 `PUT YOUR BRAND HERE` titles, zero ad/partner open tags, and zero modal tags; live all-tools returns HTTP 200 with one H1, 2 sponsor blocks, 29 advertise links, zero open tags, and zero modal tags; live sitemap XML/TXT include `advertise.php`; no non-empty live `error_log` files found after verification.
- Follow-up after user feedback on `pages/all-tools.php`: removed the visible GEO-style fact-card summary, removed the duplicate category jump row, and removed the top sponsor banner so the page moves from the intro straight into the existing category filter/tool sections. Kept the bottom sponsor inquiry block only. Deployed `includes/render-all-tools.php` via SFTP/paramiko. Verification: live all-tools HTTP 200, one H1, zero `at-ai-summary` tags, zero `at-guide-links` nav tags, zero top sponsor tags, one bottom sponsor tag, one category filter, zero modal/open-hook tags, and no non-empty live `error_log` files.
- Follow-up fixed the all-tools Google-ad/grid disparity. The all-tools renderer now places visible grid ad cards as one tall `grid-row: span 2` desktop slot per two-row tool group instead of appending multiple normal-size ad cards into the same row. When ads are blocked or empty, the same reserved two-row slot shows the existing `PUT YOUR BRAND HERE` house fallback; tablet/mobile ad slots become normal full-row blocks to avoid awkward gaps. Deployed `includes/render-all-tools.php` via SFTP/paramiko. Verification: local PHP lint clean; local ad-blocked render shows house fallback cards with `data-ad-layout="two-row"` and `grid-row: span 2`; live source contains the new two-row/full-row CSS and updated `groupSize` JS; live browser check on all-tools shows visible Google ad slots using `data-ad-layout="two-row"` / `grid-row: span 2`, one H1, zero modal/open-hook tags; remote PHP lint passed and no non-empty live `error_log` files were found.
- Implemented and live deployed the AI-search audit gap fixes the user approved. Added/updated LLM-facing discovery with expanded `llms.txt` and new `llms-full.txt`; upgraded `pages/all-tools.php` renderer with an answer-ready summary, `dateModified`, and full `CollectionPage`/`ItemList` schema; added guarded `WebApplication` and `BreadcrumbList` schema coverage for modern inline/legacy localized tool rendering; made localized meta output UTF-8 safe so invalid legacy strings cannot blank meta descriptions or JSON-LD; expanded short localized meta descriptions; added breadcrumb JSON-LD to remaining legacy info/blog pages; deployed four localized password wrappers that were modern locally but still legacy on production. Deployment used SFTP/paramiko only. Verification: remote PHP lint passed for all deployed PHP files; live targeted checks passed for all-tools JA/KO, blog, localized password pages, Japanese color range, Korean WhatsApp sentiment analyzer, About, Resources, and Language Tools; final live sitemap audit scanned 880 URLs / 879 HTML pages with 0 bad statuses, 0 missing canonicals, 0 bad H1 pages, 0 PHP error pages, 0 JSON-LD parse errors, 879/879 BreadcrumbList coverage, and 0 meta descriptions under 120 characters.
- Local-only revamp of the first-party/house ad fallback creatives after user feedback that the ads were still one repeated template with changed colors. Reworked `includes/components/adsense-slot.php` so fallback ads now render distinct template structures by placement: compact hero/banner, boxed post-test strip, tall pricing rail, product-card grid ad, and footer ribbon. Removed the old text-heavy body/chip/visual markup from rendered ads and kept the visible copy short. Updated `assets/css/global.css` with a new structural template layer, regenerated `assets/css/global.min.css`, bumped `includes/head-common.php` global CSS cache to `v=4.0`, and bumped `sw.js` to `kbt-sw-v16`. No live deploy. Verification: XAMPP PHP lint passed for `includes/components/adsense-slot.php`, `includes/render-all-tools.php`, and `includes/head-common.php`; `node --check sw.js` passed; `git diff --check` passed for touched files; localhost homepage and all-tools returned HTTP 200 with `global.min.css?v=4.0`; rendered homepage source contains hero/post/rail template markers and all-tools contains grid template markers; old `kbt-house-ad__body`, `kbt-house-ad__chips`, and `kbt-house-ad__visual` markup no longer appears in the local rendered pages.
- Follow-up fixed the blank local homepage ad box seen with an ad blocker enabled. `includes/head-common.php` now forces house-ad fallback on localhost only, skips loading the Google ad script locally, and also probes pending `adsbygoogle` elements that blockers hide before the normal fallback runs. `includes/components/adsense-slot.php` now forces all house-ad creative titles to `PUT YOUR BRAND HERE`. `assets/css/index-modern.css` / `.min.css` now make pending homepage hero and after-tool ad shells transparent instead of white, and remove the outer white shell when house fallback is active. No live deploy. Verification: local HTTPS homepage contains `forceLocalHouseAds = true`, manual slot probes, `global.min.css?v=4.0`, and eight `PUT YOUR BRAND HERE` fallback titles; local all-tools contains 21 `PUT YOUR BRAND HERE` grid fallback titles; in-app browser check showed the local hero slot switched to `kbt-ad-slot--house`, visible hero creative `558x74`, visible post-test creative `1198x154`, and no white outer hero shell.
- Follow-up centered the copy in rectangular house-ad formats after user feedback. Updated `assets/css/global.css` and regenerated `assets/css/global.min.css` so hero/banner, post-test, grid, and footer fallback ad titles/labels use centered text alignment while keeping the tall rail layout intact. No live deploy. Verification: `git diff --check` passed for the global styles; local HTTPS homepage still loads `global.min.css?v=4.0` and contains eight `PUT YOUR BRAND HERE` fallback titles; in-app browser computed styles show visible hero and post-test rectangle fallback ads with `copyTextAlign=center` and `titleTextAlign=center`.
- Live deployed the scoped house-ad fallback refresh with SFTP/paramiko: `includes/components/adsense-slot.php`, `includes/head-common.php`, `assets/css/global.css`, `assets/css/global.min.css`, `assets/css/index-modern.css`, `assets/css/index-modern.min.css`, and `sw.js`. Production keeps `forceLocalHouseAds = false` while retaining blocker probes; localhost forces house fallback for preview. Live verification passed: homepage HTTP 200 with one H1, eight `PUT YOUR BRAND HERE` fallback titles, `probeManualSlotsForBlocking`, `forceLocalHouseAds = false`, and `global.min.css?v=4.0`; live global CSS contains the template and centered text rules; live index CSS contains the transparent pending/house hero and after-tool shell rules; live `sw.js` returns `kbt-sw-v16` and `global.min.css?v=4.0`; remote PHP lint passed for the deployed PHP files; no non-empty live `error_log` files were found after verification.
- Localized the WhatsApp/text sentiment analyzer for all existing non-English site languages while keeping one shared backend/API and one shared tool implementation. Added `includes/localized-whatsapp-sentiment-page.php`, created localized routes at `languages/{arabic,french,german,japanese,korean,portuguese,russian,spanish}/whatsapp-sentiment-analyzer.php`, made `tools/whatsapp_sentiment_analyzer_tool.php` locale-aware, added Russian/Japanese/Korean to the report language selector, and expanded the local preview word lists for multiple languages. Updated the AI prompt in `api/whatsapp-deep-analyzer.php` so user-facing report strings follow the requested/detected language. Updated `includes/components/tool-list-data.php` so the analyzer appears in localized tool listings, updated `submit-indexnow.php`, regenerated `sitemap.xml` and `sitemap.txt`, deployed the scoped files via SFTP/paramiko, and submitted IndexNow. Verification: local and remote PHP lint passed for the API, shared tool, localization helper, tool-list data, submit script, and all eight wrappers; local and live page checks for all eight routes returned HTTP 200, exactly one `h1`, `id="sentiment-tool"`, no PHP error markers; live Spanish/Arabic/Korean checks confirmed selected report language defaults; live `sitemap.txt` includes the new localized URLs; live API smoke test returned `ok=true` through Groq; no live `error_log` files found after checks.
- Upgraded `whatsapp-sentiment-analyzer.php` from a lightweight browser word-count sentiment check into a deeper chat/text analyzer. Added new server endpoint `api/whatsapp-deep-analyzer.php` with session rate limiting, 18,000-character input cap, no-store/noindex headers, Groq model rotation using the existing `GROQ_API_KEY`, optional OpenRouter provider hook when `OPENROUTER_API_KEY` plus `KBT_SENTIMENT_OPENROUTER_MODELS` are later configured, strict JSON prompting, and server-side normalization of 0-1 model scores to 0-100 UI scores. Rebuilt `tools/whatsapp_sentiment_analyzer_tool.php` with professional input controls for WhatsApp exports or any text, `.txt` import, focus-person field, local preview fallback, structured report sections, red/green flags, hidden signals, speaker insights, suggested replies, copy-report action, and mobile-readable sizing. Updated page copy, schema, SEO config, tool-card text, and help content so the tool no longer claims local-only processing after adding AI analysis. Deployed the scoped files via SFTP/paramiko. Verification: local PHP lint passed for all touched PHP files; local HTTPS API smoke test returned `ok=true` via `Groq/llama-3.3-70b-versatile`; live page contains the new UI and endpoint route with no PHP error markers; live API returned `ok=true`, `SCORE=60`, `WARMTH=70` on a sample chat. In-app browser visual QA could not run because no active Codex browser pane was available.

### 2026-05-19 (Codex)
- Started manual GSC URL Inspection `Request indexing` flow in the user's logged-in Chrome for the English tool URLs that were `NEUTRAL` / unknown to Google. Before submitting, checked all 30 unknown English tool URLs live: all returned HTTP 200 with matching canonical and no `noindex`; `gpu-stress-test.php` had only visible warning copy, not a PHP warning. Confirmed GSC accepted `Request indexing` for `https://keyboardtester.click/backlight-bleed-test.php` and `https://keyboardtester.click/headphone_speaker_tester_index.php` with the dialog text `Indexing requested` / `URL was added to a priority crawl queue`. The third URL `https://keyboardtester.click/whatsapp-Brand-link-generator.php` was opened in URL Inspection but not confirmed submitted. Work paused because the Chrome extension automation became unresponsive on GSC after the second confirmation and OS-level Chrome activation targeted other Chrome tabs/windows, so continuing would risk clicking the wrong page. Progress report: `seo-audit-2026-05-19/gsc-request-indexing-progress-2026-05-20.json`; live status report: `seo-audit-2026-05-19/live-status-gsc-unknown-english-tools-2026-05-19.json`.
- Ran a current Google Search Console URL Inspection audit for English tool URLs only. Source URL set came from `includes/components/tool-list-data.php` with `tools-page` and `arabic-keyboard` excluded, producing 91 English tool URLs saved at `dirty-data/english-tool-urls-2026-05-19.txt`. Used the Search Console Domain property `sc-domain:keyboardtester.click` via `gsc-coverage-audit.py`; result: 61 indexed and 30 `NEUTRAL` / `URL is unknown to Google`. Full evidence saved at `seo-audit-2026-05-19/gsc-coverage-2026-05-19.json` and `.md`. No live deploy.
- Second mobile hero/readability follow-up after user said the first live fix still did not look good and seemed unchanged on phone. Added a stronger `kbt-mobile-hero-readable-v3` override in `assets/css/index-modern.css` and `.min.css`: mobile hero is shorter and cleaner, homepage image crop is shifted to show the workstation better, hero H1 is `36px` at 360px, hero lede is about `19.5px`-`20.5px`, the primary CTA is full-width up to `306px` with `60px` height, and category copy is bumped. Increased `pages/all-tools.php` card mobile text further through `includes/render-all-tools.php`: card title `24px`, description `18.24px`, CTA `17.92px` with `54px` height. Bumped `sw.js` to `kbt-sw-v13`, changed CSS requests in the service worker to network-first, and updated `includes/head-common.php` registration to `20260519b` to reduce stale mobile CSS. Deployed the five files via SFTP/paramiko. Verification: local and remote PHP lint passed for `includes/render-all-tools.php` and `includes/head-common.php`; live CSS contains `kbt-mobile-hero-readable-v3`, live SW contains `kbt-sw-v13`, live homepage/contrast/all-tools mobile checks at 360x800 report `overflow=0` with the new computed font sizes above; no live `error_log` files after verification.
- Follow-up fixed the mobile hero/card readability issues after the user's latest screenshots showed the homepage hero title clipping and mobile reader text still feeling too small. Updated `assets/css/index-modern.css` and `.min.css` so homepage/tool heroes use one mobile image-backed banner with overlay copy, normal wrapping hero-title lines, larger lede/button sizing, stable Microsoft Store badge placement, and no split image-above/text-below hero on mobile. Updated `includes/render-all-tools.php` mobile card CSS so tool-card media uses a stable 16:9 height, card titles wrap instead of ellipsizing, and card descriptions/CTAs render at larger mobile sizes. Deployed all three files via SFTP/paramiko. Verification: local `php -l` and remote `php -l` passed for `includes/render-all-tools.php`; live pages and live CSS returned HTTP 200 and the deployed CSS marker; Chrome mobile production checks at 360x800 and 320x740 for homepage, `contrast-test.php`, and `pages/all-tools.php` reported `overflow=0`, wrapped hero title lines inside viewport, homepage/tool hero lede around `18.56px`-`19.2px`, buttons `17.92px` with `56px` min height, and all-tools card title `21.6px`, description/CTA `16.96px`, CTA `48px` min height. No live `error_log` files were present after verification.
- Fixed the mobile homepage category strip and readability after the user screenshot showed the category rail clipped near the end and the `KeyboardTester.click` header/hero/category text too small on phones. Updated `includes/components/site-chrome.php` plus `assets/css/index-modern.css` and `.min.css`: mobile header brand/logo are larger, the mobile hero title/lede/buttons are larger, and the homepage category band now switches from a fixed grid to a horizontal flex rail with momentum scrolling, scroll snap, hidden scrollbar, safe-area start/end spacing, larger category icons/title/copy, and enough trailing space so the final `Gaming` card can be reached. Deployed all three files via SFTP/paramiko. Verification: XAMPP PHP lint clean for `site-chrome.php` and homepage brief, live homepage references a cache-busted `index-modern.min.css` containing the new rules, Chrome mobile viewport checks on live at 360x800 and 320x740 both report `bodyOverflow=0`, `itemCount=7`, `canScroll=true`, `stripDisplay=flex`, and `lastFullyReachable=true`; computed live mobile sizes include header brand `21.6px`, hero lede/button `17.28px`, category title `16.64px`, and category copy `14.08px`. Remote PHP lint for `site-chrome.php` passed and no live `error_log` files were present after checks.
- Fixed the localized Microsoft Store sitewide banner after a Korean-page screenshot showed the banner degrading into plain localized text and an underlined link instead of an official Store badge. Root cause: `includes/components/microsoft-store-badge.php` rendered the banner CSS only from the official badge function, while non-English sitewide banners used the custom text badge path. Refactored the shared badge CSS into `kbtRenderMicrosoftStoreBadgeStyles()`, made both badge renderers and the sitewide banner emit those styles safely once, and changed `kbtRenderMicrosoftStoreSitewideBanner()` to always use the official Microsoft Store SVG badge for the CTA. Deployed `includes/components/microsoft-store-badge.php` via SFTP/paramiko. Verification: XAMPP PHP lint clean, live Korean home/Korean mouse/Japanese home/homepage source all contain `kbt-ms-store-badge sitewide-store-badge`, none contain the old sitewide `kbt-ms-store-text-badge`, both badge SVG assets return HTTP 200 `image/svg+xml`, and no live `error_log` files were present after checks.
- Follow-up localized Store badge audit: checked all 8 localized homepages and localized `mouse-test.php` pages, plus all localized all-tools pages and representative alternate renderer pages (`color-range-test.php`, `dead-pixel-test.php`, and `camera-resolution-test.php`) for Arabic, French, German, Japanese, Korean, Portuguese, Russian, and Spanish. All checked pages returned rendered HTML without PHP warning/fatal markers, contained the official `kbt-ms-store-badge sitewide-store-badge` with the Store URL and banner CSS, and did not contain the old `kbt-ms-store-text-badge sitewide-store-badge`. No live `error_log` files were present after the scan.
- Upgraded the Store badge follow-up from English-only official SVGs to official localized Microsoft Store badge SVGs for all supported non-English locales. Downloaded and deployed dark/light official badge assets for `ar`, `de`, `es`, `fr`, `ja`, `ko`, `pt`, and `ru`, and updated `includes/components/microsoft-store-badge.php` to normalize site locales and choose matching assets by locale while preserving the English `en-us` badge for English pages. Deployed the component plus 18 badge SVGs via SFTP/paramiko. Verification: XAMPP and remote PHP lint clean; live Arabic, French, German, Japanese, Korean, Portuguese, Russian, and Spanish homepages plus localized `mouse-test.php` use the matching localized dark/light badge assets and no English badge in those banners; localized all-tools/color-range/dead-pixel/camera-resolution checks passed for the same languages; every new localized SVG returns HTTP 200 `image/svg+xml`; no live `error_log` files after checks.
- Reviewed the 2026-05-19 DeadLinkChecker full-scan report. It was mixed: real internal issues were the homepage fallback `images/home/headphones-diagnostic-hero-1280.jpg` reference returning HTTP 403 and `ppi-calculator.php` still referencing quarantined `producthunt-assets/05-online-ruler.png` returning HTTP 404; the Reddit rows returned HTTP 200 with a normal browser user agent and are crawler/bot-blocking false positives. Patched homepage JPEG fallback to use only working hero JPGs, changed PPI Open Graph/hero/all-tools image references to `images/online-ruler/hero.jpg`, replaced the dead Razer configuration URL with the official Razer Blade 16 model selector, replaced the ASUS news URL with the official ROG Strix SCAR 18 product page, and changed the DAA privacy opt-out link to the current YourAdChoices control page. Deployed `help/brief-keyboard-tester.php`, `ppi-calculator.php`, `help/brief-ppi-calculator.php`, `includes/render-all-tools.php`, `blog/best-laptops-with-good-gpu-2026.php`, and `privacy-policy.php` via SFTP/paramiko. Verification: touched files pass XAMPP PHP lint, live source no longer contains the old bad URLs, replacement assets/official links return HTTP 200, reported Reddit URLs return HTTP 200 with browser UA, and no live `error_log` files were present after checks.
- Investigated the 2026-05-19 GSC Core Web Vitals export. The provided CSV covered the mobile INP issue only: example URL `https://keyboardtester.click/`, group population `112`, group INP `224ms`, with affected URLs rising from `0` to `103` on 2026-05-10 and `112` by 2026-05-18. Live HTTP checks with normal mobile, Googlebot Smartphone, and Lighthouse user agents returned the real site, not an Imunify/hosting challenge. Root causes found and fixed: homepage LCP still used JPG-only hero delivery, All Tools used `loading="lazy"` on its actual above-fold LCP image, a leftover CSS safety fallback downloaded the old homepage JPG behind the real hero image, and GA/Clarity loaded directly from first touch/click handlers which can worsen field INP. Generated WebP variants for `images/home/headphones-diagnostic-hero-{900,1280,1792}.webp`, updated homepage preload and `<picture>` sources to WebP with JPEG fallback, removed the duplicate CSS background-image request, changed the first All Tools card image to `loading="eager" fetchpriority="high"`, and changed GA/Clarity first-interaction handlers to schedule loading during idle/short delay instead of inside the input event. Deployed `index.php`, `includes/head-common.php`, `help/brief-keyboard-tester.php`, `includes/render-all-tools.php`, `assets/css/index-modern.css`, `assets/css/index-modern.min.css`, and the three new WebP images with SFTP/paramiko. Verification: live homepage and All Tools HTTP 200 with no PHP error markers, WebP images HTTP 200 `image/webp` with immutable cache, no live `error_log` files, in-app browser loaded WebP hero and reported no console warnings/errors. Lighthouse mobile before/after: homepage LCP `2.8s` -> `2.1s`, performance `0.88` -> `0.97`; All Tools LCP `2.7s` -> `2.2s`, performance `0.93` -> `0.97`. Remaining evidence for hosting ticket if needed: repeated curl TTFB to PHP pages is still commonly ~`1.06s`-`1.70s` and one All Tools run spiked to `5.14s` TTFB / `19.45s` total; `.htaccess` also currently marks HTML as private and disables nginx proxy cache, so host-side cache/LiteSpeed/OPcache review may be needed if GSC remains borderline after the 28-day field window.
- Fixed the remaining live cPanel `error_log` cause. SSH showed `/home2/keyboar1/public_html/languages/russian/error_log` contained three PHP warnings from `includes/seo-meta.php`: undefined `$siteName` on lines 43, 47, and 117. Root cause was `seo-meta.php` being included from function/render scope after `config.php` had already been loaded globally, so `include_once` skipped re-defining local `$siteName`. Patched `includes/seo-meta.php` to fall back to `$GLOBALS['siteName']` and `$GLOBALS['siteDescription']`, linted locally, reproduced the scope path locally, deployed only `includes/seo-meta.php` with SFTP/paramiko, archived the live Russian log to `/home2/keyboar1/dirty-data-cpanel/2026-05-19/public-error-logs/languages/russian/error_log.before-seo-meta-fix.20260519-163342`, verified server-side `php -l`, reran the scope reproduction on the server, loaded `https://keyboardtester.click/languages/russian/` with HTTP 200, and confirmed `find /home2/keyboar1/public_html -name error_log` now returns no files.
- Completed the approved cPanel public-root cleanup. Patched the three live `.env` loaders (`api/send-ad-inquiry.php`, `includes/render-all-tools.php`, and `includes/components/adsense-slot.php`) to prefer `/home2/keyboar1/private-config/.env`, deployed them with SFTP/paramiko, moved the live `.env` out of `public_html` to `/home2/keyboar1/private-config/.env` with mode `600`, and chmodded the private config folder to `700`. Quarantined 32 public-root unattended artifacts into `/home2/keyboar1/dirty-data-cpanel/2026-05-19/unattended-public-html/`, including `kbt.zip`, `.git/`, `.github/`, `github-repo/`, `hostarmada.html`, root setup/report docs, backup `.htaccess` copies, test files, unused scripts, `producthunt-assets/`, `maintenance.html`, `latency-checker.html`, and unused `navigation.webp`; wrote a remote `MANIFEST.txt` in the same quarantine folder. Server-side verification confirmed `.env`, `.git`, `kbt.zip`, `github-repo`, `hostarmada.html`, `test_db_connection.php`, `test_curl_check.txt`, `AGENTS.md`, and `navigation.webp` are no longer present under `public_html`, and the private `.env` exists at mode `600`. A fresh 400-byte `/languages/russian/error_log` appeared after checks and was fixed/archived in the follow-up item above.
- Audited live cPanel `/home2/keyboar1/public_html` for unattended files. Inventory found 6,467 live files; local report saved under `dirty-data/cpanel-unused-audit-2026-05-19/`. High-risk/unattended findings: `kbt.zip` exists in public_html at 130,487,224 bytes, `.git/config` exists under public_html, `.env` exists under public_html and is HTTP 403 but currently used by live PHP env loaders, `github-repo/` exists as an unreferenced duplicate repo copy with 115 files and about 928 KB, and root maintenance/test files such as `hostarmada.html`, `update_tool_pages.py`, `generate_tool_images.py`, `test_db_connection.php`, and `test_curl_check.txt` are not referenced by sitemap/static scan. Do not delete `.env` without first moving it outside public_html and patching loaders in `api/send-ad-inquiry.php`, `includes/render-all-tools.php`, and `includes/components/adsense-slot.php`; other listed backup/test artifacts are good quarantine candidates after user approval.
- Checked live cPanel `public_html` for dirty local artifacts. The local `dirty-data/` folder and root SEO audit screenshots/JSON/log artifacts were not present on cPanel, but public `error_log` files were present, including a large root `public_html/error_log`. Root cause was PHP's invalid server timezone setting `Europe/Frankfurt`, which made `date()`/`strtotime()` warnings recreate logs. Added an early `date_default_timezone_set('UTC')` override in `config.php`, deployed only `config.php` with SFTP/paramiko, and moved all 14 live `error_log` files out of `public_html` into `/home2/keyboar1/dirty-data-cpanel/2026-05-19/`. Verification passed: homepage, blog index, updated article, Spanish home, Arabic home, All Tools, and `/tools/keyboard-tester/` returned HTTP 200 with no PHP error markers; follow-up server scan reported no `error_log` files under `/home2/keyboar1/public_html`.
- Cleaned local generated clutter into `dirty-data/local-cleanup-2026-05-19/` and added `dirty-data/` to `.gitignore` so future quarantine folders stay local-only. Moved generated/runtime artifacts only: Python cache, Playwright `test-results`, deploy logs, `blog/error_log`, indexing logs, root SEO screenshot/JSON audit dumps, one-off `deploy-perf-fixes.bat`, root backlink-host export, and DEV.to draft exports 03-06. Deliberately did not move tracked modified source files or remaining untracked runtime/source files such as new PHP includes, CSS/JS, images, `robots.php`, and `sitemap.txt`, because those are part of the local/live site state and moving them would break the project.
- Deployed a scoped SEO/CTR refresh for two high-impression low-CTR clusters from latest GSC/GA review. Homepage now targets the live `keyboard tester`, `keyboard test`, `key test`, `keyboard checker`, and `test keyboard online` query cluster with updated title/meta copy, H1 wording, visible keyword-strip links, visible FAQ answers, and matching FAQ/WebApplication schema while preserving the existing `/` URL, canonical, tool behavior, and successful keyboard-testing intent. `blog/best-mechanical-keyboards-for-gaming-2026.php` now leads with `Best Mechanical Keyboard 2026` while keeping `gaming` in the title/H1/short answer/excerpt so existing `best gaming keyboard 2026` traffic is not dropped; custom visible FAQ and matching FAQPage schema replaced the generic template FAQ schema for this article. Deployed only `meta-config.php`, `help/brief-keyboard-tester.php`, `index.php`, `includes/schema.php`, `blog/best-mechanical-keyboards-for-gaming-2026.php`, `blog/posts-data.php`, and `sitemap.xml` via SFTP/paramiko temp-file uploads plus atomic renames. Live verification passed: homepage, blog index, and article return HTTP 200; homepage and article each have one title and one H1; article has one FAQPage plus BlogPosting and VideoObject; blog index shows the updated article card; no PHP error markers; `sitemap.xml` lastmod is `2026-05-19` for `/`, `/blog/`, and the article; IndexNow submission returned HTTP 200 and included the article/home/blog URLs.
- Fixed local development GA4 pollution. `includes/head-common.php` now loads the Google Analytics/GTM DNS prefetch and GA4 script only when `HTTP_HOST` is `keyboardtester.click` or `www.keyboardtester.click`; localhost, `127.0.0.1`, CLI, and other non-production hosts no longer emit `G-G5EY8TGGE6`. Verification passed: XAMPP PHP lint clean for `includes/head-common.php`; simulated localhost render has no GA ID/GTM marker; simulated production render keeps both; real `http://localhost/kbt/` returns HTTP 200 with no GA/GTM markers; live `https://keyboardtester.click/` still returns HTTP 200 with `G-G5EY8TGGE6`, GTM loader marker, and no old `G-DPRQF0RTQ7`.
- Checked the outreach mailbox for the Man & Machine/washable-keyboard sponsorship email. The message was sent 2026-05-17 21:21 PKT from `outreach@keyboardtester.click` to `sales@mmimd.com` with `info@mmimd.com` CC, but no human reply was received. `INBOX` contains a permanent delivery failure from the mail system dated 2026-05-17 19:22 +0300: both `info@mmimd.com` and `sales@mmimd.com` were rejected after DATA by `smtp.antispamcloud.com` with `550 High probability of spam`. Updated `seo-audit-2026-05-02/outreach/advertiser-outreach-log-2026-05-17.csv` from `pending` to `bounced_spam_filter`.
- Deployed the hearing-age blog article live at `blog/why-cant-i-hear-17khz-hearing-age-test.php`. Uploaded the ignored article file, five WebP article images, `blog/posts-data.php`, regenerated `sitemap.xml`, and regenerated `sitemap.txt` via SFTP/paramiko using temp-file uploads plus atomic renames; no FTPS was used. Live verification passed: article HTTP 200, one H1, one title, one meta description, one canonical, JSON-LD contains `BlogPosting`, `VideoObject`, and `FAQPage`, no PHP error markers, no private email exposure, all five WebP images return HTTP 200 with `image/webp`, `/blog/` shows the new post card, both `sitemap.xml` and `sitemap.txt` include the new URL, and IndexNow submission returned HTTP 200 with the new URL included in the submitted list.

### 2026-05-18 (Codex)
- Drafted a local-only hearing-age blog article at `blog/why-cant-i-hear-17khz-hearing-age-test.php` targeting `why can't I hear 17 kHz`, `can only hear up to 17 kHz`, `hearing age test not accurate`, `17.4 kHz mosquito tone test`, and related long-tail keywords. Added Pexels-derived WebP stock images plus a local frequency checkpoint chart, updated `blog/posts-data.php` so the draft appears on the local blog index, and kept the standard KBT article pattern: fast answer, jump links, result table, practical setup checklist, related tools, authoritative sources, verified YouTube embed, `BlogPosting`, `VideoObject`, `FAQPage`, and `BreadcrumbList` JSON-LD. No live deploy. Verification passed locally: PHP lint clean for the article and `blog/posts-data.php`, local HTTP 200, one H1/title/meta description/canonical, JSON-LD graph includes the expected schema types, local blog index shows the new card, and in-app browser check found no PHP errors or horizontal overflow.
- Updated and deployed the live `hearing-age-test.php` tool after checking current online high-frequency/mosquito-tone test patterns. The tool now uses a simple 12-step 8-22 kHz quick test, reports an estimated hearing-age range from the highest clearly heard frequency, includes the 17.4 kHz mosquito checkpoint, and adds optional advanced controls for left/right/both ear testing, pulsed/steady/short-sweep tones, tone length, browser sample-rate/upper-limit display, safety-capped volume, and a manual threshold finder. Also aligned the page title/description, visible guide copy, SEO support content, WebApplication feature schema, and FAQPage schema with the new behavior. Deployed only `hearing-age-test.php`, `tools/hearing_age_tool.php`, `help/seo-content/hearing-age-test.php`, `help/hearing-age-test.php`, and `includes/schema.php` via SFTP/paramiko temp-file upload plus `posix_rename()`. Verification passed locally and live: PHP lint clean for touched PHP files, live HTTP 200, exactly one title/meta description/canonical/robots/H1, 4 JSON-LD blocks including `WebApplication` and `FAQPage`, no PHP error markers, inline scripts parse, and live browser click starts the test with answer buttons enabled and no hearing-page console errors.
- Researched a new low-competition blog topic and saved the brief at `seo-audit-2026-05-18/low-competition-keyword-research-pwm-eye-strain-2026-05-18.md`. Recommended topic: `Monitor Eye Strain at Low Brightness? Test PWM Flicker Before You Blame Blue Light`, targeting `monitor eye strain low brightness PWM` / `low brightness eye strain PWM` / `monitor flicker test phone camera`. Evidence: local blog scan found no existing PWM blog article, only PWM tool/config coverage; GSC last-90-day query data already shows `pwm test` at 87 impressions / 12 clicks / avg position 8.5, `pwm flicker test` at 30 impressions / avg position 7.5, `flicker test monitor` at 29 impressions / avg position 12.0, and `monitor flicker test` at 22 impressions / avg position 18.2; SERP/forum research shows real user demand around low brightness, eye strain/headaches, phone-camera black bands, OLED/Mini LED, and DC dimming, with competition fragmented across technical explainers and forum threads.
- Cleaned up a live SEO duplicate-title issue on `pages/all-tools.php`. The page rendered two identical `<title>` tags because `includes/render-all-tools.php` manually printed a title before including `includes/seo-meta.php`, which already outputs the canonical title/meta block. Removed the manual title line and deployed only `includes/render-all-tools.php` with SFTP/paramiko atomic temp-file + `posix_rename()`. Verification passed locally and live: All Tools now has exactly one title, one meta description, one robots meta, one canonical, one H1, one JSON-LD block, no `noindex`, and no PHP error markers. Guardrail checks for live homepage and `pwm-flicker-test.php` also still show single title/description/robots/canonical/H1 and expected JSON-LD counts.
- Hotfixed the duplicate floating AI assistant on modern inline English tool pages, verified on live `pwm-flicker-test.php`. Root cause: `includes/render-english-inline-tool-page.php` captured shared footer/chat/ad/header scripts from the legacy full-page render, then the real modern wrapper/footer loaded the shared chrome again, creating two AI assistants. Updated the inline renderer to skip shared chrome/footer/chat/ad scripts when extracting legacy tool scripts, then deployed only `includes/render-english-inline-tool-page.php` with SFTP/paramiko atomic temp-file + `posix_rename()`. Verification passed locally and live: PHP lint clean, live source has one `KBT_CHAT_LOCALE` config and one `ai-chat` loader, rendered browser state after the lazy-load delay has exactly one assistant button/window/avatar/CSS/script, one `#pwm-fullscreen-canvas`, one H1, no relevant console errors, and the PWM page still exposes `FAQPage` and `WebApplication` schema.
- Hotfixed `pwm-flicker-test.php` live after the production tool failed to run. Root cause: the rendered inline JS had invalid ternary expressions (`isBlack '#000' : '#fff'`) and the modern inline wrapper did not capture the fullscreen canvas because it was outside the legacy tool shell. Fixed the JS ternaries and moved the `#pwm-fullscreen-canvas` into the captured tool markup, then deployed only `pwm-flicker-test.php` with SFTP/paramiko atomic temp-file + `posix_rename()`. Verification passed locally and live: PHP lint clean, rendered source has exactly one fullscreen canvas and the start button, all inline non-JSON scripts pass `node --check`, browser click changes the Start button to running/Stop state and displays the canvas, no current page console errors, no PHP error markers. Live SEO guardrail for this page still has 1 title, 1 canonical, 1 robots meta, 1 H1, and 3 JSON-LD blocks including `FAQPage` and `WebApplication`.
- Deployed the locale-sticky header link fix live with SFTP/paramiko atomic temp-file + `posix_rename()` uploads. Files deployed: `header.php`, `includes/components/site-chrome.php`, and `includes/components/blog-language-notice.php`. Live verification passed for all 8 non-English locales on both `/blog/?lang={code}` and `languages/{directory}/`: logo/home link points to the matching language home, Blog link stays on `/blog/?lang={code}`, Tools link points to the matching localized all-tools page, header language label is expected, and no PHP error markers were found. English guardrail check passed on `/blog/` and `/`: logo remains `/`, Blog remains `/blog/`, and Tools remains `/pages/all-tools.php`.
- Fixed locale-sticky header links locally. `header.php` now detects the selected blog language from `?lang=`, referrer, or `kbt_blog_lang` cookie before rendering shared chrome on blog pages. `includes/components/site-chrome.php` now resolves the logo/home link and Blog link by current locale, so localized pages and `/blog/?lang={locale}` point the logo to the matching `languages/{directory}/` home page and Blog to `/blog/?lang={locale}` instead of English. `includes/components/blog-language-notice.php` now updates header/footer home, blog, and tools links immediately when the blog language selector changes without a page reload. No live deploy. Verification passed locally: PHP lint clean for `header.php`, `includes/components/site-chrome.php`, and `includes/components/blog-language-notice.php`; rendered checks for all 8 locales on both `/blog/?lang={code}` and `languages/{directory}/` showed matching localized logo home links, localized Blog links, localized All Tools links, expected header language label, exactly one H1, and no PHP error markers.
- Deployed the local shared/localized rollout to production with SFTP/paramiko using temp-file uploads plus atomic `posix_rename()` swaps; no FTPS was used. Scope: shared PHP components/includes, shared CSS/JS/assets, AI assistant/chat runtime, advertisement inquiry endpoint, shared keyboard runtime, localized renderers/components, and all eight localized language folders (`spanish`, `french`, `german`, `japanese`, `korean`, `portuguese`, `russian`, `arabic`). English root tool pages were not bulk-replaced beyond the shared files required by the localized/blog updates. Live verification passed: 24 localized URLs (home, `color-range-test.php`, and `mouse-test.php` across all 8 locales) returned HTTP 200 with the modern localized shell, no PHP error markers, and one H1; all eight localized home pages expose the expected `KBT_CHAT_LOCALE`, shared keyboard runtime, and no old localized keyboard/Pac-Man/cat visual; `/blog/?lang=es` renders the Spanish blog-language notice with no sidebar markup; `assets/js/ai-chat.min.js`, `assets/css/ai-chat.css`, and `assets/js/keyboard-tester-english.min.js` return HTTP 200 live.
- Added a shared localized blog-language notice, local-only. New component `includes/components/blog-language-notice.php` is rendered from `header.php` only on `/blog/` pages, so it covers the blog home and all blog posts without touching individual posts. It detects `?lang=`, same-site referrers from `/languages/{locale}/`, and the `kbt_blog_lang` cookie; English keeps the notice hidden, while `ar`, `fr`, `de`, `es`, `pt`, `ru`, `ja`, and `ko` show localized copy explaining that native-language blog guides are in progress and browser translation can be used meanwhile. The blog-page language dropdown is intercepted client-side so switching language stays on the current blog URL, updates the notice/header language label, and persists the blog language. No live deploy. Verification passed locally: PHP lint clean with XAMPP PHP for `header.php` and the new component; source checks show Arabic/Spanish notices visible with `?lang=ar`/`?lang=es` and hidden on plain `/blog/`; in-app browser check on `/blog/?lang=ar` then responsive-menu language switch to Spanish kept the page on `/blog/?lang=es`, updated header label to `ES`, and displayed Spanish notice text.
- Localized the shared floating AI assistant persona system, local-only. `footer.php` now passes the current `$siteChromeLocale` into the widget plus the existing greeter media assets; `assets/js/ai-chat.js` / `.min.js` now default the chat language/persona from the page locale, add Russian support, and define 9 distinct personas: Emily/en, Ava/es, Camille/fr, Lena/de, Yui/ja, Minji/ko, Clara/pt, Layla/ar, and Anya/ru. English keeps the existing Emily MP4, Spanish uses the existing Ava GIF as greeter media, and the other locales use lightweight local animated portrait greeters with a waving-hand loop. `assets/css/ai-chat.css` adds the animated persona/wave styling and RTL greeter positioning. `ai-chat.php` now accepts `ru`, localizes Russian tool URLs, and updates the prompt language count to 9. Updated AI assistant page/schema copy from 8 to 9 languages. No live deploy. Verification passed locally: JS syntax checks for `ai-chat.js` and `.min.js`; PHP lint clean for `ai-chat.php`, `footer.php`, `includes/head-common.php`, `includes/schema.php`, `ai-assistant.php`, and all 8 localized AI assistant pages; rendered Arabic/Russian pages expose `KBT_CHAT_LOCALE`, `ai-chat.css?v=3.0`, and `ai-chat.min.js?v=2.8`; browser check on `languages/arabic/` opened Layla with RTL Arabic greeter text/waving local portrait, then switching to Russian changed the widget to Anya with Russian header/status/placeholder/greeting and LTR direction.
- Localized the Microsoft Store promo banner on localized modern pages. Added locale-aware defaults in `includes/components/microsoft-store-badge.php` for `ar`, `fr`, `de`, `pt`, `ru`, `ja`, `ko`, and `es`, including banner aria/label/button text, and changed `includes/render-localized-modern-tool-page.php` so it no longer forces English fallback text when a localized page does not pass custom store copy. Non-English sitewide banners now render a localized text CTA instead of the English-only Microsoft Store SVG badge; English pages still keep the official badge image. No live deploy. Verification passed locally: PHP lint clean for the component and localized renderer; rendered-source checks across all 8 localized keyboard home pages show localized Store text badges with no English fallback strings; in-app browser reload on `languages/arabic/` shows Arabic banner text, Arabic aria label, Arabic Store CTA text, and zero English badge images in the banner.
- Fixed the local-only Arabic shared keyboard page so RTL page direction no longer mirrors the physical keyboard. Added `direction:ltr` to the shared keyboard tester/chassis/layout/row CSS in `tools/keyboard_tester_english.php`, keeping Arabic as label-only replacement while preserving the same physical key order as English. No live deploy. Verification passed locally: PHP lint clean for the shared keyboard include; Arabic desktop render keeps document `dir=rtl` but reports `.keyboard-tester` and `.keyboard-layout` as `ltr`, physical order `Esc < F1 < F12 < NumpadDivide`, no document overflow, and Arabic labels still render (`KeyQ=ض`, `KeyW=ص`, `KeyP=ح`).
- Hotfixed the local-only localized keyboard pages after the shared English keyboard rollout. Root cause of the broken language selector and doubled AI assistant was the localized modern legacy bridge carrying old shared header/footer scripts into the modern renderer, so the header click handler and AI chat loader both ran twice. Updated `includes/render-localized-modern-legacy-page.php` to skip shared chrome/footer/theme/ad/chat scripts while preserving tool scripts. Also widened the localized modern tool shell to the 1480px modern width and constrained the shared keyboard chassis to `width:100%; max-width:1640px; box-sizing:border-box` so Korean/Japanese keyboard layouts scale inside the page instead of clipping. No live deploy. Verification passed locally: PHP lint clean for the renderer/bridge/keyboard include; in-app browser check on `languages/korean/` showed one H1, one header script, one keyboard script, one AI chat instance after load, and no horizontal overflow; headless desktop checks for Korean and Japanese showed the language selector opens with 9 visible options, one keyboard JS include, one header script, no document overflow, and no left/right keyboard clipping at desktop width.
- Replaced the old per-language keyboard tool implementations with the shared English keyboard tester component on all 8 localized keyboard index pages, local-only. Added `includes/localized-keyboard-layouts.php` for locale-specific key-label maps and updated `tools/keyboard_tester_english.php` / `assets/js/keyboard-tester-english.js` so the shared keyboard reads CSP-safe JSON layout config, uses separate locale layout storage keys, supports custom layout maps, updates mobile visible labels, and has a timer fallback for background browser tabs. The localized `sections/tool.php` files are now thin wrappers that suppress the old cat/progress visual and pass the proper layout to the shared keyboard. Also hardened `includes/render-localized-modern-legacy-page.php` so scripts already inside extracted tool HTML are not duplicated. No live deploy. Verification passed: XAMPP PHP lint clean for the new helper, shared keyboard include, bridge, and all 8 localized tool wrappers; Node syntax checks passed for `keyboard-tester-english.js` and `.min.js`; browser checks passed for Korean, Arabic, Russian, Japanese, French, German, Portuguese, and Spanish keyboard pages with one `.keyboard-tester`, one keyboard JS include, selected locale layout, native key labels rendered (`ㅂ`, `ض`, `Й`, `た`, `A`, `Z`, `Ç`, `Ñ`), no `localized-keyboard-history`, no `localized-keyboard-visual`, no `cat-progress-section`, no PHP error text, and zero horizontal overflow. Korean layout switching also verified QWERTY -> Korean without leaving the page.
- Extended the local-only localized modern tool-page rollout from the approved Color Range pilot to the active tool wrappers and the 8 language keyboard index pages in all language folders. Added `includes/render-localized-modern-legacy-page.php` as a bridge for legacy localized pages: each page now guards the old body during capture, extracts the existing localized tool block/scripts/styles, strips old page furniture and duplicate H1s, skips old tools-list CSS/scripts, and renders through `includes/render-localized-modern-tool-page.php`. The bridge also sanitizes mojibake inherited from legacy titles/keywords/stage labels for Arabic, Russian, Japanese, and Korean metadata while preserving real Portuguese/Spanish accents. Bulk-updated 687 additional localized wrappers plus the 8 language `index.php` keyboard pages; Color Range remains on its dedicated localized data helper. No live deploy. Verification passed locally: PHP lint clean for 708 localized renderer/wrapper/index files; `git diff --check` clean for the 708 rollout files; full audit saved at `seo-audit-2026-05-18-localized-modern-audit.json` checked 704 localized URLs, with 696 active URLs passing HTTP 200, exactly one H1, modern `home-redesign` shell, Google ad markup, localized first-party ad fallback copy, no old `tools-list-section--unified`, no PHP error markers, no title/H1 mojibake, and a populated tool shell. The 8 `dead-pixel-test.php` localized URLs remain retired redirects and were excluded from active pass/fail. Browser spot checks passed for Spanish and Arabic language keyboard index pages, Spanish Color Range, German APM, Japanese Keyboard Sound, and Korean Mouse: correct `lang`/`dir`, localized category band and ad fallback text, 4 ad slots/Google ad markers, no old tool list, and no horizontal overflow. Production was intentionally not touched.
- Extended the local-only localized modern Color Range Test rollout to all 8 language folders (`arabic`, `french`, `german`, `japanese`, `korean`, `portuguese`, `russian`, `spanish`). Each `languages/*/color-range-test.php` is now a small wrapper around `includes/localized-modern-color-range-page.php`, so the tool uses the same modern renderer and the same hero image `images/screen-test/screen-tester-monitor-inspection-1400.webp` in every locale. Updated `includes/render-localized-modern-tool-page.php` with localized category-band labels and passed locale context into ad slots. Updated `includes/components/adsense-slot.php` so the shared Google-first / own-ad fallback rule renders localized fallback ad copy, package labels, form labels, CAPTCHA/status text, and modal copy for `es`, `fr`, `de`, `pt`, `ar`, `ru`, `ja`, and `ko` while preserving English defaults. No live deploy. Verification passed locally: PHP lint clean for changed ad/renderer/helper files and all localized wrappers; all 8 localized Color Range URLs returned HTTP 200, exactly one H1, the shared hero image, Google ad slot markup, locale-specific house-ad text, no English ad copy, no old `tools-list-section--unified`, and no PHP error markers. Browser check on `https://localhost/kbt/languages/spanish/color-range-test.php` confirmed `lang=es`, shared hero image, 4 ad slots, localized Spanish ad copy, no English ad copy, popular-tools section present, old tool list absent, and zero horizontal overflow.
- Built a local-only localized modern tool-page pilot for `languages/spanish/color-range-test.php`. Added `includes/render-localized-modern-tool-page.php` as a separate renderer instead of reusing the English-only renderer, preserving localized header/footer, local tool partials, shared ad-slot fallback behavior, canonical/hreflang output, and JSON-LD FAQ/WebApplication/Breadcrumb schema. Follow-up correction: replaced the old localized full tools grid with the same modern English `tool-popular-tools` four-card section, using localized Spanish labels/copy. No live deploy. Verification passed locally on `https://localhost/kbt/languages/spanish/color-range-test.php`: PHP lint clean for both changed files, HTTP 200, one H1, 10 hreflang links, one JSON-LD block, modern body class/category band/tool partial/popular tools/ad slots present, old `tools-list-section--unified` absent, no PHP error markers, browser metric check showed zero horizontal overflow and visible tool, and desktop/mobile screenshots were reviewed after fixing Spanish hero wrapping.
- Removed the shared floating tool sidebar from blog pages only. Updated `header.php` so `kbtRenderSiteSidebar()` is skipped when the current script path is under `/blog/`, while keeping the shared header and leaving normal tool pages unchanged. Deployed `header.php` via SFTP/paramiko. Live verification passed: `https://keyboardtester.click/blog/` and `https://keyboardtester.click/blog/best-laptops-with-good-gpu-2026.php` return HTTP 200 with one H1, shared header present, no `kbt-sb-fab` and no `kbt-sb-panel`; `https://keyboardtester.click/mouse-test.php` still renders the sidebar; no PHP error markup and no private email exposure on checked pages.
- Hotfixed the ad-block fallback duplication introduced in the previous shared fallback pass. Root cause: the per-slot script was selecting the new hidden partner fallback sibling as the ad slot, and the global blocker handler was displaying both the in-slot house creative and the outside partner fallback. Updated `includes/components/adsense-slot.php` so each slot script skips the partner fallback sibling and targets the real `.kbt-ad-slot`; updated `includes/head-common.php` so visible ad slots show only the existing in-slot house creative, while outside partner fallback panels are used only when the slot/wrapper itself is hidden by blocker CSS. Deployed both files via SFTP/paramiko. Live browser verification passed: `/blog/` with a visible blocked slot showed `houseVisible=2` and `partnerVisible=0`; `/blog/` with `.kbt-ad-slot` hidden showed one partner fallback for `blog_hero_after_metrics` and one for `site_footer_panel`; `blog/best-laptops-with-good-gpu-2026.php` with visible blocked slots showed `houseVisible=3` and `partnerVisible=0`; all checks had zero horizontal overflow, no PHP error markers, and no private email exposure.
- Hardened the sitewide Google-ad/own-ad rule for blocker cases and redesigned the blog "You might also like" component. `includes/components/adsense-slot.php` now renders a non-ad-named partner fallback beside each shared ad slot, and `includes/head-common.php` can mount that fallback outside hidden ad wrappers such as the footer ad section when an ad blocker hides the original container. Updated `assets/css/global.css`/`.min.css` and `sw.js` cache versions for the fallback styling. Rebuilt `includes/components/related-blog-posts.php` into the new clean rectangular card pattern with `Related guides`, `Read guide` CTAs, 16:9 images, dark-theme support, and no old arrow/mojibake text. Deployed all six files via SFTP/paramiko. Verification passed on the exact live blog article: HTTP/CSS/SW 200, exactly one H1/canonical, related section has 3 new-style cards, forced ad-blocker simulation mounted the footer fallback outside the hidden wrapper, popup opened in place with placement `site_footer_panel`, URL stayed unchanged, zero horizontal overflow, no PHP error markers, and no private recipient email in source.
- Fixed advertising form delivery and the inconsistent blog post header. Updated `api/send-ad-inquiry.php` so the ad inquiry endpoint sends through authenticated site SMTP when private `OUTREACH_SMTP_*` settings are available, instead of relying on raw PHP `mail()` for production delivery. Added the SMTP settings to the live private `.env` over SFTP/paramiko without exposing values in public files. Deployed `api/send-ad-inquiry.php` and `includes/components/blog-article-ads.php`. Verification passed: local endpoint submission returned JSON success through SMTP; live blog article form and live All Tools form both returned JSON success after signed CAPTCHA validation; live `/api/send-ad-inquiry.php` rejects GET with HTTP 405; live `.env` contains the required SMTP/ad keys; rendered blog article and All Tools sources do not expose the private recipient address.
- Enforced the Google-first/own-ad fallback rule on blog article rail ads and aligned blog post header styling with the blog homepage. `includes/components/blog-article-ads.php` now calls the shared `kbtRenderAdSlot()` component for `blog_article_left_rail` and `blog_article_right_rail`, preserving sticky/wide rails while adding first-party fallback creative and popup behavior when Google ads are blocked or unfilled. Added shared article-header CSS variables/styles so blog posts use the same light/dark header treatment as `/blog/`. Live verification on `blog/best-laptops-with-good-gpu-2026.php`: HTTP 200, exactly one H1 and one canonical, article rail placements and modal present, old `data-blog-rail-ad` hook absent, white header background with `#1a1a1a` brand/nav text and blue tools CTA, right rail sticky at `top: 88px` after scroll, rail width `240px` at 1890px viewport, rail shell border/background removed, zero horizontal overflow, and no private recipient address in source.

### 2026-05-17 (Codex)
- Removed the visible wrapper boundaries around Google ad placements and improved blog article rail behavior after user feedback. Updated `includes/components/site-chrome.php` so the shared footer AdSense section has no outer card border/background/padding and no inner slot border/background, leaving only the Google creative or the first-party fallback creative visible. Updated `includes/components/blog-article-ads.php` so article side rails use a stretched rail with a sticky inner block, no rail frame/background, and wider desktop rail widths (`180px`, `220px`, `240px` on wide screens). Deployed both shared component files via SFTP/paramiko. Live verification on `blog/best-laptops-with-good-gpu-2026.php`: HTTP 200, 1 H1, 1 canonical, right rail sticky at `top: 88px` after scroll, rail/ad width `240px` at 1890px viewport, rail shell border/background removed, footer ad section and inner slot border/background removed, zero horizontal overflow, no PHP error markers, and no private recipient email exposure.
- Added and deployed a Google-first sponsored slot in the blog homepage hero gap below the metrics. Added `blog_hero_after_metrics` to `includes/adsense-slots.php` and rendered it from `blog/index.php` with the shared AdSense component, so normal browsers get Google AdSense and blocked/unfilled states show the existing first-party "Advertise with us" fallback/modal. Deployed only `blog/index.php` and `includes/adsense-slots.php` via SFTP/paramiko. Verification passed locally and live: `https://keyboardtester.click/blog/` returns HTTP 200 with exactly 1 H1 and 1 canonical, the slot is inside the hero copy below the metrics and before Reader favorites, AdSense slot `6240375832` is present, fallback click opens the modal in place with placement `blog_hero_after_metrics`, URL stays `/blog/`, zero horizontal overflow, no PHP error markers, and no private recipient email exposure.
- Fixed the blog homepage header color mismatch after the redesign. `blog/index.php` now sets the same light/dark shared header variables and scoped header control styles used by the redesigned homepage/all-tools pages, so the blog no longer falls back to the older dark header in light mode. Deployed only `blog/index.php` via SFTP/paramiko. Live verification compared homepage, all-tools, and blog computed header colors: all three now use white header background, `#1a1a1a` header text/brand/nav, blue `.click` accent, zero horizontal overflow, and no PHP error markers.
- Cleaned up and deployed the live blog homepage at `blog/index.php` to match the newer site style. The archive now uses a flatter Microsoft-style hero, rectangular guide cards, simplified blog copy, `Read guide` CTAs, three featured article cards instead of a blank pinned ad cell, cleaner desktop/mobile spacing, dark-theme-aware colors, and a mobile-specific hide for the floating sidebar FAB on the blog page to avoid content overlap. Deployed only `blog/index.php` via SFTP/paramiko. Verification passed locally and live: `https://keyboardtester.click/blog/` returns HTTP 200, exactly one H1, exactly one canonical, no PHP error markers, no horizontal overflow on desktop/mobile, three featured cards and zero top-grid ad boxes, loaded hero image asset, and no private email exposure.
- Fixed the mobile menu contrast issue in the shared header component. The `90+ Tools` mobile nav item could inherit higher-specificity page header text colors on redesigned pages, leaving dark text on the blue CTA in light-theme mobile menus. Updated `includes/components/site-chrome.php` with higher-specificity mobile selectors so the mobile CTA keeps white text on Microsoft blue in both light and dark themes. Deployed the shared header file via SFTP/paramiko. Verification: local mobile checks at 390px on homepage, all-tools, and mouse-test showed `90+ Tools` at 5.78:1 contrast and normal menu controls at 17.4:1; live rendered source for homepage, all-tools, and mouse-test contains the new selector and no PHP error markers.
- Checked current GitHub awesome/backlink PR status for KeyboardTester.click. GitHub search found 26 PRs authored by `nasirazizawan009` mentioning KeyboardTester.click: 19 open, 2 merged, and 5 closed-not-merged. Main tracked PR `mathewlewallen/awesome-free-tools#62` remains open, non-draft, mergeable, requested reviewer `mathewlewallen`, no new maintainer comments after the user's 2026-05-09 follow-up, and changes only `_partials/readme-data.md` (+3). Recent/important open PRs `Bakumon/awesome-online-tools#29`, `Siddharth11/Colorful#26`, `esteves-esta/awesome-touch-typing#3`, `goabstract/Awesome-Design-Tools#520`, `EanNewton/Awesome-Keebs#3`, `Delapouite/awesome-keyboard#8`, and `devtoolsd/awesome-devtools#206` are all still open and clean/mergeable. Merged PRs remain `StanForever/awesome-websites#49` and `nafasebra/awesome-webdesign-tools#32`.
- Sent one targeted advertiser sponsorship email to Man & Machine from `outreach@keyboardtester.click` via SMTP, addressed to `sales@mmimd.com` with `info@mmimd.com` CC. Offer pitched a sponsored blog post plus one sponsor ad on the Keyboard Tester page and one on the Mouse Tester page, with 1-month `$249`, 3-month `$549`, and 1-year `$1,699` packages. SMTP accepted all recipients and a sent copy was appended to `INBOX.Sent`; log saved at `seo-audit-2026-05-02/outreach/advertiser-outreach-log-2026-05-17.csv`. Do not resend this prospect unless the user approves a follow-up.
- Finished the live English tool-template rollout after the user reported many tool pages still using the old style. Added `luckywheeltoolindex.php` to the shared English tool catalog, wrapped Lucky Wheel with the shared inline English renderer, and deployed the English catalog routes plus supporting renderers/components/assets via SFTP/paramiko. Live verification across 92 English catalog routes passed: every route returned HTTP 200, used the new `home-redesign` / all-tools shell, had exactly 1 H1 and 1 canonical, no PHP error markup, and a reachable page image. Browser spot checks passed for `latency-checker.php`, `luckywheeltoolindex.php`, `ocr-tool.php`, `touch-screen-test.php`, and `pages/all-tools.php`; all-tools now includes the Lucky Wheel card and still does not expose the private ad email.
- Corrected all-tools filler ad state switching after user reported direct sponsor cards still appeared with ad blocker turned off. Updated `includes/render-all-tools.php` so visible filler cells try Google AdSense first and do not get the `at-card--house-ad` fallback class while Google is pending/available. Removed the localhost shortcut that forced house ads, and fixed blocked-state refresh so if ad blocking is detected after a slot was initialized it still converts to direct sponsor fallback. Deployed `includes/render-all-tools.php` via SFTP/paramiko. Live verification on `pages/all-tools.php`: normal state had `house=0`, `loaded=14`, `emailExposed=false`; forced blocked state had `house=14`, `loaded=0`, `emailExposed=false`. Screenshots saved as `seo-audit-2026-05-17-adblock-check/all-tools-google-first-live-v2.png` and `seo-audit-2026-05-17-adblock-check/all-tools-google-first-forced-blocked-live-v2.png`.
- Updated the all-tools page ad behavior after user clarified no persistent vertical/horizontal ads and no blank ad boxes should appear there. Removed visible top/bottom direct-sponsor bands from `includes/render-all-tools.php`, suppressed the shared footer ad on all-tools via `$GLOBALS['kbtSuppressFooterAd']`, changed grid ads so they render only as end-of-row filler cards equal to the current grid gaps, and exposed `window.kbtRefreshAllToolsAds` so the shared ad-block detector does not force every hidden all-tools ad card visible. Also exposed `window.kbtLoadAds` in `includes/head-common.php` and kept direct sponsor fallback visible until a real AdSense iframe exists, preventing blank Google filler boxes. Deployed `includes/render-all-tools.php` and `includes/head-common.php` via SFTP/paramiko. Live verification passed on `pages/all-tools.php`: desktop had `sponsorVisible=0`, `footerAdVisible=0`, `kbtAdSlotsVisible=0`, every category had `visibleAds === needed`, ads at row end, `blankVisible=0`, and no private email exposed; mobile had zero visible ad cards/sections. Screenshot saved as `seo-audit-2026-05-17-adblock-check/all-tools-fillers-only-live-final-v2.png`.
- Fixed ad fallback click behavior so first-party ads no longer navigate users to `pages/all-tools.php`; each fallback ad now opens the advertiser inquiry popup in-place on the current page. Moved the modal/captcha flow into shared `includes/components/adsense-slot.php`, added global modal styling in `assets/css/global.css`, updated generic ad copy around direct ads staying visible when standard ad networks are blocked, and improved `api/send-ad-inquiry.php` to preserve the actual clicked placement label in inquiry emails. Also fixed all-tools ad-block behavior so fallback ad cards stay in their original PHP-rendered positions instead of being rearranged after fallback activation. Deployed `includes/components/adsense-slot.php`, `includes/render-all-tools.php`, `api/send-ad-inquiry.php`, `assets/css/global.css`, `assets/css/global.min.css`, `includes/head-common.php`, and `sw.js` via SFTP/paramiko; cache bumped to `global.min.css?v=3.6` and `sw.js?v=20260517g`. Live verification passed: homepage fallback click kept URL unchanged and opened `Advertise with us`, private email absent, all-tools ad cards all visible/house-styled with `moved_count=0`; screenshots saved as `seo-audit-2026-05-17-adblock-check/modal-open-live.png` and `seo-audit-2026-05-17-adblock-check/all-tools-adblock-stable-live.png`.
- Reworked the narrow vertical rail house ad after user feedback that the previous vertical banner was still poor. Shortened rail-specific copy to `Reach tool users`, reduced body/CTA text, redesigned only the rail CSS for the 180x600 desktop gutter, regenerated `assets/css/global.min.css`, bumped live cache to `global.min.css?v=3.5` and `sw.js?v=20260517f`, and deployed `includes/components/adsense-slot.php`, `assets/css/global.css`, `assets/css/global.min.css`, `includes/head-common.php`, and `sw.js` via SFTP/paramiko. Live verification passed on `https://keyboardtester.click/`: HTTP 200, new CSS/SW versions present, new rail copy present, no private email in source, and Playwright screenshot/overflow check saved as `seo-audit-2026-05-17-adblock-check/rail-redesign-live.png` with zero clipped rail elements.
- Polished the first-party ad-block fallback creatives after user rejected the earlier visual design. Updated `includes/components/adsense-slot.php`, `assets/css/global.css`, `assets/css/global.min.css`, `includes/head-common.php`, and `sw.js`; deployed live via SFTP/paramiko. Cache bumped to `global.min.css?v=3.4` and `sw.js?v=20260517e`. Verified live `mouse-test.php` and `pages/all-tools.php` source, CSS, and service worker, with ad-block screenshots saved as `seo-audit-2026-05-17-adblock-check/mouse-adblocked-final.png` and `seo-audit-2026-05-17-adblock-check/all-tools-adblocked-final.png`.
- Emergency live repair completed after the `mouse-test.php` redesigned hero rendered as a giant image-only section in production. Deployed a scoped `assets/css/index-modern.css` / `.min.css` repair for `.mouse-hero` and `.tool-template-hero`, plus cache-busted route/render files and `sw.js`; verified live `mouse-test.php`, `mouse_sensitivity_DPI_tester.php`, `keyboard-polling-rate-test.php`, homepage, and all-tools return HTTP 200 with one H1 and no PHP error markers. Follow-up ad-block behavior changed from hiding empty ad boxes to first-party fallback creatives: deployed `includes/components/adsense-slot.php`, `includes/head-common.php`, `assets/css/global.css`, `assets/css/global.min.css`, `includes/render-all-tools.php`, `pages/all-tools.php`, and `sw.js`. Normal browsers still receive Google `adsbygoogle` slots; blocked/unfilled slots now render animated "Advertise with us" house ads linked to the all-tools inquiry form. Verified live source has Google slots, house-ad markup, `global.min.css?v=3.2`, `sw.js?v=20260517c`, packages/prices, modal form, and no exposed private recipient email; Chrome screenshots saved under `seo-audit-2026-05-17-adblock-check/`.
- Deployed the shared `includes/components/site-chrome.php` mobile header update so the responsive menu source includes a mobile-only `90+ Tools` link while keeping the desktop CTA. Deployed `ai-chat.php` so the site AI prompt knows the $59 / $149 / $229 advertising packages and shortens visible links to clickable tool names/keywords instead of raw full URLs. Live verification passed for `mouse-test.php` and `pages/all-tools.php`: HTTP 200, one H1, mobile tools link present, advertising modal/packages present, and no private recipient email in rendered source; `ai-chat.php` returns the expected GET rejection rather than a PHP error.
- Live deploy completed for the redesigned homepage, all-tools page, shared header/footer, and the required supporting includes/assets. Deployed via SFTP/paramiko to `/home2/keyboar1/public_html`: `index.php`, `pages/all-tools.php`, `header.php`, `footer.php`, shared chrome/sidebar/footer components, all-tools renderer/data, AdSense/helper components, homepage/blog/category components, keyboard tester dependencies, Microsoft Store badge assets, the new home hero image set, and `api/send-ad-inquiry.php`. Remote `.env` already had `KBT_AD_INQUIRY_RECIPIENT` and `KBT_AD_INQUIRY_SECRET`, so no secret values were written into public files. Live verification passed for `https://keyboardtester.click/` and `https://keyboardtester.click/pages/all-tools.php`: HTTP 200, one H1, one canonical, shared header/footer present, Microsoft Store badge present, no PHP error markers, no private recipient email in rendered source, two all-tools sponsorship sections present, all 1/3/5-month packages/prices present, modal form and signed CAPTCHA fields present, key CSS/JS/image assets HTTP 200, and the ad endpoint rejects direct GET/invalid CAPTCHA without sending mail.
- Local-only SEO parity audit completed before any live update. Compared 871 live sitemap URLs against local `https://localhost/kbt` rendered output and saved the report at `seo-audit-2026-05-17-full-local-vs-live-seo-compare.json`. Fixed missing local FAQ schema coverage by adding centralized FAQs for `keyboard_double_click`, `mouse_accuracy`, `mouse_drag`, and `right_click_cps` in `includes/schema.php`, and set the redesigned keyboard tester route to use the `keyboard_tester` schema key plus the live keyboard OG image. Repaired mojibake in affected localized SEO text across 72 localized/root files plus five shared localized renderer includes. Final audit result: 0 local non-200 URLs for live 200 URLs, 0 PHP error markers, 0 JSON-LD parse errors, 0 missing live schema types, 0 hreflang count differences, 0 H1 count issues, and 0 mojibake markers. Remaining 268 meta differences are content/title/description/keyword changes in the local redesign versus current live, not structural SEO failures. No live deploy.
- Local-only all-tools advertiser inquiry feature completed for `pages/all-tools.php` via `includes/render-all-tools.php` plus new endpoint `api/send-ad-inquiry.php`. Added two direct-sponsorship CTA bands on the English all-tools page (top and bottom), launch-rate packages at $59 / $149 / $229 for 1 / 3 / 5 months, and a modal inquiry form for name, work email, company, target URL, campaign details, package, placement, honeypot, and signed stateless math CAPTCHA. Recipient is read server-side from local `.env` (`KBT_AD_INQUIRY_RECIPIENT`) and is not rendered in page text/source. Verification passed: XAMPP PHP lint for the renderer and endpoint, local HTTPS 200 render, two sponsor bands present, signed `captcha_token` present, no raw private recipient in rendered source, modal opens/focuses correctly, default 3-month package selected, and invalid CAPTCHA returns the expected JSON/UI error without attempting a visible email handoff. No live deploy.
- Local-only follow-up for the typing test and footer social config. Fixed the typing prompt box so the active character auto-follows inside long/custom text instead of requiring users to manually scroll the prompt area; added contained overscroll behavior and verified with PHP lint / `git diff --check` / browser auto-follow smoke testing. Updated the shared X social link in `config.php` from `https://x.com/software15325` to `https://x.com/HW_Tools`, which flows through the shared footer via `includes/components/site-chrome.php`. Verification passed locally on the homepage and `keyboard_typing_test.php`: HTTP 200, new X URL present, old X URL absent, 1 H1, 1 canonical, and no PHP error markup. No live deploy.
- Local-only typing test upgrade completed for `keyboard_typing_test.php` and `tools/keyboard_typing_test_tool.php`. Preserved the English shared renderer route, existing URL, meta/canonical path, and `typing_speed` schema while replacing the old simple WPM widget with a richer typing test panel: built-in/custom prompt modes, paste-your-own-text support, clipboard import, timed or finish-prompt runs, strict mode, start/pause/finish/reset controls, live net WPM, gross WPM, CPM, accuracy, errors, consistency, progress, mistake breakdown, local result history, and copyable result text. Also cleaned the route page title dash to ASCII. Verification passed locally: PHP lint for both files, `git diff --check` for the touched files, HTTPS rendered-source check with 1 H1 / 1 canonical / no PHP error markup, and in-app browser smoke tests for page load, real keypress input, finish/result history, and custom-text mode. No live deploy.
- Local-only English tool-page template rollout completed. Added shared renderers for English reusable-partial tools and older inline tools, then converted the English tool routes to the finalized homepage-style structure: shared header, Microsoft-style hero, category band, live tool stage with header-hide behavior, after-tool sponsored placement, popular tools, balanced guideline/FAQ section, relevant blog cards, Microsoft Store banner, and shared footer. Preserved existing URLs, page meta/canonical variables, schema calls, and legacy SEO/source content where practical; trimmed long legacy guide paragraphs into compact visible summaries and added right-column checklist support when FAQ content is short to avoid large empty guide/FAQ columns. Verification passed locally: PHP lint for 118 converted/rendering files, rendered-source checks for 90 tool routes + 19 intent routes + `pages/all-tools.php` with 1 H1 and 1 canonical on converted pages, and headless Chrome desktop/mobile spot checks for homepage, mouse test, DPI test, key repeat, keyboard shortcut identifier, and dead-pixel pages. No live deploy.
- Local-only second-page template test completed on `mouse_sensitivity_DPI_tester.php`. Converted the DPI tester route to the same finalized 9-section home/mouse-page structure: shared header, Microsoft-style full-width hero, slim home category band, live DPI tool with the actual tool heading visible immediately after the category strip, blank after-tool sponsored placement, shared four-card popular tools, ad-railed guideline/FAQ section, relevant guide cards, Microsoft Store banner, and shared footer. Preserved the existing route, page title/description/keywords, canonical path through `seo-meta.php`, and `generateToolPageSchema('dpi_tester', ...)`. Replaced the old DPI hero partial with the home hero pattern, replaced the old long `.seo-content` article stack with compact keyword-rich guideline/FAQ content, added `dpi_after_tool_banner` to the local AdSense slot map, and added an internal heading to `tools/mouse_sensitivity_dpi_tester_tool.php`. Verification passed with XAMPP PHP lint, rendered-source checks for 1 H1 / 1 canonical / no old blocks / no visible ad text / no PHP error markup, `git diff --check`, and a 1920x900 headless Chrome screenshot showing the DPI tool heading at the fold. No live deploy.
- Local-only mouse fold adjustment. Removed the separate pre-tool intro block from `mouse-test.php` and removed the small `Mouse tester` kicker inside `tools/mouse_click_test.php`, so the first visible tool text is the actual heading `Mouse button and scroll wheel diagnostics`. Added page-specific mouse-stage overrides to remove the outer tool-shell padding/card and pull the tool header row up to the first viewport after the category strip, matching the homepage keyboard-preview behavior. Verified with XAMPP PHP lint, rendered source checks, and headless Chrome screenshots at 1920x1080 and 1920x900. No live deploy.
- Local-only mouse-page/homepage visibility refinement. Added a visible `tool-stage-intro` heading block above the live mouse tool so the first viewport after the hero/category strip clearly tells users that the tool is immediately below, matching the homepage behavior where the keyboard is partially visible. Added shared after-tool sponsored placement banners on both `index.php` and `mouse-test.php` before the popular-tools cards without visible `AD` / `Advertisement` wording. Extended the shared header hide/show script in `includes/components/site-chrome.php` from keyboard-only targeting to `[data-header-hide-stage]`, so the mouse tool gets the same extra testing space behavior as the homepage. Also refined `tools/mouse_click_test.php` with a more realistic mouse drawing, richer click/scroll diagnostics, sound toggle, and compact related mouse-check links; added `Popular tools` + `All tools` headings/links and `Latest guides` + `View all guides` headings around guide cards. Verification passed with PHP lint and rendered-source checks for the affected homepage, mouse page, shared components, ad-slot config, and header script. No live deploy.
- Local-only follow-up for the finalized homepage/mouse-page template. Removed visible ad placeholder wording from `includes/components/adsense-slot.php` and the loaded homepage hero placement CSS (`assets/css/index-modern.css` / `.min.css`) so local placement boxes stay blank until AdSense fills them. Restored a small shared `Popular tools` heading through `includes/components/tool-popular-tools.php`, which now appears above the four-card tool block on both the homepage and `mouse-test.php`. Rebuilt `tools/mouse_click_test.php` into a richer mouse button + scroll-wheel diagnostic panel inspired by scrolltest.net behavior: duration presets, start/pause/reset, sound toggle, click counters, scroll graph, elapsed/speed/delta metrics, reverse-event count, and a short live report. Verification passed with PHP lint for `mouse-test.php`, `index.php`, `includes/components/adsense-slot.php`, `includes/components/tool-popular-tools.php`, and `tools/mouse_click_test.php`; local rendered source confirms no visible `Ad slot` / `AD` placeholder text, `Popular tools` appears on both pages, and the mouse page includes the scroll dashboard and sound toggle. No live deploy.
- Follow-up local-only refinement for `mouse-test.php` after user rejected the first prototype as not close enough to the finalized homepage. The page now uses the homepage-style shared header loading path, full-width home hero treatment with hero Store button and hero ad slot, the same homepage category band component, live mouse tool, four-card popular tools, ad-railed homepage-style guide/FAQ layout, relevant guide cards, Microsoft Store banner, and shared footer. Removed the old `tool-category-strip` and old `.seo-content` article stack from this page. Verification passed with PHP lint for `mouse-test.php`, `help/brief-mouse-test.php`, `help/seo-content/mouse-test.php`, and `includes/components/home-category-band.php`; local rendered source has 1 H1, 1 canonical, shared header, hero, hero ad, hero Store button, category band, tool stage, popular tools, guide/FAQ, 2 guide rails, blog guide cards, Store banner, footer, no old category strip, no old SEO-content block, and no PHP error text. Browser check on `https://localhost/kbt/mouse-test.php` confirmed the section order and no current mouse-page console errors. No live deploy.
- Local-only single-page prototype for `mouse-test.php` after user paused the broader tool-page rollout. Rebuilt only the visible mouse-test page sequence to match the finalized homepage template: shared header, homepage-style full-width hero, category strip, live mouse tester, four-card popular tools, guideline/FAQ content, relevant blog guides, Microsoft Store banner, and footer. Removed the old mouse page trust strip, feature band, process cards, intent cluster block, and keyboard cross-link block from this page only. Preserved the existing SEO include, canonical/meta/schema output, tool include, URL, and no live deploy was run. Also bumped the service-worker version and added localhost-only no-store headers because the in-app browser was showing a stale cached copy of `mouse-test.php` on the plain URL. Verification passed: PHP lint for `mouse-test.php`, `help/brief-mouse-test.php`, and `includes/head-common.php`; `node --check sw.js`; local HTTPS 200; one H1; one canonical; old blocks absent; local no-cache header present; and browser check confirmed the section order after reload.

### 2026-05-16 (Codex)
- Local-only homepage category-link update. `help/brief-keyboard-tester.php` now sends all 7 homepage category-band links to `/pages/all-tools.php#keyboard`, `#mouse`, `#display`, `#camera`, `#audio`, `#utility`, and `#gaming` instead of the older category/tool destinations. `includes/render-all-tools.php` now treats an incoming category hash as an active filter on page load, so `/pages/all-tools.php#camera` opens with only the Camera section visible and the Camera tab/minimap state active. Verification passed: PHP lint for both files, homepage category-band source has 7 all-tools hash links and 0 old category/tool links, direct browser check for `#camera` showed active pill/map `camera`, 6 hidden sections, only the Camera section visible, and 2 visible Camera cards.
- Local-only all-tools ad filler pass for `/pages/all-tools.php`. `includes/render-all-tools.php` now inserts hidden filler ad cards into each category grid and uses a resize-aware placement script to show only the number needed to complete the current row count, with staggered category-specific positions so ads avoid vertical/horizontal alignment where the grid allows. `includes/adsense-slots.php` now maps three reusable all-tools grid placements to existing manual AdSense slots. Visible "Advertisement" wording was removed from the card markup; live slots render only the Google ad unit. Verification passed: PHP lint for `includes/render-all-tools.php`, `includes/adsense-slots.php`, and `help/brief-keyboard-tester.php`; local rendered source had 90 tool cards, 20 ad filler cards, no `Advertisement` text, and no PHP fatal/parse errors; headless Chrome desktop check at 1600x900 confirmed full rows, no consecutive ad cards, and Gaming ads at row/column `(1,2)`, `(2,3)`, `(3,1)`.
- Local-only homepage category strip line cleanup. `assets/css/index-modern.css`, `assets/css/index-modern.min.css`, and `help/brief-keyboard-tester.php` now remove the old category-card top border from the homepage category band and keep a single 2px `--cat-accent` strip per category, preventing the double/dual-color line seen after adding category icons. Verification passed: PHP lint, local homepage rendered without fatal/parse errors, and browser computed styles showed 7 category links/icons, `border-top-width: 0px`, and one 2px accent `::before` line per category.
- Local-only category color identity pass for `/pages/all-tools.php` and the homepage category band. `includes/render-all-tools.php` now carries each category accent into the top filter tabs, section header rules, card top borders, minimap rail, and minimap dropdown; selected tab count/icon contrast is explicitly corrected for light/dark themes; minimap dropdown color strips are inset per row so they do not appear as one continuous line. `help/brief-keyboard-tester.php` now adds the same category icons and accent variables to the homepage category strip. No live deploy. Verification passed: PHP lint for both files, rendered source found 90 all-tools cards, 7 homepage category icons, category accent vars, inset minimap stripe CSS, selected count overrides, and browser check confirmed 90 cards, 8 minimap bars/items, keyboard card/section/menu accents, stripe top/bottom gaps of 7px, 7 homepage icons/links/title rows, and 0 console errors.
- Follow-up refinement for local-only all-tools minimap title: replaced the floating menu title from localized `All Tools` text to branded `KeyboardTester.click`, using the same Impact/Arial Black logo font treatment with the `.click` blue accent and dark-theme matching colors. Verification passed: PHP lint, rendered source found the branded title and 90 cards, and browser smoke confirmed title text `KeyboardTester.click`, 13.5px logo-style font, no overflow, 8 bars, and 0 console errors.
- Follow-up refinement for local-only all-tools minimap styling: changed the tiny rail to use uniform short inactive lines (`15px`) and a longer active blue line (`26px`), and narrowed the hover category panel to `224px` with tighter padding/count spacing. Verification passed: PHP lint, rendered source still has 90 cards and 8 minimap bars, and browser check confirmed 224px panel width, 40px rail, 15px inactive line, 26px active line, 9px rail overlap, and 0 console errors.
- Follow-up fix for local-only all-tools minimap hover usability: removed the hover gap between the right-side rail and the category panel by moving the panel flush/overlapping against the rail. Verification passed: PHP lint, rendered source still has 90 cards and 8 minimap bars, and browser geometry check showed the panel now overlaps the rail by 9px with 0 console errors.
- Local-only `/pages/all-tools.php` quick minimap behavior update. `includes/render-all-tools.php` now makes the right-side category rail a hover/focus menu instead of a click-pinned menu; clicking the rail itself dismisses it, while clicking a category jumps to that page location, keeps all sections visible, updates the hash, and dismisses the panel. The tiny rail now has 8 section-aware bars and the active bar/item tracks the current scroll location, including the final category at page end. No live deploy. Verification passed: PHP lint, rendered source check found 90 cards, 8 minimap bars/items, no page-map click pinning, scroll-tracking/dismissed-hover code present, and in-app browser smoke found 90 cards, 8 bars, 8 items, 1 active bar/item, toggle click produced 0 `is-open`, 1 dismissed rail, and 0 console errors.
- Local Windows RAM/startup cleanup after user reported 90% memory usage with little open. Verified physical RAM was 90.1% used with abnormal kernel pool pressure (`Pool Paged Bytes` ~5.97 GB, `Pool Nonpaged Bytes` ~4.62 GB). Removed reappeared Chrome/Edge user Run auto-launch entries, removed OneDrive auto-start and disabled OneDrive scheduled tasks, stopped active `chrome.exe`/`msedge.exe` browser processes, stopped `MariaDB`, changed `MariaDB` startup from Automatic to Manual, kept `Apache2.4` running and Automatic for XAMPP, and set machine Chrome/Edge policies to disable background mode plus Edge startup boost. No KBT site code or live deploy.

### 2026-05-14 (Codex)
- 2026-05-16 follow-up: Completed the local-only `/pages/all-tools.php` card-image audit requested after the minimap work. `includes/render-all-tools.php` now preserves `blog/images`, `assets/images`, `producthunt-assets`, and Microsoft Store screenshot paths correctly, reads page/brief hero image candidates before falling back, resolves images in visible render order, and uses targeted relevant overrides where the destination page had a generic/repeated hero. Updated the affected English tool page `$pageOgImage` values and visible `help/brief-*` hero images so the all-tools card visual matches each destination page's own hero/OG image source, while keeping all tool URLs unchanged. No live deploy. Verification passed: PHP lint for the renderer plus affected root and brief files, rendered all-tools source found 90 cards, 90 unique image sources, 90 unique normalized visual keys, 0 missing images, 0 byte-duplicate image files, 0 hero mismatches for destination pages except the special keyboard tester directory that has no separate image hero, and in-app browser reload found 90 cards/images, 0 old `color-system-guide2.webp`, and 0 console errors.
- 2026-05-16 follow-up: Added a local-only ChatGPT-style page minimap / floating table-of-contents control to `/pages/all-tools.php` through `includes/render-all-tools.php`. The right-side stacked-line rail opens a rectangular category switcher with All Tools plus the 7 category counts, stays theme-aware in light/dark mode, works on narrow and desktop widths, and reuses the existing category filter state/history hash instead of changing tool URLs or card markup. No live deploy. Verification passed: PHP lint, rendered source found 90 cards, 90 images, 1 minimap, 8 minimap items, 0 old `color-system-guide2.webp` images, `git diff --check`, and in-app browser click test opened the minimap, selected Mouse, set `#mouse`, activated both the Mouse tab and minimap item, left 1 visible category / 6 hidden categories, and reported 0 console errors.
- 2026-05-15 follow-up: Replaced the local-only all-tools `Key Repeat Rate Tester` card image after user feedback. `includes/render-all-tools.php` now manually assigns `images/keyboard/special-keys-layout.webp` to `key-repeat-rate` and prevents the weak `color-system-guide2` variant from being reused as a separate visual when the stronger color-system visual is already present. No live deploy. Verification passed: PHP lint, local source check found 90 cards, 90 unique image sources, 0 missing images, Key Repeat uses `special-keys-layout.webp`, `color-system-guide2.webp` no longer appears, and browser reload found 90 cards/images with 0 console errors.
- 2026-05-15 follow-up: Normalized shared header dimensions in `includes/components/site-chrome.php` after user noticed header height/spacing differed between the homepage and `/pages/all-tools.php`. Root cause: homepage-specific `index-modern.css` had the final 64px desktop / 60px mobile rail sizing while the shared component still had a taller default. The shared header now owns the same rail dimensions, logo size, action height, spacing, CTA radius/color, and responsive nav height everywhere, so page changes should not resize the header or cause a header-height flicker. No live deploy. Verification passed: PHP lint for `includes/components/site-chrome.php` and `includes/render-all-tools.php`, local rendered HTML checks found the shared 64px/60px header rules and one shared header on both homepage and all-tools, `git diff --check` passed, and a temporary headless Chrome CDP measurement found homepage/all-tools desktop both render `headerHeight=65` including border and `containerHeight=64`, while mobile both render `headerHeight=61` including border and `containerHeight=60`.
- 2026-05-15 follow-up: Fixed the local-only all-tools page shared header theme behavior after user noticed the header did not visibly change between light and dark mode. Root cause: `includes/head-common.php` keeps the global `--header-bg` dark by default, while the homepage had page-specific light/dark header overrides that `pages/all-tools.php` did not load. `includes/render-all-tools.php` now scopes all-tools header CSS variables and controls so light mode uses a white Microsoft-style header and dark mode uses a dark header, without changing URLs, cards, or live production. Verification passed: PHP lint for `includes/render-all-tools.php`, local rendered HTML includes light/dark header variables, theme-button rules, the shared header, the theme toggle, and `theme.min.js`; `git diff --check` passed. Browser automation connected but timed out while clicking the responsive menu, so visual click verification was not completed in the in-app browser.
- 2026-05-15 follow-up: Removed the visible local-only `pages/all-tools.php` intro block after user feedback. `includes/render-all-tools.php` no longer renders the large `All Tools` kicker, H1 intro, explanatory paragraph, count badge, `Category guides` navigation strip, or duplicate filter-title label; the page now starts directly with the category filter and tool cards under the shared header. Kept a screen-reader-only H1 for accessibility/SEO structure without showing the removed section. No live deploy. Verification passed: PHP lint for `includes/render-all-tools.php`, local HTTPS source check found 0 hero sections, 0 guide nav blocks, 0 filter-title blocks, 90 card blocks, and 0 `Open tool` labels; in-app browser reload found 0 hero elements, 0 guide elements, 0 filter-title elements, 90 cards, 8 filter tabs, and 0 console errors.
- 2026-05-15 follow-up: Polished the local-only `pages/all-tools.php` cards after user feedback. `includes/render-all-tools.php` now uses each English tool's specific CTA from `includes/components/tool-list-data.php` instead of repeating `Open tool` on every card, while localized pages keep their translated generic fallback unless a localized override exists. Added English-specific polling CTA overrides so keyboard and mouse polling actions are distinct. Also expanded the all-tools visual duplicate guard for copied image assets such as `screen-test/hero` versus `screen-tester-monitor-inspection`, shared screen/mouse/audio step images, and other known duplicate stock assets, so the rendered 90-card set avoids identical visuals. No live deploy. Verification passed: PHP lint for `includes/render-all-tools.php`, local source has 90 card CTAs, 90 unique CTA labels, and 0 `Open tool` labels, browser reload found 90 cards/90 CTAs/0 `Open tool`/0 console errors, local card image audit found 90 card images, 90 perceptual image hashes, 0 duplicate perceptual-hash groups, and 0 missing images.
- 2026-05-15 follow-up: Updated the local-only `pages/all-tools.php` image resolver through `includes/render-all-tools.php` so no two visible tool cards reuse the same image. The resolver now tracks used image visual keys, treats WebP/PNG/resized variants of the same file as the same visual, tries exact tool folders first, then category-related folders, then a deduplicated local image pool. No URLs, canonicals, hreflang, schema, header, footer, or sidebar behavior changed. Verification passed: PHP lint for `includes/render-all-tools.php`, local HTTPS 200 for `/pages/all-tools.php`, rendered 90 cards/90 card images, 90 unique card image src values, 90 unique normalized visual keys, 0 missing card images, 0 sidebar elements, and in-app browser reload found 90 cards/90 card images with 0 console errors.
- 2026-05-15 follow-up: Redesigned the local-only `pages/all-tools.php` surface through the shared `includes/render-all-tools.php` renderer. The all-tools page now calls the shared header directly without rendering the old foldable sidebar, keeps the shared footer, and displays all 90 tool entries as Microsoft-style rectangular image cards with large top media, Segoe-style headings/descriptions, aligned blue CTAs, square corners, and responsive 4/3/2/1-column behavior. Added a resolver that reuses existing local tool/category hero images and falls back safely by category without changing tool URLs, canonical, hreflang, or schema. No live deploy. Verification passed: PHP lint for `includes/render-all-tools.php`, local HTTPS 200 for `/pages/all-tools.php`, rendered 90 cards/90 images/90 CTAs, 1 header, 1 footer, 0 sidebar elements, all local image paths exist, `git diff --check` clean for the renderer, in-app browser confirmed 90 cards/images/CTAs and no sidebar, and headless Chrome desktop/mobile CDP checks found 1 H1, 0 horizontal overflow, 0 broken images, 0 missing alt text, square card radius, and Microsoft blue CTA color.
- 2026-05-15 follow-up: Continued local-only homepage release polish after user feedback. Added a single official Microsoft Store badge button to the lower-right of the homepage hero image via `help/brief-keyboard-tester.php` and `assets/css/index-modern.css`/`.min.css`; desktop and mobile checks confirm the badge stays inside the hero image and does not overlap hero copy/actions. Also changed the AI chat greeter top-right control from a `v` hide-greeter action into a real `×` close button, regenerated `assets/js/ai-chat.min.js`, regenerated `assets/css/ai-chat.min.css`, and bumped footer chat asset versions to JS `v=2.7` and CSS `v=2.9`. Fixed local PWA release-audit noise by making `manifest.webmanifest` icon paths relative and updating `sw.js` to scope precache URLs correctly under `/kbt/` locally while still working at `/` live; `includes/head-common.php` now registers `sw.js?v=20260515`. Verification passed: PHP lint for `index.php`, `footer.php`, `includes/head-common.php`, and `help/brief-keyboard-tester.php`; `node --check` for `assets/js/ai-chat.js` and `.min.js`; regenerated CSS/JS minified assets; `git diff --check`; Playwright desktop/tablet/mobile/small-mobile audit found one H1, no 404 responses, no console errors, no broken images, no missing alt text, no page-level horizontal overflow, mobile menu/theme/`90+ Tools` visible, chat close dismisses the popup, `Chat now` still starts the chat and focuses input, and the back-to-top button docks before the footer. No live deploy.
- 2026-05-15 follow-up: Polished the local-only homepage popular-tools label and assistant greeter after user feedback. `index.php` now keeps only a small blue `Popular tools` H2 label above the four cards and removes the larger `Top online testing tools` line. `assets/css/index-modern.css` and `.min.css` style that label in the same small blue section-kicker pattern used by the guide cards. `assets/css/ai-chat.css` and `.min.css` remove the blue Emily/Groq/language header strip during the first greeter view, square the assistant window, online pill, collapse button, and Chat now button to match the rectangular homepage styling, and `footer.php` bumps the local chat CSS cache to `v=2.8`. No live deploy. Verification passed: PHP lint for `index.php` and `footer.php`, regenerated `assets/css/index-modern.min.css` and `assets/css/ai-chat.min.css`, `git diff --check`, and local HTTPS source check confirmed `Popular tools`, no `Top online testing tools`, and `ai-chat.css?v=2.8`.
- 2026-05-15 follow-up: Added a minimal local-only heading above the four top-performing homepage tool cards after user feedback. `index.php` now labels the card section with visible H2 `Popular tools` and `aria-labelledby="popular-tools-title"`, while `assets/css/index-modern.css` and `.min.css` add Microsoft-style light/dark heading typography. No live deploy. Verification passed: PHP lint for `index.php`, regenerated `assets/css/index-modern.min.css`, `git diff --check`, and local HTTPS source check confirmed the heading and ARIA wiring.
- 2026-05-15 follow-up: Polished the local-only homepage header behavior after user feedback. `includes/components/site-chrome.php` now auto-hides the shared header while the homepage `#keyboard-stage` is the active viewport section, then restores it after the user scrolls beyond the tool; it also keeps header menus/language panels from hiding while open. `tools/keyboard_tester_english.php` now makes the key-history controls follow the header/site theme only, so the keyboard tool's internal layout theme no longer forces the text box dark in site light mode. `assets/css/index-modern.css` and `.min.css` now keep the hero H1 as two controlled lines (`Open Source & Free` / `Keyboard Tester`) with no wrapping inside either line. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php`, `tools\keyboard_tester_english.php`, and `help\brief-keyboard-tester.php`; regenerated `assets/css/index-modern.min.css`; `git diff --check`; static local HTML check for `90+ Tools`, hero lines, and header-hide script; headless Chrome verified white key-history textbox in site light theme, dark key-history textbox in site dark theme, `site-header--tool-hidden` at `#keyboard-stage`, header restored after scrolling past the tool, and hero lines set to `nowrap`.
- 2026-05-15 follow-up: Slimmed the local-only homepage category band after user feedback. `assets/css/index-modern.css` and `.min.css` now reduce the category band padding, category cell min-height from 82px to 58px on desktop, title/copy font sizes, mobile cell height to 50px, and colored category line thickness from 4px to 2px. No live deploy. Verification passed: regenerated `assets/css/index-modern.min.css`; in-app browser reload at `https://localhost/kbt/?footer_icons_dark=1` found 1 category band, 7 category links, and 0 console errors.
- 2026-05-15 follow-up: Corrected the local-only latest blog and Microsoft Store sections after user clarified they should be rectangular, not curved. `includes/components/homepage-latest-blog.php` now uses `border-radius: 0` on homepage latest blog cards and images. `includes/components/microsoft-store-badge.php` now uses `border-radius:0` on the sitewide Store banner wrapper and hero Store slot wrapper. No live deploy. Verification passed: PHP lint for both component files and in-app browser reload at `https://localhost/kbt/?footer_icons_dark=1` found 3 latest cards, 1 Store banner, and 0 console errors.
- 2026-05-15 follow-up: Continued the local-only homepage polish from user screenshots. `assets/css/index-modern.css` and `.min.css` now make the top-performing tool card CTAs align horizontally by letting card bodies flex and reserving consistent title/description rows; header language/theme/menu buttons now have dark-theme backgrounds and the Tools CTA is explicitly theme-safe. `tools/keyboard_tester_english.php` now makes the homepage key-history textbox follow both site dark mode and the keyboard tool's own `data-theme="dark"` state. `includes/components/homepage-latest-blog.php` restyles latest blog cards as rounded rectangles with rounded images and a blue highlighted first card. `includes/components/microsoft-store-badge.php` restyles the Windows app banner as a rounded light blue/green rectangle. `index.php`, `includes/adsense-slots.php`, and `includes/components/adsense-slot.php` add desktop-only guide/FAQ ad rails with a min-width guard, and `includes/components/site-chrome.php` docks the Back to top button before the footer. No live deploy. Verification passed: PHP lint for changed PHP files, regenerated `assets/css/index-modern.min.css`, in-app browser DOM check at `https://localhost/kbt/?footer_icons_dark=1` found 4 popular cards/CTAs, 3 latest cards, 1 store banner, 2 ad rails, key history, header controls, and 0 console errors.
- 2026-05-15 follow-up: Polished the local-only homepage header, keyboard controls, and footer. `includes/components/site-chrome.php` now removes the redundant `Home` nav link from the header, keeps the logo as the home link, balances Blog/language/theme/Tools spacing, and makes dark-theme GitHub/X footer icons bright instead of black. `assets/css/index-modern.css` and `.min.css` now align the remaining homepage header controls at consistent 40px sizing. `tools/keyboard_tester_english.php` now makes the key-history textbox white with dark readable text and a blue focus ring. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php`, `tools\keyboard_tester_english.php`, and `index.php`; `git diff --check`; local HTML confirms no header Home nav, 1 Blog nav link, white key-history CSS, Tools CTA, and dark GitHub/X icon override; in-app browser DOM check at `https://localhost/kbt/?footer_icons_dark=1` found 1 Blog nav link, Tools CTA, key history, GitHub/X icons, and no console errors.
- 2026-05-15 follow-up: Updated the four local-only homepage top-performing tool cards with SEO-focused headings and descriptions while preserving URLs/images. `index.php` now uses `Mouse DPI Tester`, `Input Latency Test`, `Click Speed Test (CPS)`, and `Mouse Button Test` with descriptions targeting mouse DPI online, keyboard/mouse input delay, CPS/click speed, and mouse buttons/scroll wheel/pointer input. No live deploy. Verification passed: PHP lint for `index.php`, `git diff --check`, local HTML contains all updated headings/descriptions, and in-app browser DOM check at `https://localhost/kbt/?seo_cards=1` found 4 cards, all updated headings, and no console errors.
- 2026-05-15 follow-up: Tuned the shared back-to-top button to better match the Microsoft screenshot font/icon details. `includes/components/site-chrome.php` now uses a thin inline SVG up arrow instead of the heavy text arrow, Segoe UI text at 16px/600, 176px minimum button width, 14px icon/text gap, and flush-right fixed positioning with the same gray fill. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php`, `git diff --check`, local HTML contains the SVG viewBox, `stroke-width: 1.6`, `min-width: 176px`, `font-family: "Segoe UI"`, and `right: 0`, and in-app browser DOM check at `https://localhost/kbt/?back_top_ms_arrow=1` found text `Back to top`, one SVG icon, aria `Back to top`, and no console errors.
- 2026-05-15 follow-up: Updated the shared footer floating back-to-top control to match the Microsoft-style screenshot the user provided. `includes/components/site-chrome.php` now renders `↑ Back to top` as visible text, updates the English aria label to `Back to top`, and styles the control as a fixed rectangular gray button (`#d9d9d9`, 154px min-width, 48px height, square corners) with mobile sizing. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php`, `git diff --check`, local HTML contains `Back to top`, `back-to-top__icon`, `min-width: 154px`, and `background: #d9d9d9`, and in-app browser DOM check at `https://localhost/kbt/?back_to_top_ms=1` found text `↑Back to top`, aria `Back to top`, one icon span, and no console errors.
- 2026-05-15 follow-up: Refined the local-only homepage header/hero wording after user rejected the previous logo treatment. `includes/components/site-chrome.php` now uses a wider Microsoft-like header rail, removes the Open Source badge from the logo area, and uses an Impact/Arial Black-style stretched wordmark. `help/brief-keyboard-tester.php` now uses H1 `Open Source & Free Keyboard Tester`, split into two visible line spans (`Open Source & Free` / `Keyboard Tester`). `assets/css/index-modern.css` sets the final hero title to `max-width: 16ch`, displays `.hero-title-line` as blocks, and `assets/css/index-modern.min.css` was regenerated. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php`, `help\brief-keyboard-tester.php`, and `index.php`, `git diff --check`, local HTML confirms the two title spans and no header `Open source</span>` badge, and in-app browser DOM check at `https://localhost/kbt/?hero_title_2line=1` found H1 text `Open Source & Free Keyboard Tester`, exactly 2 `.hero-title-line` spans, and no console errors.
- 2026-05-15 follow-up: Refined the local-only homepage/shared header and homepage keyboard controls after user feedback. `includes/components/site-chrome.php` now uses a stretched wordmark treatment with Windows-native display font sizing instead of a larger expanded logo, changes the header badge to compact `Open source` info-chip styling, removes the old `Try Tools` CTA/scroll handler, and moves `Tools` into the same CTA slot linking to `/pages/all-tools.php`. `assets/css/index-modern.css` mirrors the homepage-specific wordmark/badge treatment and `assets/css/index-modern.min.css` was regenerated. `tools/keyboard_tester_english.php` now makes the homepage controls flatter with Microsoft-style 2px radii, neutral reset/select controls, and `#0067b8` primary Advanced Options styling. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php` and `tools\keyboard_tester_english.php`, `git diff --check`, local HTML contains `Open source`, `Segoe UI Variable Display`, `scaleX(1.12)`, and `siteHeaderToolsCta`, and in-app browser DOM check at `https://localhost/kbt/?logo_stretch_fix=1` found `Open source`, no `Try Tools`, one Tools CTA pointing to `/kbt/pages/all-tools.php`, and no console errors.
- 2026-05-15 follow-up: Updated the local-only homepage guide/FAQ content for the new theme while preserving ranking terms. `index.php` now uses the heading `How to run an accurate online keyboard test`, keeps terms like `online keyboard tester`, `stuck keys`, `keyboard ghosting`, `N-key rollover`, `keyboard not typing`, and adds a visible guide step for `Use Advanced Options and Pro Test` with heatmap, statistics, ghost-click monitoring, latency checks, and exportable proof. Updated matching homepage FAQPage JSON-LD in `includes/schema.php` so visible FAQ and schema stay aligned. No live deploy. Verification passed: PHP lint for `index.php` and `includes\schema.php`, `git diff --check`, local HTML contains the Advanced Options/Pro Test wording and 8 visible FAQ details / 8 FAQ schema questions, and in-app browser DOM check at `https://localhost/kbt/?advanced_pro_guideline=1#guidelines` found the new terms and no console errors.
- 2026-05-15 follow-up: Removed the lower local-only homepage keyboard-variant/extra-tool block but restored the latest blog posts after the user clarified not to remove blog content. `index.php` now omits the `Keyboard test variants`/`Popular Keyboard Checks` intent cluster, the `home_after_guide` ad slot, `related-tools.php`, and the single tool blog CTA, while keeping `includes/components/homepage-latest-blog.php` so the `Latest guides` / `Fresh hardware testing guides` blog section remains. No live deploy. Verification passed: PHP lint for `index.php` and `tools\keyboard_tester_english.php`, `git diff --check`, repo scan confirmed the removed variant/includes are absent from `index.php`, and in-app browser DOM check at `https://localhost/kbt/?blog_restored=1` found the latest-guides blog heading restored, keyboard variant phrases absent, and no console errors.
- 2026-05-15 follow-up: Updated the local-only homepage hero/header after user requested the Microsoft screenshot font/color style, image shifted right, H1 text `Free Keyboard Tester`, larger `KeyboardTester.click` logo text without increasing the header strip, and header pill text `Open Source`. Changed `help/brief-keyboard-tester.php` H1, changed shared local header pill text in `includes/components/site-chrome.php`, and added final scoped CSS in `assets/css/index-modern.css` for a dark `#333333` hero overlay, Segoe-style white H1/lede, Microsoft blue `#0078d4` CTA, right-shifted hero image crop, larger homepage brand text with fixed compact header height, and mobile dark copy band; regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `includes\components\site-chrome.php`, `help\brief-keyboard-tester.php`, `index.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; Chrome headless screenshots at 1821x900 and 390x844; in-app browser reload at `https://localhost/kbt/?ms_hero=1` confirmed H1 `Free Keyboard Tester`, pill text `Open Source`, 7 category links, headphones hero source, keyboard tester present with 104 keys, and no console errors.
- 2026-05-15 follow-up: Added the category banner back below the local-only homepage hero instead of overlaying it on the image. Updated `help/brief-keyboard-tester.php` with a `home-category-band` containing 7 category links (Keyboard, Mouse, Display, Camera, Audio, Utilities, Gaming) and added final scoped CSS in `assets/css/index-modern.css` for a compact Microsoft-style band with colored top bars, light/dark contrast, and horizontal mobile scrolling; regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `help\brief-keyboard-tester.php`, `index.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; Chrome headless screenshots at 1821x900 and 390x844; in-app browser reload at `https://localhost/kbt/?category_band=1` confirmed 1 hero image, 1 category band, 7 category links, headphones hero source, keyboard tester present with 104 keys, and no console errors.
- 2026-05-15 follow-up: Updated the generated local-only homepage hero image after the user liked the generated direction but wanted headphones instead of the foreground keyboard. Used built-in image editing on the generated hero reference to remove the physical keyboard and add matte black over-ear headphones with blue accents, generated responsive JPEG assets as `images/home/headphones-diagnostic-hero-{900,1280,1792}.jpg`, and updated `help/brief-keyboard-tester.php` sources/alt text. Kept the existing generated-workspace hero CSS and regenerated no additional CSS. No live deploy. Verification passed: PHP lint for `help\brief-keyboard-tester.php`, `index.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; Chrome headless screenshots at 1821x900 and 390x844; in-app browser reload at `https://localhost/kbt/?headphones_hero=1` confirmed 1 hero image, `headphones-diagnostic-hero-1280.jpg`, category count 0, keyboard tester present with 104 keys, and no console errors.
- 2026-05-15 follow-up: Replaced the local-only homepage hero portrait/photo direction after user disliked the banner. Used built-in image generation to create a no-person keyboard/laptop diagnostic workspace image, saved responsive JPEG assets as `images/home/keyboard-diagnostic-hero-{900,1280,1792}.jpg`, updated `help/brief-keyboard-tester.php` to reference the new assets and alt text, and added a final generated-workspace hero pass in `assets/css/index-modern.css` with a full image background, soft left readability gradient, black/blue text treatment, dark-theme support, and mobile image-first stacking; regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `index.php`, `help\brief-keyboard-tester.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; local HTTP 200 with new hero asset/no Pexels portrait/no category markup; Chrome headless screenshots at 1821x900 and 390x844; in-app browser cache-busted reload at `https://localhost/kbt/?hero_check=20260515` confirmed 1 hero image, `keyboard-diagnostic-hero-1280.jpg`, category count 0, keyboard present with 104 keys, and no console errors.
- 2026-05-15 follow-up: Reworked the local-only homepage hero based on the user's reference image direction: left-aligned copy, right-side uploaded Pexels image, and site colors only instead of the reference orange. Added a final scoped hero override in `assets/css/index-modern.css` with a white/blue split background, subtle blue accent shape, black desktop headline, blue CTA/link treatment, right-aligned contained image, dark-theme overrides, and mobile stacking with image first/copy below; regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `index.php`, `help\brief-keyboard-tester.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; Chrome headless screenshots at 1821x900 and 390x844; in-app browser reload at `https://localhost/kbt/` confirmed 1 hero image, category count 0, keyboard present with 104 keys, and no console errors.
- 2026-05-15 follow-up: Adjusted the local-only homepage hero so the uploaded Pexels image is shown in full, including the lower laptop/keyboard area. Updated `assets/css/index-modern.css` and regenerated `assets/css/index-modern.min.css` so desktop hero media uses `object-fit: contain`, left alignment, a dark right-side field, and right-aligned copy instead of cropping the image as a cover background; mobile now shows the full image as a top band with copy below. No live deploy. Verification passed: PHP lint for `help\brief-keyboard-tester.php`, `index.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; local HTTP 200 confirms the Pexels hero remains and category markup is absent; in-app browser reload reports 1 hero image, category count 0, keyboard present with 104 keys, and no console errors; Chrome headless screenshots at 1821x900 and 390x844 confirm the full image is visible and the keyboard/tester content remains below.
- 2026-05-15 follow-up: Removed the local-only homepage category strip from the hero and replaced the hero media with the user's uploaded Pexels laptop/person photo. Generated responsive JPG assets under `images/home/pexels-freestockpro-hero-{900,1280,1920}.jpg`, updated `help/brief-keyboard-tester.php` picture sources/alt text, adjusted the desktop hero copy width/crop in `assets/css/index-modern.css`, and regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `help\brief-keyboard-tester.php`, `index.php`, and `tools\keyboard_tester_english.php`; `git diff --check`; local HTTP 200 has the new hero/no old hero/no category markup; in-app browser reload at `https://localhost/kbt/#keyboard-stage` confirmed 1 hero image, category count 0, keyboard present with 104 keys, new hero image source, and no console errors.
- 2026-05-15 follow-up: Fixed the local-only homepage keyboard viewport issue after the keyboard appeared out of screen. Updated `assets/css/index-modern.css` and regenerated `assets/css/index-modern.min.css` with homepage-specific fit rules: reduced desktop banner height, added `scroll-margin-top` on `#keyboard-stage`, widened the tool shell from the older 1180px cap, capped it at 1440px for sane scaling, and forced the keyboard container to stretch within the available viewport. No live deploy. Verification passed: PHP lint for `index.php` and `tools\keyboard_tester_english.php`, `git diff --check`, in-app browser reload at `https://localhost/kbt/#keyboard-stage` confirmed 1 keyboard tester, 104 keys, no cat progress, and no console errors; headless desktop checks at 1821x900, 1600x900, and 1365x900 confirmed no horizontal overflow, 104 visible keys, 0 keys outside the right edge, and 0 keys clipped below the viewport.
- 2026-05-15 follow-up: Redesigned the local-only desktop keyboard control area and placed it visually under the keyboard. Updated `tools/keyboard_tester_english.php` so the keyboard tester wrapper uses flex ordering, the physical keyboard stays first, the key-history/settings panel sits below it in a minimal white Microsoft-style control surface, Advanced Options expands below that panel, and the empty legacy `progress-section` remains hidden so it no longer shows as a dark square. No live deploy. Verification passed: PHP lint for `tools\keyboard_tester_english.php` and `index.php`, targeted `git diff --check`, localhost HTTP 200, desktop Playwright at 1365x900 confirmed keyboard order 10, controls order 20, feature controls order 30, controls top below keyboard bottom, no horizontal overflow, `cat-progress-section` count 0, and the expanded advanced controls render without the empty progress placeholder.
- 2026-05-15 follow-up: Removed the local-only homepage cat/Pacman progress strip from the keyboard tester while keeping the keyboard itself intact. Added `kbtSuppressKeyboardCatProgress` in `index.php`, guarded the cat progress preload block on the homepage, and guarded the cat progress CSS/component/script in `tools/keyboard_tester_english.php` so the shared module still works on other keyboard pages. No live deploy. Verification passed: PHP lint for `index.php` and `tools\keyboard_tester_english.php`, targeted `git diff --check`, localhost HTTP 200 with `cat-progress-section` count 0, `FULL GAME`/`Dots:` absent, keyboard tester present, and in-app browser check confirmed 1 keyboard tester, 104 keys, and no console errors.
- 2026-05-15 follow-up: Updated the local-only homepage Microsoft-style tiles to feature the user's listed top-performing tools instead of the previous mixed popular tests. Replaced tile links/content/images in `index.php` with `mouse_sensitivity_DPI_tester.php` using `images/mouse-dpi/hero.*`, `latency-checker.php` using `images/latency-checker/hero.*`, `mouse_speed_tester.php` using `images/mouse-speed/hero.*`, and `mouse-test.php` using `images/mouse/hero.*`. Kept the tile styling unchanged. No live deploy. Verification passed: PHP lint for `index.php`, `git diff --check`, in-app browser sees the four expected tile titles/CTAs, and headless Playwright confirms all four tile hrefs, relevant WebP images loaded, no horizontal overflow, and CTA text remains white on Microsoft blue.
- 2026-05-15 follow-up: Tweaked the local-only Microsoft-style popular-test tiles after user feedback that the tile images were too horizontal. Updated `assets/css/index-modern.css`/`.min.css` so desktop tile images use a taller 16:9 aspect ratio and mobile tile images use 16:10. No live deploy. Verification passed: PHP lint for `index.php`, `git diff --check`, rendered desktop tile image dimensions are 296x166 with ratio 1.78 and no horizontal overflow, and all four tile images loaded successfully in the Playwright tile-grid screenshot.
- 2026-05-15 follow-up: Updated the local-only homepage popular-test section after user feedback. Removed the visible `Popular tests`, `Continue with the right diagnostic`, and `Four quick paths...` intro text from `index.php`. Restyled the four popular-test cards in `assets/css/index-modern.css`/`.min.css` as full-width Microsoft-style product tiles with Segoe-style typography, larger title/body sizing, edge-to-edge section width, square card edges, subtle shadow, larger image bands, and blue CTA buttons inside each tile. No live deploy. Verification passed: PHP lint for `index.php` and `help\brief-keyboard-tester.php`, `git diff --check`, local HTML has 1 H1, 4 guide cards, 4 tile CTAs, 9 FAQs, and none of the removed intro text; in-app browser reload at `https://localhost/kbt/` sees 4 guide cards and 0 old heading/kicker; headless Playwright desktop/mobile light/dark reports no horizontal overflow, 4 cards, 4 CTAs, and old section text absent.
- 2026-05-15 follow-up: Reworked the local-only homepage again to replicate the Microsoft-style hero screenshot more directly. Downloaded a related free stock laptop/keyboard workspace photo from Pexels (`https://www.pexels.com/photo/dark-desk-setup-with-laptop-and-keyboard-35697247/`), optimized it into local assets under `images/home/keyboard-workspace-hero-{900,1280,1920}.{webp,jpg}`, and changed `help/brief-keyboard-tester.php` to use those local responsive sources. Updated `assets/css/index-modern.css`/`.min.css` so the banner is a full-width background photo with dark left gradient, white hero text, blue CTA, and a contrast-safe category strip over the top. No live deploy. Verification passed: PHP lint for `index.php` and `help\brief-keyboard-tester.php`, `git diff --check`, local HTML has 1 H1/new hero image/4 guide cards/9 FAQs/tool present, in-app browser reload at `https://localhost/kbt/`, and headless Playwright desktop/mobile light/dark reports no horizontal overflow, loaded local hero image, 7 category links, 4 guide cards, 9 FAQ items, desktop 1365x900 shows 16 physical keys across 1 visible row, and mobile 390x900 shows the mobile tester in the first viewport.
- 2026-05-15 follow-up: Reworked the local-only homepage hero after user feedback that the previous visual was not neat. Kept the Microsoft-inspired structure but changed the dark full-image banner into a cleaner light/dark split hero, replaced the previous dark mouse/desk image with `images/keyboard/special-keys-layout-*`, shortened the H1 to `Free Online Keyboard Tester`, removed leftover dark-theme hero-card styling, kept the category strip contrast-safe in both themes, tightened mobile hero height so the tool appears sooner, and regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `index.php` and `help\brief-keyboard-tester.php`, `git diff --check`, in-app browser reload at `https://localhost/kbt/`, and headless Playwright screenshots/metrics for desktop/mobile light/dark under `%TEMP%\kbt-home-redesign-check\`; desktop 1365x900 shows 37 visible physical keys across 2 rows with no horizontal overflow, and mobile 390x900 has no horizontal overflow with the mobile tester visible in the first viewport.
- 2026-05-15 follow-up: Adjusted the local-only homepage again to match the Microsoft homepage pattern the user showed. Checked the current Microsoft homepage (`https://www.microsoft.com/en-pk`) for the structure: top nav, full-width hero image with short copy/CTA, and next content beginning below. Updated `help/brief-keyboard-tester.php` so the 7 category links sit in a sleek strip over the top of the main banner, made the banner full-bleed left/right, changed banner copy to overlay the image directly instead of sitting in a card, tuned light/dark category strip contrast, and reduced desktop banner height so the live keyboard tester starts immediately below with the first two keyboard rows visible in a 1365x900 first viewport. Kept the 4 popular-tool image cards after the tool and regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint, `git diff --check`, local HTML has 1 H1/7 category links/4 guide cards/9 FAQ items/tool present, in-app browser reload works, and headless Playwright light/dark desktop/mobile reports no horizontal overflow, canonical present, H1 white on banner, category strip contrast in both themes, and desktop first viewport shows 37 visible keys across 2 key rows (`visibleKeyTops` 808 and 858 at 1365x900).
- 2026-05-15 follow-up: Revised the local-only homepage redesign after user feedback that the prior pass was still the old site adjusted into a new template. New local structure now follows the requested order: header, large minimal image banner, category strip, live keyboard tool, four image cards linked to popular tools, then a compact keyboard guide and Q/A FAQ. Removed the visible old trust/features/process blocks from homepage flow, removed the old visible `help/keyboard-tester.php` include from the homepage, kept the existing H1 text, kept visible FAQ questions aligned with homepage FAQ schema, hid the old floating sidebar FAB on `body.home-redesign`, and regenerated `assets/css/index-modern.min.css`. No live deploy. Verification passed: PHP lint for `index.php` and `help\brief-keyboard-tester.php`, `git diff --check` for touched files, local HTML has 1 H1, 4 guide cards, 9 FAQ summaries, no visible old trust strip, keyboard tester still present, in-app browser sees banner/4 cards/9 FAQ/tool and hidden FAB, and headless Playwright at 1365x900 plus 390x900 light/dark reports no horizontal overflow, loaded banner/guide images, 7 category links, 4 guide cards, 9 FAQ items, canonical present, and no old trust/features/process sections.
- 2026-05-15 follow-up: Built the first local-only homepage redesign draft so the user can review before broader site/template work. Scope was design-only around `index.php`, `help/brief-keyboard-tester.php`, and `assets/css/index-modern.css`/`.min.css`: added `body.home-redesign`, a Microsoft/Fluent-inspired white/gray/blue header/hero treatment, user-facing hero copy, a seven-category homepage strip using existing URLs, responsive mobile CTA handling, light/dark scoped overrides, and regenerated the minified landing CSS. Preserved the existing H1 text, canonical/schema path, keyboard tester include, and URL structure. Verification passed: `php -l index.php`, `php -l help\brief-keyboard-tester.php`, `git diff --check` for touched files, local render at `https://localhost/kbt/` with exactly 1 H1, 7 suite links, `#keyboard-tester` present, canonical present, hero image loaded, no desktop/mobile horizontal overflow at 1365x900 and 390x900, and no dark-mode overflow. Browser plugin screenshot timed out, so headless Playwright saved local preview screenshots under `%TEMP%\kbt-home-redesign-check\`.
- 2026-05-15 follow-up: Created a full local filesystem backup before starting the planned local-only redesign work. Backup path: `C:\xampp\htdocs\kbt-backups\kbt-local-backup-20260515-093053`; robocopy log: `C:\xampp\htdocs\kbt-backups\backup-20260515-093053-robocopy.log`; manifest: `BACKUP-INFO.txt` inside the backup. Source was `C:\xampp\htdocs\kbt`, Git HEAD `fe0ebb0`, with 757 dirty/untracked status lines at backup time. Robocopy copied 12,333 source files plus the backup manifest, about 0.69 GB, exit code 3 (successful copy with extras). Spot checks confirmed `.env`, `.git`, `.ssh-deploy`, `blog`, and `includes` exist in the backup. This was local only; no live deploy.
- 2026-05-15 follow-up: Removed the `Privacy` and `About` links from the shared header navigation in `includes/components/site-chrome.php` after the user asked for those links to appear only in the footer. Kept footer link lists untouched. Deployed by removing only the two matching live header anchors over SFTP/paramiko. Verification passed: PHP lint clean locally, `git diff --check` clean for the touched file, local and live `/blog/` header nav no longer contains `privacy-policy.php` or `about-us.php`, header still contains Home/Tools/Blog, footer still contains Privacy/About, and live headless render reports nav text `Home Tools Blog EN ... Try Tools` with no horizontal overflow.
- 2026-05-15 follow-up: Enlarged the shared site-header brand lockup after the user clarified they meant the `KeyboardTester.click` text/logo area. Updated `includes/components/site-chrome.php` locally and patched only the matching live header CSS/markup over SFTP/paramiko to avoid uploading unrelated dirty local changes in the same file. New live rendering uses a larger desktop brand (`clamp(1.28rem, 1vw + 1rem, 1.42rem)`), 42px desktop logo, larger open-source pill, and a 1080px hamburger breakpoint so the bigger brand does not crowd the nav at tablet widths. Verification passed: PHP lint clean locally, `git diff --check` clean for the touched file, live `/blog/?codex_header_check=20260515` HTTP 200 with new CSS/42px marker and no old `font-size: 1.08rem`, and headless live renders at 1365/1024/390px showed no body horizontal overflow.
- 2026-05-15 follow-up: Added `codex-ga-analytics@gen-lang-client-0940278364.iam.gserviceaccount.com` to the new GA4 property `KeyboardTester.click` (`properties/537895037`) as `predefinedRoles/viewer` through the official Google Analytics Admin API Explorer after the GA UI rejected the service-account email. API Explorer returned HTTP 200 with an access binding under `properties/537895037/accessBindings/...`. Local verification with `google-analytics-credentials.json` now passes: `AnalyticsAdminServiceClient.list_account_summaries()` returns account `accounts/38868148` / `nasirtrekkingID`, property `properties/537895037` / `KeyboardTester.click`, and a Data API `run_report` call succeeds with 0 rows (expected for a fresh property).
- 2026-05-15 follow-up: User created a new GA4 property under account `nasirtrekkingID` for `KeyboardTester.click` with web stream ID `14880944957` and Measurement ID `G-G5EY8TGGE6`. Replaced the old sitewide GA4 ID `G-DPRQF0RTQ7` with `G-G5EY8TGGE6` in `includes/head-common.php` and deployed only that shared head file over SFTP/paramiko. Verification passed: PHP lint clean, touched-file `git diff --check` clean, local homepage render contains the new ID and no old ID, live homepage, `mouse-test.php`, and `blog/best-gaming-mouse-2026.php` all return HTTP 200, contain `gtag/js?id=G-G5EY8TGGE6`, contain `gtag('config', 'G-G5EY8TGGE6')`, do not contain `G-DPRQF0RTQ7`, show no PHP error text, and live CSP still allows `googletagmanager.com` and `google-analytics.com`. The later follow-up above resolved the GA4 property-access blocker for MCP.
- 2026-05-15 follow-up: User downloaded a new Google Cloud service-account JSON for visible project `Keyboard` / `gen-lang-client-0940278364`. Copied it to ignored local file `google-analytics-credentials.json` and reconfigured global Codex MCP server `analytics-mcp` to use that credential plus `GOOGLE_CLOUD_PROJECT=gen-lang-client-0940278364` and `GOOGLE_PROJECT_ID=gen-lang-client-0940278364`. Initial smoke test authenticated without the old API-disabled error but returned `account_summary_count 0`; the later follow-up above resolved this by adding the service account to GA4 and re-verifying `properties/537895037`.
- Installed Google's `google-analytics-mcp` / `analytics-mcp` MCP server locally for Codex. Because `pipx` was not installed and default `python` is 3.14, installed `pipx` under the existing Python 3.11 runtime and then installed `analytics-mcp` 0.5.0 with Python 3.11. Registered global Codex MCP server `analytics-mcp` with command `C:\Users\TECH OFFICE\.local\bin\analytics-mcp.exe` and env keys `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_CLOUD_PROJECT`, and `GOOGLE_PROJECT_ID` pointing at the existing `google-credentials.json` / `kbt-indexing-4659` project. Verification passed for local install and Codex MCP registration. Remaining blocker: Google Analytics Admin API and Google Analytics Data API are not enabled in project `kbt-indexing-4659`, and the service account does not have permission to enable them (`serviceusage.googleapis.com` returned 403). User/project owner needs to enable `analyticsadmin.googleapis.com` and `analyticsdata.googleapis.com` in Google Cloud Console, then ensure `kbt-indexing-sa@kbt-indexing-4659.iam.gserviceaccount.com` has at least Viewer access on the GA4 property.
- Removed the experimental `sitemap2.xml` now that it is no longer needed. Updated local and live `robots.txt` plus `robots.php` so only `sitemap.xml` and `sitemap.txt` are advertised, deleted `/home2/keyboar1/public_html/sitemap2.xml` over SFTP/paramiko, and deleted `https://keyboardtester.click/sitemap2.xml` from the GSC Domain property (`sc-domain:keyboardtester.click`) via the Search Console API. Verification passed: `robots.php` PHP lint clean, touched-file `git diff --check` clean, live `robots.txt` returns HTTP 200 as `text/plain; charset=utf-8`, no longer contains `sitemap2.xml`, still contains the XML and TXT sitemap declarations, live `sitemap.xml` and `sitemap.txt` return HTTP 200 with correct content types, live `sitemap2.xml` returns HTTP 404, and GSC sitemap list now contains only `https://keyboardtester.click/sitemap.xml` and `https://keyboardtester.click/sitemap.txt`.
- Cleaned up the live `mouse-test.php` page after the user reported duplicate FAQ content. Removed the redundant generic `help/mouse-click-test.php` guide/FAQ include and the broad `includes/components/tools-list.php` catalog from the focused mouse tester page. Repointed the hero "Browse all tools" CTA to `/pages/all-tools.php` and the tool-stage quick-tips CTA to `#seo-content` so no removed anchors remain. Also normalized the mouse-page keyboard cross-link separators to ASCII hyphens while touching the section. Deployed only `mouse-test.php` and `help/brief-mouse-test.php` via SFTP/paramiko. Verification passed: PHP lint clean, `git diff --check` clean for the touched files, localhost and live HTTP 200, exactly 1 H1, exactly 5 visible FAQ `<details>`, one `FAQPage` schema marker, one `WebApplication` schema marker, canonical still `https://keyboardtester.click/mouse-test.php`, no PHP warnings, no missing hash targets, duplicate `Online Mouse Tester Guide` absent, `More testing tools` absent, updated `/pages/all-tools.php` link live, all sampled mouse WebP images return HTTP 200, and direct IndexNow submission for the URL returned HTTP 200.
- Added the GA4 measurement tag `G-DPRQF0RTQ7` sitewide through the shared `includes/head-common.php` include. Inserted the official Google tag after the existing analytics DNS prefetch block so every page using the shared head emits the GA4 loader and `gtag('config', 'G-DPRQF0RTQ7')`. Kept the change scoped: did not modify `.htaccess` or `privacy-policy.php` because live CSP already allows `googletagmanager.com`/`google-analytics.com`, and the privacy policy already discloses Google Analytics/analytics cookies. Deployed by patching the remote `includes/head-common.php` over SFTP/paramiko instead of uploading unrelated dirty local changes. Verification passed: local PHP lint clean; local homepage render contains the GA4 loader/config; live homepage, `mouse-test.php`, and `blog/best-gaming-mouse-2026.php` all return HTTP 200, contain `G-DPRQF0RTQ7`, contain `gtag/js?id=G-DPRQF0RTQ7`, contain the config call, show no PHP error text, and send CSP headers allowing GTM/GA domains.
- Cleaned up the high-performing live `mouse_sensitivity_DPI_tester.php` page without changing the URL, title, meta description, canonical, primary tool, DPI reference content, related tools, blog CTA, or JSON-LD schema. Removed the broad `includes/components/tools-list.php` "More testing tools" section from the DPI page, changed the hero secondary CTA from the removed `#tools` target to `#guidelines`, and replaced the generic `help/mouse-sensitivity-dpi-tester.php` guide/accordion with a short DPI-specific checklist. Deployed the production page safely by patching the remote file over SFTP/paramiko instead of uploading unrelated local dirty changes from the same page. Verification passed locally and live: HTTP 200, exactly 1 H1, no PHP warnings, primary DPI tool present, canonical still `https://keyboardtester.click/mouse_sensitivity_DPI_tester.php`, WebApplication and FAQPage schema markers still present, `More testing tools` removed, generic FAQ removed, FAQ heading appears once, new `Quick DPI Test Checklist` present, related tools and blog CTA still present, and direct IndexNow submission for the DPI URL returned HTTP 200.

### 2026-05-13 (Codex)
- Published the canonical DEV.to cross-post for `blog/ghost-touch-test-online-touchscreen-dead-zones.php` from `dev-to-articles/06-ghost-touch-test-online-touchscreen-dead-zones.md`. Adapted the article into DEV.to-native Markdown with front matter, four tags, canonical URL back to the KeyboardTester.click article, absolute live WebP image URLs, DEV.to YouTube liquid embed `{% youtube Un0w5FdlTrA %}`, source links, and no PHP/layout markup. Published via `dev-to-publish.py` using the existing `.env` API key. DEV.to article ID `3665485`, public URL `https://dev.to/nasirazizawan/ghost-touch-test-online-find-phantom-taps-dead-zones-and-touchscreen-drift-4epj`. Verification passed: API/list shows the article as published, API detail reports the correct canonical URL, public page returns HTTP 200 with title, canonical article URL, cover image, YouTube marker, and touch-screen tool link present, and all referenced live KeyboardTester images return HTTP 200 as `image/webp`.
- Resolved the Google Search Console Videos non-critical `uploadDate` schema issue from the 2026-05-13 CSV export. Converted date-only `VideoObject.uploadDate` values to full ISO datetimes with timezone in seven older blog pages: `blog/how-to-copy-pro-crosshair-cs2-valorant-generator-guide.php`, `blog/keyboard-switch-types-gaming-guide.php`, `blog/league-of-legends-actions-per-minute-tracker-apm-test.php`, `blog/mouse-acceleration-windows-11-disable-raw-input-gaming-guide.php`, `blog/v4n4g0n-keyboard-45-percent-custom-mechanical-guide.php`, `blog/what-is-keyboard-ghosting-anti-ghosting-fix-guide.php`, and `blog/what-is-webrtc-leak-how-to-fix-guide.php`. Deployed only those seven scoped article files via SFTP/paramiko. Verification passed: PHP lint clean for all seven files, repo scan found no remaining date-only `uploadDate` values in `blog`/`includes`, live pages return HTTP 200 with exactly 1 H1 and valid timezone-bearing `uploadDate` values, built-in IndexNow returned HTTP 200, and direct IndexNow submission for all seven URLs returned HTTP 200.
- Follow-up deployed for the upgraded `touch-screen-test.php` after user screenshot feedback. Added prominent in-tool links to the relevant live blog guide `blog/ghost-touch-test-online-touchscreen-dead-zones.php` via a mobile-visible quick-guide panel and a side-panel guide card, updated the visible FAQ plus FAQPage JSON-LD from 4 to 5 questions, and kept the guide link in the how-to copy. Fixed the laptop/color-mode text overlap by removing the duplicate canvas-centered `"{Color} display check"` label from `assets/js/touch-screen-test.js`; color mode now shows only the status pill (`Tap to cycle display colors: ...`). Bumped the tool asset query string to `v=20260513b`, regenerated `assets/css/touch-screen-test.min.css` and `assets/js/touch-screen-test.min.js`, and redeployed only the scoped touch-screen page/assets via SFTP/paramiko. Verification passed locally and live: PHP lint clean, JS syntax clean for source/minified JS, `git diff --check` clean, local/live HTTP 200, exactly 1 H1, no PHP warnings, live page has 4 guide-link occurrences and 5 FAQ schema questions, live minified CSS contains `touch-quick-guide`, live minified JS no longer contains `display check`, mobile Playwright check shows no horizontal overflow at 390px, desktop Playwright color-mode check confirms the duplicate text is removed, the linked blog guide returns HTTP 200 with 1 H1, and IndexNow returned HTTP 200 success with `touch-screen-test.php` included.
- Upgraded and deployed the live English `touch-screen-test.php` from a basic draw/ghost checker into a fuller diagnostic tool inspired by Online Mic Test, XbitLabs, and TouchScreenTest.com patterns. New live capabilities: mode tabs for Multi-touch, Dead zones, Precision, Pressure, and Colors; fullscreen; PNG snapshot; sound/haptic toggles; device/API info; gesture history; dead-zone coverage progress; precision average error; pressure reporting; and ghost watch. Moved the scoped tool CSS/JS into new source and minified assets: `assets/css/touch-screen-test.css`, `assets/css/touch-screen-test.min.css`, `assets/js/touch-screen-test.js`, and `assets/js/touch-screen-test.min.js`. Deployed only these five scoped files via SFTP/paramiko. Verification passed locally and live: PHP lint clean, JS syntax clean for source/minified JS, `git diff --check` clean, local and live HTTP 200, exactly 1 H1, no PHP warnings, `WebApplication`/`FAQPage`/`BreadcrumbList` schema markers present, minified assets HTTP 200, mobile/desktop Playwright screenshots captured, live mobile HAR had 20 entries with 0 failed requests, and IndexNow returned HTTP 200 with `touch-screen-test.php` included.
- Published and deployed `blog/ghost-touch-test-online-touchscreen-dead-zones.php` targeting `ghost touch test`, `ghost touch test online`, `ghost touch detector`, `touch screen test online`, and `touchscreen dead zone test`. Followed the blog-post consistency workflow with fast answer, jump links, active-voice diagnostic workflow, source list, related tools/posts, FAQPage/BlogPosting/BreadcrumbList/VideoObject schema, and verified YouTube embed `Un0w5FdlTrA` from ASUS Support. Created four article-specific WebP assets under `blog/images/ghost-touch-*.webp`, updated `blog/posts-data.php`, regenerated `sitemap.xml` and `sitemap.txt`, and deployed the scoped article/assets/registry/sitemaps via SFTP/paramiko. Verification passed locally and live: article HTTP 200, exactly 1 H1, no PHP warnings, JSON-LD includes `BlogPosting`, `VideoObject`, `FAQPage`, and `BreadcrumbList`, all four live images return HTTP 200 as `image/webp`, blog index card is present, live sitemap contains the new URL, and IndexNow returned HTTP 200 with the new article URL included.

### 2026-05-12 (Codex)
- Published the DEV.to canonical cross-post for the updated best gaming mouse guide from `dev-to-articles/05-best-gaming-mouse-2026.md`. Adapted the content for DEV.to with front matter, four tags, canonical URL back to `https://keyboardtester.click/blog/best-gaming-mouse-2026.php`, absolute product image URLs, a DEV.to YouTube liquid embed, and no PHP/layout markup. Published via `dev-to-publish.py` using the existing `.env` API key. DEV.to article ID `3658055`, public URL `https://dev.to/nasirazizawan/best-gaming-mouse-2026-practical-picks-by-grip-price-and-game-type-2bjg`. Verification passed: API reports `published=True` with the correct canonical URL, and the public page returns HTTP 200 with title, canonical article URL, cover image, YouTube marker, and price markers present.
- Refreshed live `blog/best-gaming-mouse-2026.php` after user feedback that prices were not visible and the images were generic. Reworked the post as the main evergreen buyer guide, added a visible duplicate-intent note distinguishing it from `blog/best-recent-gaming-mouse-launches-2026.php`, replaced generic stock media with eight product-specific WebP images, added a visible official-US-price column and price callouts for each main pick, updated `blog/posts-data.php` to use the product hero, and deployed the scoped article/registry/images via SFTP/paramiko. Verification passed locally and live: article HTTP 200, exactly 1 H1, price column and all checked prices present, duplicate note present, YouTube embed retained, no old stock-image references remain, all eight live images return HTTP 200 as `image/webp`, blog index card uses the new hero, and IndexNow returned HTTP 200 with the URL included.
- Added contextual internal links to `blog/best-gaming-mouse-2026.php` from relevant live pages: `blog/best-mechanical-keyboards-for-gaming-2026.php`, `blog/best-recent-gaming-mouse-launches-2026.php`, `blog/best-recent-gaming-keyboard-launches-2026.php`, `blog/best-recent-wireless-keyboard-launches-2026.php`, `mouse-test.php` through `help/seo-content/mouse-test.php`, `polling-rate-test.php`, and `mouse_sensitivity_DPI_tester.php` through `help/seo-content/mouse-dpi.php`. Deployed only the seven scoped changed files via SFTP/paramiko; did not upload unrelated local dirty changes in `mouse_sensitivity_DPI_tester.php`. Verification passed live: all seven pages return HTTP 200, each has exactly 1 H1, and each renders at least one link to `/blog/best-gaming-mouse-2026.php`; IndexNow returned HTTP 200 with the new article URL included.
- Published and deployed `blog/best-gaming-mouse-2026.php` as a researched buyer guide targeting `best gaming mouse 2026`. Followed the blog-post consistency workflow with fast answer, jump links, comparison table, practical grip/weight/polling-rate advice, Reddit/Quora/Google Trends research framing, source links, FAQPage/BlogPosting/BreadcrumbList/VideoObject schema, related tools, and related posts. Added six stock WebP images under `blog/images/best-gaming-mouse-2026-*.webp`, embedded verified YouTube video `CURKRJSoUMY`, updated `blog/posts-data.php` and `submit-indexnow.php`, regenerated `sitemap.xml` and `sitemap.txt`, and deployed the scoped files via SFTP/paramiko. Verification passed locally and live: article HTTP 200, exactly 1 H1, valid JSON-LD types `BlogPosting`, `BreadcrumbList`, `FAQPage`, `VideoObject`, YouTube/source markers present, blog index card present, all six live images return HTTP 200 as `image/webp`, live sitemap contains the URL, and IndexNow returned HTTP 200 with the new URL included.
- Created and deployed a plain-text sitemap at `https://keyboardtester.click/sitemap.txt` for the user's request to have a sitemap without markup/html tags. Updated `generate-sitemap.php` so future sitemap generation writes both the normal XML sitemap (`sitemap.xml`) and a one-URL-per-line text sitemap (`sitemap.txt`). Updated `.gitignore` to allow tracking `sitemap.txt`, added the text sitemap to `robots.txt`/`robots.php`, and added an `.htaccess` header rule so `sitemap*.txt` serves as `text/plain; charset=utf-8`. Deployed `.htaccess`, `generate-sitemap.php`, `sitemap.xml`, `sitemap.txt`, `robots.txt`, and `robots.php` via SFTP/paramiko. Verification passed: live `sitemap.txt` returns HTTP 200 as `text/plain; charset=utf-8`, contains 869 unique `https://keyboardtester.click/` URLs, has 0 lines with `<` or `>` characters, and live `robots.txt` advertises `Sitemap: https://keyboardtester.click/sitemap.txt`. Live `sitemap.xml` still returns HTTP 200 as XML with 869 URLs and 0 HTML-tag hits.
- Rechecked the user's reported GSC fetch error for `sitemap.txt`. Live fetches using a Googlebot-style user agent passed for `https://keyboardtester.click/sitemap.txt`, `https://keyboardtester.click/sitemap.xml`, and `https://keyboardtester.click/sitemap2.xml`: all return HTTP 200, correct content types, and the `.txt` file has 869 URL lines with 0 `<`/`>` characters. Submitted all three sitemap URLs through the Search Console API under the accessible Domain property `sc-domain:keyboardtester.click`. API accepted each submission and immediate status showed `lastSubmitted=2026-05-12T12:51Z`, `isPending=true`, `warnings=0`, and `errors=0`. The service account only has the Domain property (`sc-domain:keyboardtester.click`); attempts against the URL-prefix property `https://keyboardtester.click/` returned HTTP 403, so future API sitemap submissions should use the Domain property unless permissions change.

### 2026-05-11 (Codex)
- Addressed the GSC mobile Core Web Vitals "Needs improvement" group affecting 103 URLs with INP > 200ms and LCP > 2.5s. Deployed a shared mobile performance patch via SFTP/paramiko: `includes/head-common.php` now defers Bootstrap JS/CSS on mobile, gates Google Font CSS to desktop/tablet media, delays AdSense on mobile, and serves `global.min.css?v=3.0`; `includes/components/adsense-slot.php` skips `adsbygoogle.push()` on mobile; `footer.php` lazy-loads AI chat CSS/JS after initial load instead of loading the widget immediately; `global.css`/`.min.css` hide ad slots on mobile and add `content-visibility` to below-fold sections; homepage suppresses the footer ad and continues to use minified landing CSS. Also patched 88 high-impact remote PHP templates/pages so Google Fonts are desktop/tablet-only and `index-modern.min.css` is used where applicable, without uploading unrelated local dirty edits from those files. Evidence: `seo-audit-2026-05-11/cwv-mobile-high-impact-remote-patch-2026-05-11.json`.
- Live mobile verification passed for homepage, `mouse-test.php`, `keyboard_typing_test.php`, `latency-checker.php`, and `languages/french/mouse-test.php`: each returned one H1, no horizontal overflow, `global.min.css?v=3.0`, visible mobile ad slots `0`, no early `pagead2`, Google Fonts, Bootstrap, or AI chat requests in the first 2.5 seconds, and no failed requests. This should directly target mobile LCP and INP, but GSC/CrUX uses rolling field data so validation will take time after clicking "Start validation".
- Checked the user's GSC robots warning for `Crawl-delay: 1`. Local `robots.txt`/`robots.php` and live `https://keyboardtester.click/robots.txt`, `https://www.keyboardtester.click/robots.txt`, and `http://www.keyboardtester.click/robots.txt` all return HTTP 200 as `text/plain; charset=utf-8` with `crawlDelay=False`; the GSC screenshot is from an older fetched version dated 2026-04-29. No additional robots content change was needed beyond keeping the current no-`Crawl-delay` files deployed.
- Hotfixed the live English/homepage keyboard layout after the desktop keyboard was clipping left/right in production. Root cause was the 2026-05-11 performance/scaling change using viewport-derived fixed width assumptions; the real rendered keyboard layout is ~1536px wide inside a 1400px homepage frame, so no transform was applied and the board overflowed under `overflow:hidden`. Updated `assets/js/keyboard-tester-english.js`, regenerated/deployed `assets/js/keyboard-tester-english.min.js` via SFTP/paramiko, and verified live at 1800px, 1365px, and 1024px viewports after scrolling into the tool: transform scales now apply, and left/right/bottom clipping are all false. Screenshots/evidence saved under `seo-audit-2026-05-11/live-keyboard-*-after-fix-scrolled.png`.
- Updated the blog writer instructions in `BLOG-POST-AGENT.md` and the Claude mirror `.claude/agents/kbt-blog-writer.md`. Future blog blueprints now must explicitly account for semantic coverage, experience-based/reproducible workflow detail, competitor benchmarking, easy readability, native target-language phrasing, grammar/punctuation cleanup, and active voice. This was a docs-only agent update; no live deploy was needed.
- Installed MemPalace `3.3.5` via Python user-site and added `C:\Users\TECH OFFICE\AppData\Roaming\Python\Python314\Scripts` to the user PATH. Initialized this KBT workspace with `mempalace init . --yes --no-llm` after enabling Python UTF-8 mode to avoid a Windows git-author decode failure. Mined 100 safe files into the local palace under wing `kbt`, respecting `.gitignore` and explicitly including `CLAUDE.md`, `seo-audit-2026-04-26/SESSION-TRACKER.md`, and `seo-audit-2026-05-11/low-competition-keyword-research-apm-2026-05-11.md`. Result: 2,587 drawers across rooms; `mempalace search "SFTP paramiko deploy AI coordination" --wing kbt` returned the expected deploy/coordination docs. Registered global Codex MCP server `mempalace` with UTF-8 env flags using `codex mcp add`; a new Codex session should expose the MCP tools. Auto-save hooks were not enabled yet.
- Ran low-competition keyword research for the tools/gaming niche using fresh 90-day Google Search Console data plus SERP/source checks. Selected `League of Legends actions per minute tracker` because the site already had near-page-one impressions on the cluster but little click capture (`apm test`, `action per minute test`, `apm calculator`, `league of legends actions per minute tracker`, `league of legends apm tracker website`). Evidence saved in `seo-audit-2026-05-11/gsc-traffic-2026-05-11-90d.json`, `seo-audit-2026-05-11/gsc-query-page-90d-2026-05-11.json`, and `seo-audit-2026-05-11/low-competition-keyword-research-apm-2026-05-11.md`.
- Published and deployed `blog/league-of-legends-actions-per-minute-tracker-apm-test.php` with four WebP stock-style images, fast answer, jump links, practical first-person APM workflow, Riot/MDN source links, verified League of Legends YouTube embed `uo_lXcsdMKE`, BlogPosting/FAQPage/VideoObject/BreadcrumbList schema, related tools, and related posts. Updated `blog/posts-data.php`, `submit-indexnow.php`, `help/seo-content/apm-test.php`, and regenerated `sitemap.xml`. Deployed scoped files/assets via SFTP/paramiko and verified live article/blog/APM page/sitemap/images. IndexNow returned HTTP 200 and included the new URL.
- Published the canonical DEV.to cross-post from `dev-to-articles/04-league-apm-tracker.md` using `dev-to-publish.py` and the existing `.env` API key. DEV.to article ID `3647518`, public URL `https://dev.to/nasirazizawan/league-of-legends-actions-per-minute-tracker-use-an-apm-test-without-chasing-spam-80p`. Verification passed: DEV.to API list shows PUBLISHED and the public page returns HTTP 200.
- Fixed the latest PageSpeed/Lighthouse findings reported by the user without changing indexed URLs, canonicals, noindex rules, redirects, or sitemap URL sets. Homepage initial HTML now opts out of Google Fonts by setting `$loadInterFont = false`, removed the remaining JetBrains Mono Google Fonts preload, delays Clarity until first interaction or 12s, and delays AdSense/Funding Choices until first interaction or 15s. Live homepage verification returned HTTP 200, no `fonts.googleapis.com` marker in initial HTML, and the expected delayed Clarity/AdSense timers.
- Fixed the reported low-contrast keyboard tester text by updating `.cat-mood`, `.treats-eaten`, and the mobile keyboard tip colors, then scoped the dark overrides to `html.dark-theme` only so the light page is not accidentally affected by the tool's internal `data-theme="dark"` keycap theme. Regenerated `assets/css/keyboard-cat-progress.min.css`; PHP lint and local mobile contrast checks passed.
- Reworked `robots.txt` for the Lighthouse "robots.txt is not valid / unable to download" warning. The live `/robots.txt` path is now served through `robots.php` as `text/plain; charset=utf-8`, with a simplified robots file and both sitemap entries. Removed the remote static `robots.txt` via SFTP so Apache rewrites `/robots.txt` to `robots.php`. Live Lighthouse-style verification passed on the unversioned URL: HTTP 200, 962-byte robots body, starts with `# robots.txt`, includes `Sitemap: https://keyboardtester.click/sitemap.xml`, and has no `Crawl-delay`. HostArmada/Engintron still adds `Cache-Control: max-age=86400` and security headers on the `.txt` URI, but the fetch/body/content type are valid.
- Fixed the mobile header/readability issue reported by the user. In `includes/components/site-chrome.php`, the `KeyboardTester.click` brand no longer shrinks to `0.97rem` on narrow screens, the brand text is nowrap-stabilized, and the hamburger button now stacks its three spans vertically with explicit bar sizing so it renders as three visible lines on the right side.
- Enforced 16px mobile text where the previous CSS still dropped to 15px/14px: updated `assets/css/global.css`/`.min.css` root mobile font sizing and common paragraph/tool-header text, and added a homepage mobile override in `assets/css/index-modern.css`/`.min.css` for hero/body/FAQ/tool text. Updated `includes/head-common.php` to serve `global.min.css?v=2.9`, bumped the service worker registration version to `20260511`, and updated `sw.js` to `kbt-sw-v3` with the new CSS precache URL to reduce stale-cache risk.
- Deployed the mobile header/font-size/cache files via SFTP/paramiko. Live verification passed: `https://keyboardtester.click/` returns the new `global.min.css?v=2.9`, `sw.js?v=20260511` contains `kbt-sw-v3`, live `global.min.css?v=2.9` has `html{font-size:16px}` and no `html{font-size:15px}`/`14px` rules, and a live 390px mobile Chrome render reports `htmlFontSize=16px`, sample text `16px`, brand font about `17.94px`, `toggleFlexDirection=column`, and `barCount=3`.
- Fixed the homepage PageSpeed issues reported by the user: the large `.hero-copy` CLS shift, forced reflow from keyboard geometry reads, and unminified inline keyboard JavaScript. Kept the changes scoped to performance; no indexed URLs, canonicals, sitemap entries, noindex rules, schema, or redirects were changed.
- Removed the above-fold hero entrance animation in `assets/css/index-modern.css`/`.min.css`, which was the reported layout-shift element. Local Lighthouse after the fix reported CLS `0.000049` and the layout-shifts audit only showed a tiny header brand font-load shift, not the hero block.
- Moved the large keyboard initializer and Pro Diagnostics initializer out of inline HTML into cacheable minified assets: `assets/js/keyboard-tester-english.min.js` and `assets/js/keyboard-tester-advanced.min.js`, with source files kept beside them. `tools/keyboard_tester_english.php` and `tools/keyboard-tester-advanced.php` now load those assets with `filemtime()` cache busting.
- Removed early forced-layout reads from keyboard scaling by replacing `scrollWidth`/`offsetWidth`/`clientWidth` measurement with fixed layout dimensions plus lazy `IntersectionObserver`/`requestIdleCallback` scheduling. Also removed `document.fonts.ready` from the scaling path.
- Updated the cat progress enemy movement path so Chrome uses compositor-friendly `style.translate` instead of per-frame `left/top` writes, with the existing `left/top` fallback retained for older browsers. Updated `assets/css/keyboard-cat-progress.css` to use `will-change: translate, transform, opacity`.
- Removed the remaining Pro Diagnostics animation reflow restarts by replacing `void el.offsetWidth` animation resets with Web Animations API pulses plus class fallback.
- Verification passed locally: PHP lint clean for `tools/keyboard_tester_english.php` and `tools/keyboard-tester-advanced.php`; Node syntax checks clean for the new/minified JS assets; `git diff --check` clean for touched files; local rendered homepage contains no `offsetWidth`, `scrollWidth`, `clientWidth`, or `document.fonts.ready`; local Lighthouse after the fix shows no forced reflow items, no unminified JavaScript savings, CLS `0.000049`, and TBT `0 ms`. Evidence saved in `seo-audit-2026-05-11/lighthouse-local-after-perf2.json`.
- Deployed 11 scoped performance files via SFTP/paramiko. Live verification passed with public DNS/IP resolution: homepage HTML references `keyboard-tester-english.min.js` and `keyboard-tester-advanced.min.js`, no longer contains the large inline keyboard initializer, and contains no `offsetWidth`/`scrollWidth`/`clientWidth`/`document.fonts.ready` strings. Live JS/CSS assets return HTTP 200 with expected content types. Live Lighthouse after deploy reported CLS `0`, no forced reflow items, no unminified JavaScript savings, and DOM-size score `1`; LCP/main-thread still vary on production due full page/style/server/third-party work. Evidence saved in `seo-audit-2026-05-11/lighthouse-live-after-perf.json`.
- Completed a live-safe technical SEO hardening pass based on the 8-point checklist. No indexed URL slugs, canonicals, sitemap main URLs, noindex rules, or redirect behavior were changed. Work focused on crawl cleanup, internal discovery, schema gaps, render-blocking font imports, and mobile text wrapping.
- Downloaded and analyzed Apache access logs for April/May 2026 from `/home2/keyboar1/logs/` via SSH/SFTP. Parsed 380,650 lines and 11,014 bot hits: Googlebot had mostly 200/304 responses, with 25 total 404 hits. The only repeated actionable Googlebot 404 was `/flags/japan_flag.svg`, fixed by adding a safe alias file from the existing Japanese flag asset. Old WordPress/blog oddities were already covered by existing 410/redirect handling, so no new risky redirects were added.
- Ran full local sitemap crawl checks before and after the changes. The post-change audit checked 867 sitemap URLs and found 0 non-200 URLs, 0 orphan candidates, 0 visible FAQ pages missing FAQPage schema, and 0 H1 issues. Evidence saved under `seo-audit-2026-05-11/`, including `technical-seo-local-crawl.json`, `technical-seo-post-change-check.json`, and `log-analysis-bots-apr-may-2026.json`.
- Fixed internal discovery without changing existing indexed URLs by adding an English-only "Category guides" internal-link row to `includes/render-all-tools.php`, linking the six category/hub pages that were only weakly discoverable from the crawl: `mouse-and-keyboard-test-tools.php`, `pages/category-keyboard.php`, `pages/category-mouse.php`, `pages/category-audio-video.php`, `pages/category-utilities.php`, and `pages/category-language-keyboards.php`.
- Closed schema gaps on localized keyboard home pages by adding JSON-LD FAQPage output matching visible FAQ content for Spanish, Portuguese, and Japanese keyboard index pages. Kept JSON-LD-only schema style to avoid duplicate microdata/JSON-LD problems.
- Removed embedded Google Fonts `@import` from Spanish, Portuguese, and Japanese localized keyboard tool sections. Moved the needed fonts into async preload/no-script font loading on the parent pages and added mobile wrapping safeguards in `assets/css/index-modern.css` and `assets/css/index-modern.min.css` for hero titles, intro copy, and CTAs.
- Added `Sitemap: https://keyboardtester.click/sitemap2.xml` to `robots.txt` alongside the main sitemap so crawlers can discover the simple URL-only sitemap experiment without replacing `sitemap.xml`.
- Deployed 11 scoped files via SFTP/paramiko to production and verified live with public DNS/IP resolution: `flags/japan_flag.svg` returns HTTP 200 as SVG, `robots.txt` advertises both sitemaps, `/pages/all-tools.php` contains the new category guide links, Spanish/Portuguese/Japanese pages contain FAQPage schema and no embedded font imports, and live CSS contains the mobile text-wrap rules.

### 2026-05-10 (Codex)
- Pulled fresh Google Search Console daily performance data for `sc-domain:keyboardtester.click` covering 2026-02-08 through 2026-05-08 and produced a 90-day traffic forecast for 2026-05-09 through 2026-08-06. Actual trailing 90 days: 1,941 clicks, 164,428 impressions, 1.18% CTR, avg position 11.2. Last 30 days were the breakout period: 1,548 clicks and 137,631 impressions, avg position 9.4. Forecast scenarios: conservative last-30 run-rate 4,644 clicks / 412,893 impressions; linear daily trend 8,745 clicks / 791,583 impressions; aggressive monthly-delta continuation 12,510 clicks / 1,149,645 impressions. Saved evidence to `seo-audit-2026-05-10/gsc-traffic-forecast-2026-05-10.json`.
- Simplified the experimental `sitemap2.xml` to a URL-only sitemap: 10 high-priority canonical URLs with only `<url><loc>...</loc></url>` entries, no `lastmod`, `changefreq`, `priority`, image tags, or other metadata. Deployed only `sitemap2.xml` via SFTP/paramiko. Live verification passed: `https://keyboardtester.click/sitemap2.xml` returns HTTP 200 with `Content-Type: application/xml; charset=utf-8`, parses as valid XML, has 10 `<url>` entries and 10 `<loc>` entries, and contains no `lastmod`/`changefreq`/`priority`. Submitted the exact sitemap URL to GSC via Search Console API; API accepted it with `lastSubmitted=2026-05-10T14:08:54.068Z`, `isPending=true`, `warnings=0`, and `errors=0`. This was a narrow experiment for the known GSC sitemap fetch issue, not a renewed root-cause investigation.
- Created and deployed the new researched blog post `blog/microphone-picking-up-desktop-audio-windows-11.php` targeting `microphone picking up desktop audio windows 11`. The post uses a diagnostic/how-to structure with a fast answer, jump links, Windows 11 input routing workflow, Stereo Mix/Listen-to-this-device checks, Realtek/headset crosstalk section, Discord/OBS/Teams/Zoom guidance, source list, FAQPage schema, BlogPosting schema, BreadcrumbList schema, and VideoObject schema.
- Added four supporting WebP assets under `blog/images/`: `mic-desktop-audio-windows-11-hero.webp`, `mic-desktop-audio-routing-map.webp`, `mic-windows-11-input-settings-check.webp`, and `mic-headset-crosstalk-test-flow.webp`. Added the post to `blog/posts-data.php`, added it to `submit-indexnow.php`, regenerated/deployed `sitemap.xml`, and deployed the scoped article/media/registry/indexing files via SFTP/paramiko.
- Verification passed: PHP lint clean for the new article, `blog/posts-data.php`, and `submit-indexnow.php`; local article HTTP 200 with one H1, BlogPosting/FAQPage/VideoObject schema, four image references, YouTube embed, and mic tool link; local blog index showed the post; live article HTTP 200 with one H1 and expected markers; all four live WebP images returned HTTP 200 as `image/webp`; live blog index showed the post; live sitemap contains the new URL and image references with 867 URLs; IndexNow endpoint returned HTTP 200 and included the new URL.
- Follow-up: the original YouTube embed `ShiMQjipLZk` displayed "Playback on other websites has been disabled by the video owner" on the live article. Replaced it with embeddable YouTube video `vdLglW2CEMM` (`How To Fix Mic Picking Up All PC Sounds (Realtek Manager) (Discord)`), updated the visible iframe plus VideoObject schema, deployed the article via SFTP/paramiko, and verified the live HTML now references `vdLglW2CEMM` and no longer references `ShiMQjipLZk`.
- Published a DEV.to cross-post from `dev-to-articles/03-microphone-desktop-audio-windows-11.md` using the existing `dev-to-publish.py` helper and `.env` `DEVTO_API_KEY`. DEV.to article ID `3644258`, public URL `https://dev.to/nasirazizawan/microphone-picking-up-desktop-audio-on-windows-11-trace-the-signal-path-first-m44`. The DEV.to frontmatter includes canonical URL `https://keyboardtester.click/blog/microphone-picking-up-desktop-audio-windows-11.php` and cover image `https://keyboardtester.click/blog/images/mic-desktop-audio-windows-11-hero.webp`. Verification passed: DEV.to API list shows the post as PUBLISHED, public page HTTP 200, page source contains the title, canonical URL, and original article link.

### 2026-05-09 (Codex)
- Addressed the actionable first-party PageSpeed image delivery/LCP hints from the user's report. Created `assets/images/ai-chat/greeter-avatar-96.webp` (96x96, 1.4 KB) and updated `footer.php` so the AI assistant uses it instead of the 8 KB avatar. Added a `priority` option to `includes/components/microsoft-store-badge.php`; homepage and all-tools hero badges now render `loading="eager"` plus `fetchpriority="high"`, while footer/sitewide badges stay lazy. Deployed `footer.php`, `includes/components/microsoft-store-badge.php`, `help/brief-keyboard-tester.php`, `includes/render-all-tools.php`, and the new avatar via SFTP/paramiko. Verification passed: live avatar asset returns HTTP 200 as `image/webp` at 1436 bytes, homepage HTML uses `greeter-avatar-96.webp?v=1` and no longer references the old avatar path, homepage/all-tools hero badges are eager/high priority, normal tool-page footer badge remains lazy, and live Playwright confirmed the chat avatar natural size is 96x96 with the hero badge priority attributes present.
- Removed the visible AI assistant chat-window border while the greeter/video card is playing. Updated `assets/css/ai-chat.css` so `.kbt-chat-window.kbt-greeter-active` uses a transparent outer border and transparent greeter separator, then bumped `includes/head-common.php` to load `ai-chat.css?v=2.7`. Deployed both files via SFTP/paramiko. Verification passed locally and live: opening the assistant on mobile showed `kbt-greeter-active` with `borderTopColor: rgba(0, 0, 0, 0)` and `greeterBorderBottomColor: rgba(0, 0, 0, 0)`, while the normal chat border remains after the greeter is dismissed.
- Created and deployed a small experimental sitemap at `https://keyboardtester.click/sitemap2.xml` for GSC fetch testing. The file contains exactly 10 high-priority canonical URLs and does not replace the main `sitemap.xml`. Deployed `sitemap2.xml` via SFTP/paramiko. Verification passed: live `sitemap2.xml` returns HTTP 200 with `Content-Type: application/xml; charset=utf-8`, parses as valid XML with 10 `<url>` entries, remote file size is 1914 bytes, and all 10 listed URLs return HTTP 200.
- Fixed the suspected mobile footer ad layout issue by making the shared footer AdSense panel desktop/tablet-only. Updated `includes/components/site-chrome.php` so `.footer-ad-section` is hidden at `max-width:768px`, removing the blank/odd footer ad block on phones while keeping the desktop footer ad active. Deployed via SFTP/paramiko. Verification passed locally and live: homepage, `mouse-test.php`, and Spanish localized homepage returned HTTP 200 with the mobile hide rule present; Playwright mobile checks showed `.footer-ad-section` display `none`, height `0`, and no horizontal overflow; desktop homepage still showed the footer ad as `display:flex`.
- Completed a sitewide mobile compatibility audit for all shared tools. Existing `assets/js/mobile-tool-adapters.js` already handled mobile touch modes for mouse tester, click speed, DPI, polling rate, ghost click, latency, trail, and gamepad guidance. Added a scoped gap-filling layer for uncovered physical mouse/keyboard pages: `includes/components/mobile-tool-alternatives.php`, `assets/css/mobile-tool-alternatives.css`, `assets/js/mobile-tool-alternatives.js`, and `footer.php` wiring. On mobile, uncovered physical mouse pages now load a touch response alternative and hide the desktop-only panel; uncovered physical keyboard pages now load a mobile keyboard alternative and hide the physical-keyboard panel; desktop remains unchanged. Deployed via SFTP/paramiko. Verification passed locally and live: desktop `mouse-lod-test.php` showed no mobile card, live mobile `mouse-test.php` kept the existing adapter only, live mobile `mouse-lod-test.php` and `key-repeat-rate-test.php` showed the new alternatives and hid the original tool, live mobile Spanish keyboard home linked to `/languages/spanish/typing-test.php`, and live mobile `touch-screen-test.php` showed no unnecessary adapter. Audit note saved locally at `seo-audit-2026-05-09/mobile-tool-compatibility-audit-2026-05-09.md`.
- Added the new X profile link (`https://x.com/software15325`) to the shared site footer. Updated `config.php` with `socialLinks['x']`, added the X inline icon/color in `includes/components/site-chrome.php`, and placed the X footer button in the `Community` social group with Facebook/Instagram to avoid lengthening the first footer card. Deployed `config.php` and `includes/components/site-chrome.php` via SFTP/paramiko. Verification passed: PHP lint clean, local homepage HTTP 200 with X URL/icon marker, and live homepage, English mouse-test page, and Spanish localized mouse-test page returned HTTP 200 with the X URL, label, and `social-link-icon--x` marker present.
- Fixed the homepage keyboard theme dropdown so it visibly changes the keycaps, not just the wrapper/control colors. Root cause: the dropdown already set `data-theme` correctly, but homepage-specific CSS in `assets/css/index-modern.min.css` was overriding `.key` with hard-coded black keycap styles. Updated `tools/keyboard_tester_english.php` theme variables/default `data-theme`, changed the homepage keycap override in `assets/css/index-modern.css` to consume `--key-bg`, `--key-text`, `--key-border`, `--key-bottom-shadow`, and related theme variables, regenerated `assets/css/index-modern.min.css`, and deployed the three files via SFTP/paramiko. Verification passed: PHP lint clean, local homepage HTTP 200, local and live browser checks confirmed light/cyberpunk/forest selections update computed keycap background, text, border, shadow, selected value, and `localStorage.keyboardTheme`.
- Local-only AI chat greeter prototype implemented. Converted the user-provided 55 MB GIF into lightweight local assets under `assets/images/ai-chat/`, then replaced that first assistant with the user's newer Grok MP4 assistant. Current assets are `greeter-avatar.webp`, `greeter-poster.webp`, and compact cropped `greeter-emily.mp4` (~765 KB). The floating chat trigger uses the human avatar with online dot, and the open-state greeter card plays the MP4 with online pill, greeting copy, and `Chat now` action while keeping the existing Groq chat backend/session flow intact. Follow-up refined the first-open flow so only the greeter card is visible before `Chat now`; messages, chips, and input stay hidden until clicked, then chat starts and focuses `#kbtChatInput`. The assistant is named `Emily` in the header, greeter title, and English welcome message. The optional name prompt and `Skip` chip were removed completely from the local chat flow and source strings. Updated `footer.php` asset wiring, `assets/js/ai-chat.js`, regenerated `assets/js/ai-chat.min.js`, and bumped chat CSS/JS cache versions. Verification passed locally: `footer.php` and `includes/head-common.php` PHP lint clean, `http://localhost/kbt/` HTTP 200, desktop Chrome headless check confirmed avatar opens the card, the MP4 source plays as `greeter-emily.mp4`, `Chat now` hides the greeter and focuses `#kbtChatInput`, the old name prompt is absent after chat start, and the chat z-index sits above the site sidebar FAB. No live deployment performed.
- Rechecked the user's 16 "Crawled - currently not indexed" URLs through the Google Search Console URL Inspection API. Fresh result: 10 URLs were already `Submitted and indexed`; only 6 remained `Crawled - currently not indexed`: French mouse test, French latency checker, German/French/Portuguese click-speed pages, and Japanese QR generator. All 6 had `ALLOWED`, `INDEXING_ALLOWED`, `SUCCESSFUL`, mobile crawl, and self canonicals, confirming this was a value/differentiation issue rather than a technical block.
- Strengthened the 6 remaining problem pages with localized support content and structured data: added WebApplication + BreadcrumbList schema to all 6, expanded Japanese QR FAQ/schema and visible QR guidance, added French mouse symptom diagnostics, French latency browser/input-lag explanations, and localized CPS interpretation/troubleshooting sections for German/French/Portuguese click-speed pages. Expanded related-tool differentiation in `includes/seo-clusters.php` so mouse and screen pages cross-link the closest specialized variants.
- Cleaned up structured data while fixing the indexing set: removed the stale free-tool `offers` block from shared WebApplication schema, added a dedicated `monitor_color_test` schema/FAQ for `color-test.php`, switched `color-test.php` away from the generic `screen_tester` schema, and removed the manual `offers` block from `refresh-rate-test.php`.
- Verification/deploy passed: PHP lint clean for touched files, `git diff --check` clean, local render audit showed HTTP 200/one H1/schema/no offers/new support sections, full non-blog sitemap duplicate audit checked 828 URLs with 0 duplicate titles/descriptions/canonicals/exact text groups, deployed 11 files via SFTP/paramiko, live verification passed on the 6 remaining GSC pages plus `color-test.php`, `refresh-rate-test.php`, and `sitemap.xml`, Google Indexing API accepted the 6 remaining URLs, and IndexNow returned HTTP 200 with "URLs submitted successfully". Local evidence saved in `seo-audit-2026-05-09/gsc-crawled-not-indexed-study-2026-05-09.md` and `local-duplicate-meta-audit-2026-05-09.json`.
- Added the live Microsoft Store listing link (`https://apps.microsoft.com/detail/9nxdnslwdq2s?hl=en-us&gl=US`) in all recommended site locations after the app went live: homepage hero CTA/badge via `help/brief-keyboard-tester.php`, shared footer social links via `includes/components/site-chrome.php`, `about-us.php`, and `resources.php`. Centralized the URL in `config.php` as `$microsoftStoreUrl` and `socialLinks['microsoft_store']`.
- Published a short launch article at `blog/keyboardtester-click-microsoft-store-windows-app.php` with real Store/PWA screenshot asset `blog/images/keyboardtester-microsoft-store-windows-app.png`, BlogPosting/FAQPage/BreadcrumbList/VideoObject schema, Microsoft Store CTA, related tools, and source links. Added the post to `blog/posts-data.php`, added the URL to `submit-indexnow.php`, regenerated/deployed `sitemap.xml`, and deployed all changed public files via SFTP/paramiko.
- Verification passed: PHP lint clean for changed PHP files, local HTTP 200 checks passed for homepage/about/resources/blog article/blog index/image, live HTTP 200 checks passed for the same pages/assets, live homepage/about/resources/footer/article all contain the Microsoft Store URL, the live article has one H1 and required schema markers, live sitemap contains the new article and 866 URLs, and IndexNow returned HTTP 200 with the new article URL included.
- Follow-up cache fix for users not seeing the homepage CTA: live no-query homepage source already contained `Get Windows app`, but the PWA service worker script was still being served from long static cache at the bare `sw.js` URL. Updated `sw.js` to `kbt-sw-v2`, removed `/` from precache, and changed `includes/head-common.php` to register `sw.js?v=20260509` so returning browsers fetch the new worker. Deployed `sw.js`, `.htaccess`, and `includes/head-common.php`; live verification confirmed the homepage contains `sw.js?v=20260509`, the Store CTA, and the versioned service worker returns `kbt-sw-v2`.
- Replaced the plain Microsoft Store CTA with an official badge component. Downloaded the official English dark/light Microsoft Store badge SVGs from Microsoft's badge image endpoint into `assets/images/microsoft-store-badge-en-us-dark.svg` and `assets/images/microsoft-store-badge-en-us-light.svg`, added reusable renderer `includes/components/microsoft-store-badge.php`, and used it on the homepage hero, `about-us.php`, `resources.php`, and the Microsoft Store launch blog post. Deployed 7 files via SFTP/paramiko and submitted IndexNow. Verification passed: PHP lint clean for changed PHP files, local and live homepage/about/resources/blog checks returned HTTP 200 with badge assets and Store URL present, the homepage no longer contains the plain `Get Windows app` text button, and both live SVG badge assets serve as `image/svg+xml`.
- Follow-up contrast fix on the Microsoft Store launch blog post: the dark Store callout panel text could render too low-contrast in the live page. Tightened the inline CSS to `.blog-content .store-panel p { color:#f8fafc!important; font-weight:650; }`, deployed only `blog/keyboardtester-click-microsoft-store-windows-app.php`, and verified live HTTP 200, one H1, badge present, and the fixed CSS rule present.
- Moved the homepage Microsoft Store badge from the left hero CTA stack into the right-side hero blank area below the diagnostics cards, added the official badge to the all-tools hero through `includes/render-all-tools.php`, and added a slim sitewide Microsoft Store CTA banner above the shared footer through `footer.php` and `includes/components/microsoft-store-badge.php`. Deployed the changed shared files and badge SVGs via SFTP/paramiko. Verification passed: PHP lint clean, local and live homepage/all-tools/tool/localized/blog pages returned HTTP 200 with the expected Store URL/banner markers, live badge SVGs return `image/svg+xml`, live desktop screenshot confirmed the badge appears in the pointed homepage area, and IndexNow returned HTTP 200 with `URLs submitted successfully`.
- Added the new Bluesky profile link (`https://bsky.app/profile/keyboard-tester.bsky.social`) to the shared footer social links. Updated `config.php` with `socialLinks['bluesky']`, added a Bluesky inline footer icon/color and conditional footer link in `includes/components/site-chrome.php`, deployed both files via SFTP/paramiko, and verified live homepage, `mouse-test.php`, and Spanish mouse-test page all return HTTP 200 with the Bluesky URL, label, and icon class present.
- Rebalanced the shared footer after the first footer card became too long from social links. Updated `includes/components/site-chrome.php` so the first card keeps only GitHub/GitLab, the third `Testing Tools` card includes a `Community` mini social group for YouTube/Facebook/Instagram, and the fourth `More Tools` card includes an `App & Updates` mini social group for Bluesky/Microsoft Store. Deployed via SFTP/paramiko. Verification passed: PHP lint clean, local and live homepage/English tool/Spanish localized pages returned HTTP 200 with `Community`, `App & Updates`, Bluesky, and Microsoft Store markers present; live browser check confirmed the first four footer cards now distribute links as 2/5/11/11.
- Corrected the footer rebalancing to preserve equal-height cards while removing the large blank area inside the first two cards. Kept `.footer-container` equal-height stretching, moved YouTube into the first footer card with GitHub/GitLab/Bluesky/Microsoft Store, kept Facebook/Instagram under a `Community` mini group in the Quick Links card, and left Testing/More Tools focused on tool links. Deployed `includes/components/site-chrome.php` via SFTP/paramiko. Verification passed: PHP lint clean, live homepage/English tool/Spanish localized pages returned HTTP 200 with all social markers and equal-height CSS present, and live browser measurement showed the first four footer cards equal at 432px with bottom gaps about 1/1/57/20px.

### 2026-05-08 (Codex)
- Fixed the Portuguese homepage keyboard layout Shift issue. `languages/portuguese/sections/tool.php` had an ISO extra key (`IntlBackslash`, showing `< >`) between left Shift and Z, but left Shift still used the long ANSI `key-2-25x` width. Changed Portuguese left Shift to `key-1-25x`, deployed the single section file via SFTP/paramiko, and verified live `https://keyboardtester.click/languages/portuguese/`: HTTP 200, deployed HTML has `key key-1-25x` for `ShiftLeft`, the old `key-2-25x` ShiftLeft is absent, the `< >` key remains present, Playwright measured the live left Shift as the short ISO key, and there is no horizontal overflow.
- Fixed the non-Latin localized APM text corruption introduced during the APM upgrade. Arabic, Russian, Japanese, and Korean APM landing pages plus their localized tool wrappers contained literal `???` replacement text while the shared site chrome rendered normally. Replaced the corrupted labels with proper UTF-8 translations in `languages/{arabic,russian,japanese,korean}/apm-test.php` and `languages/{arabic,russian,japanese,korean}/tools/apm-test-tool.php`, deployed the 8 files via SFTP/paramiko, and submitted IndexNow. Verification passed: source scan found no `???` in any localized APM page/tool wrapper, PHP lint clean for the 8 fixed files, `git diff --check` clean, local affected pages returned HTTP 200 with one H1 and APM markers, and live affected pages returned `charset=UTF-8`, no `???`, and localized hero/tool text in the HTML.
- Upgraded and deployed the APM Test after comparing against XbitLabs' richer benchmark. Rebuilt `tools/apm_test_tool.php` from a simple counter into a grid/keyboard benchmark with 3x3/4x4/5x5 modes, keyboard mode, duration presets plus custom duration, useful-hit scoring, accuracy/miss tracking, live/average/peak APM, local recent history, personal bests, skill badges, chart, tile-size and sound controls, and copyable results. Updated `apm-test.php`, APM help/SEO copy, `includes/schema.php`, all 8 localized APM landing pages, and all 8 localized tool wrappers so English plus localized pages use the shared upgraded tester. Regenerated/deployed `sitemap.xml` and deployed 23 files via SFTP/paramiko. Verification passed: PHP lint clean for changed PHP files, inline JS `node --check` clean, `git diff --check` clean, local pages rendered with one H1 and APM markers, live English plus all localized APM pages returned HTTP 200 with one H1 and upgraded markers, live Playwright interaction confirmed grid hits and keyboard hits update on production, live sitemap contains the English/Spanish/Korean APM URLs, and IndexNow returned HTTP 200 with "URLs submitted successfully". Note for future regeneration: the localized APM files were updated directly; update the generator translation source before regenerating these wrappers.
- Resubmitted the canonical sitemap `https://keyboardtester.click/sitemap.xml` to Google Search Console via the Search Console API using the existing service account. API response succeeded; immediate sitemap info showed `lastSubmitted=2026-05-08T07:37:08.364Z`, `isPending=true`, `isSitemapsIndex=false`, `warnings=0`, and `errors=0`. Live `sitemap.xml` still returned HTTP 200 with `Content-Type: application/xml; charset=utf-8` before submission. This was only a retry, not a new investigation of the known GSC "Couldn't fetch" issue.
- Replaced the shared footer YouTube Shorts promo panel with an in-footer Google AdSense panel. Removed the old `site_before_footer` placement from `footer.php` and `includes/adsense-slots.php`, added `site_footer_panel`, removed the footer YouTube facade markup/CSS/JS from `includes/components/site-chrome.php`, and let the homepage render the new footer ad by removing its suppress flag. Because all localized footer files include the shared footer, the change applies to English plus all 8 localized footers. Deployed `index.php`, `footer.php`, `includes/adsense-slots.php`, and `includes/components/site-chrome.php` via SFTP/paramiko. Verification passed locally and live on homepage, Spanish, Arabic, Japanese, and `latency-checker.php`: HTTP 200, `footer-ad-section` and `site_footer_panel` present, old YouTube footer strings absent, and `site_before_footer` absent.
- Corrected the YouTube Shorts upload after the first OAuth flow selected the personal `@nasirazizawan` channel. Re-authorized the intended KeyboardTester channel (`UCwZ16zCK4uA_y4G71GROCuw`, `@KeyboardTester-dot-click`) and uploaded 9 public Shorts there. Correct-channel video IDs: `Wc8zr1yzfqU`, `CJvZPthJhqE`, `vC20A2kO690`, `-iEy1Frvq34`, `R-F_c9ANcv4`, `IzZdKqYr5IU`, `LXGwMyMfCT0`, `1HzxYeo7Wqc`, `fXk2evuR5iQ`. Each video has a unique SEO-focused title, tags, and updated description with 8 contextual KeyboardTester.click links. Public oEmbed verification and authenticated API verification passed for all 9 correct-channel videos. Upload, preview, and description-update logs are in `seo-audit-2026-05-08/youtube-upload-previews/`. The original wrong-channel token was renamed to `youtube-oauth-token-wrong-channel-legacy.json` so it is not reused accidentally; those first wrong-channel uploads still need separate cleanup from the personal channel if desired.

### 2026-05-06 (Codex)
- Standardized all 36 blog article pages on the new blog-post template without removing existing article information. Added the shared component `includes/components/blog-template-enhancements.php` and applied additive BlogPosting support sections across the blog: fast-answer/jump-link support where needed, quick action checklists, related tool cards, FAQ blocks where missing, BreadcrumbList/VideoObject/FAQPage schema support, and May 6, 2026 updated-date markers. Replaced the invalid dead-pixel YouTube ID with the relevant color-test video ID (`Q6zYolmmhwI`) while preserving existing valid embeds/facades. Updated all 36 entries in `blog/posts-data.php`, regenerated `sitemap.xml`, deployed all 36 ignored article files plus the shared component, `blog/posts-data.php`, and `sitemap.xml` via SFTP/paramiko. Verification passed: PHP lint clean for 36 articles, component, and posts registry; local HTTP render audit passed for 36 articles with one H1, YouTube/video presence, VideoObject, FAQPage, BreadcrumbList, updated date, and template markers; local Playwright smoke passed on desktop/mobile for representative articles; live render audit passed for all 36 article URLs; live sitemap contains 36 unique blog URLs; live blog listing pages show the May 6 update metadata; IndexNow returned HTTP 200 with "URLs submitted successfully".
- Refreshed and deployed `blog/how-to-test-keyboard-online.php` using the new blog-post consistency workflow. Updated title/meta/H1 to `How to Test a Keyboard Online: Keys, Ghosting, NKRO and Latency`, added BlogPosting + FAQPage + BreadcrumbList + VideoObject schema, added a verified YouTube embed from the user's own channel (`ecmuKN0NJTI`), expanded the article with a fast answer, jump links, quick checklist, diagnostic cards, symptom table, browser-limit table, laptop/mobile/Bluetooth guidance, fixes by result, source notes, six related tool cards, related guides, and visible FAQ. Updated `blog/posts-data.php`, regenerated/deployed `sitemap.xml`, and submitted IndexNow. Verification passed: PHP lint clean, local HTTP 200 with one H1/schema/video/tool cards, local desktop/mobile headless browser checks without horizontal overflow, SFTP/paramiko deployment completed, live HTTP 200 with one H1/VideoObject/FAQPage/BlogPosting/YouTube embed, live headless browser verified hero image and iframe, live image assets HTTP 200, `/blog/?page=4` card updated, live sitemap contains the article lastmod `2026-05-06` plus four image entries, and IndexNow returned HTTP 200 with the article URL included.
- Tightened the blog-post consistency workflow so every future blog blueprint includes `video_embed` as a required parameter. Updated `BLOG-POST-AGENT.md`, `AGENTS.md`, and the local `.claude/agents/kbt-blog-writer.md` prompt so each blog post should include one verified relevant YouTube embed with `VideoObject` schema; weak filler videos should be rejected and rare exceptions documented in the blueprint.
- Added a reusable blog-post consistency workflow for future article creation/updates. New tracked guide: `BLOG-POST-AGENT.md`, which defines the `kbt-blog-post-consistency` blueprint parameters (`image_count`, fast answer, jump links, quick tips, FAQ pattern, info cards/tables, related tools/posts, source checks, deploy and verification plan). Updated `AGENTS.md` so future blog requests must read this spec and invoke/simulate the specialist before editing. Also refreshed the local ignored Claude agent prompt at `.claude/agents/kbt-blog-writer.md` to mirror the current static-PHP/SFTP workflow and replaced the stale FTP/WordPress-era guidance.
- Rebuilt and deployed `blog/input-latency-checker-keyboard-mouse-delay.php` into a deeper gamer/power-user guide. Updated title/meta/H1/schema to `Input Latency Checker: Keyboard and Mouse Delay Guide for Gamers`, added BlogPosting + FAQPage + BreadcrumbList schema, rewrote the article around the full input pipeline, browser measurement limits, polling-rate math, jitter interpretation, gamer testing workflow, power-user dock/KVM/remote-session issues, practical fixes, source links (NVIDIA, RTINGS, Microsoft Raw Input, MDN Event.timeStamp, Blur Busters), and related-tool cards for latency, polling, mouse, reaction time, frame skipping, and CPU stress tests.
- Added/deployed relevant stock-style WebP assets for the article: `blog/images/input-latency-gaming-setup-hero.webp`, `blog/images/input-latency-keyboard-polling-workbench.webp`, and `blog/images/input-latency-mouse-response-pad.webp`. Updated `blog/posts-data.php`, regenerated/deployed `sitemap.xml` with the article lastmod `2026-05-06`, and submitted IndexNow. Verification passed: PHP lint clean for article and posts registry, local HTTP 200 with one H1/BlogPosting/FAQPage/source links/new images, local and live headless browser checks with hero image loaded and six tool cards, live image assets HTTP 200 as `image/webp`, live `/blog/?page=3` card updated, live sitemap contains 865 URLs and article lastmod `2026-05-06`, and live IndexNow returned HTTP 200 with the article URL included.
- Upgraded and deployed `latency-checker.php` for the high-performing latency page. Updated the H1/title/meta/schema/help copy to `Keyboard Latency Test and Mouse Input Delay Checker`, added a large live "Last input" panel for the pressed key/button, added Keyboard and Mouse click modes, target sample counts, jitter, consistency labels, progress, and a compact recent-sample graph in `tools/latency_checker_tool.php`.
- Added quick related input checks inside the latency tool for Mouse Click Latency, Mouse Tester, Mouse Polling Rate, and Reaction Time, plus a dedicated `Related Mouse and Input Latency Tools` section using `includes/related-tools.php`. Updated `meta-config.php`, `seo-config.php`, `includes/schema.php`, help files, and regenerated/deployed `sitemap.xml` with `latency-checker.php` lastmod `2026-05-06`.
- Verification passed: PHP lint clean for changed PHP files, inline JS parse check clean, `git diff --check` clean, local HTTP 200 with one H1/one FAQPage schema/new markers present, local headless Chrome interaction verified key `R` and mouse `Left click`, SFTP/paramiko deployment completed, live HTTP 200 confirmed updated title/H1/last-input/mouse-mode/jitter/related markers, live sitemap includes 865 URLs and latency lastmod `2026-05-06`, live IndexNow returned HTTP 200 with the latency URL included, and live headless Chrome interaction verified keyboard and mouse modes after page load.

### 2026-05-05 (Codex)
- Follow-up wording adjustment for `ghost-click-detector.php`: changed the primary visible phrase and metadata to `Free Mouse Ghost Click Detector` per search-term targeting. Deployed `ghost-click-detector.php`, `help/brief-ghost-click-detector.php`, `tools/ghost_click_detector_tool.php`, `meta-config.php`, `seo-config.php`, and `includes/schema.php`; live verification passed with HTTP 200, title `Free Mouse Ghost Click Detector - Double Click Test Online | KeyboardTester.click`, H1/stage heading `Free Mouse Ghost Click Detector`, and one FAQPage schema.
- Follow-up to the ghost click CTR update: simplified the visible page H1 and tool-stage heading to `Ghost Click Detector` so the searched term stays primary and the top of the page does not mix "ghost click" and "double click" as competing product names. Kept double-click/switch-bounce language in supporting copy for user education. Deployed `ghost-click-detector.php`, `help/brief-ghost-click-detector.php`, and `tools/ghost_click_detector_tool.php`; live verification passed with HTTP 200, H1 `Ghost Click Detector`, stage heading `Ghost Click Detector`, and old mixed header absent.
- Upgraded and deployed `ghost-click-detector.php` for CTR and search-intent alignment. Rebuilt `tools/ghost_click_detector_tool.php` from a basic hardcoded click counter into a diagnostic mouse double-click/ghost-click tester with left/right/middle filters, adjustable 80/150/250/300 ms thresholds, target sample size, pause/reset, suspicious rate, fastest/average interval stats, per-event log, and TXT report export. Rewrote hero/stage copy, benefit cards, quick guide, and `help/seo-content/ghost-click.php` around "mouse double click test", switch bounce, and mouse chatter; reduced the visible FAQ to one focused section. Updated `meta-config.php`, `seo-config.php`, `includes/schema.php`, and `sitemap.xml`. Verification passed: PHP lint clean for changed PHP files, inline JS `node --check` clean, `git diff --check` clean, local and live `ghost-click-detector.php` HTTP 200 with one H1, one FAQPage schema, new title/tool markers present, old generic copy absent, and live sitemap lastmod `2026-05-05`. Submitted `https://keyboardtester.click/ghost-click-detector.php` via Bing Webmaster API (1/1 accepted) and ran live IndexNow (HTTP 200, 859 URLs).
- Deployed scoped CTR and discovery updates for `latency-checker.php` and `mouse_sensitivity_DPI_tester.php`. Updated the latency page title/meta/hero/stage/help copy around "keyboard MS", "input delay", and "response time"; updated the DPI page title/meta/hero/stage/help copy around "DPI analyzer", "mouse DPI checker", and eDPI. Added a contextual homepage SEO-content link to `blog/keyboard-switch-types-gaming-guide.php` and a contextual DPI-page link to `scroll-wheel-test.php`. Regenerated/deployed `sitemap.xml`; live verification passed for latency, DPI, homepage, and sitemap lastmod markers. Submitted the four affected discovery URLs (`latency-checker.php`, `mouse_sensitivity_DPI_tester.php`, `scroll-wheel-test.php`, and `blog/keyboard-switch-types-gaming-guide.php`) through Bing Webmaster API (4/4 accepted) and IndexNow (HTTP 200). Note: Google URL Inspection API remains read-only and the Google Indexing API is officially limited to JobPosting/livestream video URLs, so normal tool/blog pages still need manual GSC URL Inspection "Request indexing" if the user wants to force a request.

### 2026-05-04 (Codex)
- Fixed the English guide/help anomaly across localized tool pages. Added `includes/components/localized-tool-support.php` to render language-matched usage guidance, privacy notes, tips, and translated related-tool cards from the shared tool registry. Replaced the old English `help/seo-content`, `related-tools.php`, and `help` includes across 184 localized wrappers (23 tools x 8 locales) and updated `scripts/gen-localized-wrappers.py` so future wrappers use the localized support component. Deployed 185 live PHP files via SFTP/paramiko. Verification passed: PHP lint clean for 185 files, `git diff --check` clean, source scan found zero remaining localized wrapper references to the old English help/related includes, local render check passed for all 184 affected URLs, and live sweep passed for all 184 affected URLs with HTTP 200, localized support present, and no old English guide/related strings.
- Updated and deployed the Online Ruler calibration/vertical layout fix. `tools/online_ruler_tool.php` now renders the credit-card calibration overlay with a realistic card aspect ratio (`85.6 x 54`), chip, contactless mark, card-brand text, and network circles. Vertical mode no longer rotates the horizontal SVG; it draws a true vertical SVG and expands the ruler stage height so the full vertical ruler is visible instead of clipped. Verification passed: PHP lint clean for `tools/online_ruler_tool.php` and `online-ruler.php`, inline JS `node --check` clean, local Chrome CDP check confirmed card `324 x 204`, vertical SVG `110 x 1133.86`, and stage min-height `1174px`, SFTP/paramiko deployment completed, and live page HTTP 200 contained the new markers with the old `rotate(90deg) translateY(-100%)` transform absent.
- Published a DEV.to article for the online ruler: `Why I Built an Online Ruler That Needs Calibration` at `https://dev.to/nasirazizawan/why-i-built-an-online-ruler-that-needs-calibration-48kj`. Draft saved at `dev-to-articles/02-online-ruler-screen-measurements.md`. The post intentionally contains only one visible URL, pointing to `https://keyboardtester.click/online-ruler.php`; verification passed via DEV.to API/public page HTTP 200 and link count check.
- Follow-up PageSpeed forced-reflow fix for the homepage keyboard progress strip. Updated `assets/js/keyboard-cat-progress.js` so initialization no longer calls `getBoundingClientRect()` or any other synchronous geometry read; it now seeds track dimensions from viewport estimates and lets `ResizeObserver` supply the real track size asynchronously, scaling ghost positions after observer updates. Regenerated `assets/js/keyboard-cat-progress.min.js` and deployed both JS files via SFTP/paramiko. Verification passed: `node --check` clean for source/minified JS, `git diff --check` clean, local homepage HTTP 200 with one H1/progress strip present, local and live minified progress asset HTTP 200 with `estimateTrackWidth`/`ResizeObserver` markers and no `getBoundingClientRect`, `offsetWidth`, `clientWidth`, or `scrollWidth` strings.
- Added and deployed a protected support email reveal flow. Replaced the visible support email and direct mailto links in the site footer, About, Privacy, Disclaimer, Feedback error state, llms.txt, AI chat prompt, maintenance page, and public project docs. Added `includes/components/protected-email.php`, which renders a click-to-reveal button and a lightweight math human-check modal before creating the visible email/mailto link in the browser. Split the server-side email literal in `config.php`, `api/send-feedback.php`, and `send_feedback.php`, and changed Organization JSON-LD contactPoint from raw email to the feedback URL. Deployed 16 files via SFTP/paramiko. Verification passed: PHP lint clean for touched PHP files, `git diff --check` clean, active source search has no raw support email or direct support mailto outside old ignored audit snapshots, local and live HTTP checks show protected reveal/check markers where expected and no raw support email or raw mailto in rendered responses.
- Added visible published and last-updated dates across the blog. `blog/posts-data.php` now stores an `updated` field for all 36 posts, matching each article's `dateModified`; `/blog/` cards and homepage latest-blog cards now render both Published and Updated dates with `<time datetime="YYYY-MM-DD">` markup. Normalized all 36 article pages so the visible `.post-meta` line includes Published and Last updated `<time>` elements while preserving existing JSON-LD `datePublished`/`dateModified`. Updated `blog/generate-blog.php` so future generated articles inherit the same visible date pattern. Deployed 40 files via SFTP/paramiko, including ignored article files. Verification passed: PHP lint clean for 40 files, `git diff --check` clean, local and live blog index/homepage/article checks show published/updated time markup, sampled live articles preserve matching JSON-LD dates, and 37 blog URLs were submitted to IndexNow with HTTP 200.
- Reduced first-party homepage PageSpeed findings for the hero video facade and keyboard progress strip. Recompressed `images/yt-thumbs/3d9SyUUlz1Y.webp` from 27.9 KB to 16.9 KB at the same 480x270 dimensions. Updated `assets/js/keyboard-cat-progress.js` so the arcade progress animation caches track geometry, observes resize changes, and computes Pac-Man/ghost/treat collision and sparkle positions from animation state instead of repeated per-frame layout reads; regenerated `assets/js/keyboard-cat-progress.min.js`. Deployed all three assets via SFTP/paramiko. Verification passed: `node --check` clean for source and minified JS, `git diff --check` clean, local homepage HTTP 200 with hero thumb/script/progress strip present, local thumb HTTP 200 as `image/webp` with 16,854-byte length, live thumb HTTP 200 as `image/webp` with 16,854-byte length, live homepage HTTP 200, and live minified JS contains the cached geometry markers.
- Updated and deployed the hero image for `blog/keyboard-shortcuts-windows-mac-linux-complete-cheat-sheet.php`. Replaced the old keyboard-test heatmap hero with a more relevant stock-photo-based WebP focused on Control, Option, and Command modifier keys (`blog/images/keyboard-shortcuts-modifier-keys-hero.webp`, 1200x690). Updated the article hero image, Open Graph image, Article schema `image`, `dateModified=2026-05-04`, and the tracked blog-card image/alt text in `blog/posts-data.php`. Deployed the ignored article and image plus tracked post data via SFTP/paramiko. Verification passed: PHP lint clean, local article HTTP 200 with one H1/new image/no old image/schema image, local image HTTP 200 as `image/webp`, live article HTTP 200 with new image/no old image/schema image/dateModified, live image HTTP 200 as `image/webp`, `/blog/` card references the new image/alt, and IndexNow returned HTTP 200 success with the shortcuts URL included.
- Updated and deployed the homepage trust-strip layout. Changed the five benefit cards under the keyboard tester into a fixed two-column grid on desktop, added a right-side rectangular AdSense block using new placement `home_trust_side`, and kept tablet/mobile behavior responsive with the ad stacking below the cards. Updated `index.php`, `assets/css/index-modern.css`, `assets/css/index-modern.min.css`, and `includes/adsense-slots.php`. Verification passed: PHP lint clean, `git diff --check` clean, local homepage HTTP 200 with five trust cards/new ad slot/no `Advertisement` text, SFTP/paramiko deployment completed, live homepage HTTP 200 with `trust-layout` and `home_trust_side`, and live UTF-8 stylesheet contains the new trust layout/ad rules.
- Fixed and deployed `pacman-game.php` gameplay/contrast update. Updated `assets/js/pacman-game.js` so ghost/Pac-Man collisions use a sprite-sized collision radius plus piecewise swept movement segments across turns and tunnel wrapping; this prevents ghosts from visually crossing Pac-Man without a catch. Updated `assets/css/pacman-game.css` with stronger dark background treatment, brighter hero/HUD/status text, stronger heading shadow, clearer button borders/backgrounds, and visible focus rings. Verification passed: `node --check` for JS, PHP lint for `pacman-game.php`, local HTTP 200 with one H1, targeted collision harness passed visual-overlap/corner-crossing/tunnel-wrap/non-overlap cases, computed contrast pairs were all 14:1 or higher, SFTP/paramiko deployment completed, and live checks confirmed the page HTTP 200 plus updated JS/CSS markers.
- Removed the visible "Advertisement" text overlay from sitewide ad boxes. Updated `includes/components/adsense-slot.php` so normal AdSense slots no longer render a `.kbt-ad-label` span and use `aria-label="Sponsored slot"` instead. Updated `includes/components/blog-article-ads.php` so blog article side rails no longer render the label span and no longer include the visible word in ARIA. Deployed both shared components via SFTP/paramiko. Verification passed locally and live on homepage, `/blog/`, and `blog/keyboard-switch-types-gaming-guide.php`: HTTP 200, no `Advertisement` string in rendered HTML, zero `.kbt-ad-label` spans, and sponsored-slot ARIA present.
- Updated the live `blog/keyboard-switch-types-gaming-guide.php` article to remove the visible "SEO and Search Intent Study Behind This Guide" section and the matching jump link, keeping the keyword/meta/schema optimization in the page head only. Deployed the ignored article via SFTP/paramiko. Verification passed locally and live: PHP lint clean, article HTTP 200, one H1, visible SEO heading removed, `#seo-research` jump removed, YouTube embed still present, FAQ schema still present, and IndexNow endpoint returned HTTP 200 success.
- Published `blog/keyboard-switch-types-gaming-guide.php` live as an in-depth gamer-focused keyboard switch type guide. Added SEO title/meta/keywords, BlogPosting + FAQPage + VideoObject schema, a relevant YouTube switch explainer embed, Reddit/search-intent notes, RTINGS/Corsair/SteelSeries/Newzoo/Steam source links, switch pros/cons tables, budget recommendations, testing workflow links, and a 10-game switch recommendation matrix. Created four custom explanatory WebP diagrams: hero switch-type overview, force-curve chart, Hall Effect sensor diagram, and game recommendation matrix. Updated `blog/posts-data.php`, regenerated/deployed `sitemap.xml`, submitted IndexNow, and verified production article HTTP 200, one H1, canonical/schema/video/source coverage, four image assets as `image/webp`, `/blog/` card visibility, and sitemap article/image entries. Note: the article and image assets live under ignored `blog/` paths and were deployed directly via SFTP/paramiko.
- Follow-up CTR/content pass deployed for `blog/best-mechanical-keyboards-for-gaming-2026.php`. Added Wooting 60HE v2 as the seventh contender with a dedicated 60% section, official Wooting product image, comparison-table row, FAQ coverage, and source links to Wooting, PC Gamer, and RTINGS. Reconfirmed the full article image set as product-specific images (Gamakay NS68, Keychron Q1 HE 8K, Wooting 80HE, Wooting 60HE v2, NuPhy Field75 HE V2, Razer Huntsman V3 Pro TKL, ASUS ROG Azoth 96 HE), not generic keyboard filler. Added the new image to BlogPosting structured data and kept BreadcrumbList markup for Search Console enhancement eligibility.
- Live CTR refresh deployed for `blog/best-mechanical-keyboards-for-gaming-2026.php`. Updated the ignored article and tracked `blog/posts-data.php` so the SERP-facing title/H1/card now target the stronger GSC query cluster (`best gaming keyboard 2026`, budget, Hall Effect, full-size, Reddit). Added an answer-first quick-picks block, a dedicated full-size section, and FAQ entries for budget mechanical, Hall Effect, Reddit recommendation checks, and full-size intent. Regenerated/deployed `sitemap.xml` so the article has `lastmod=2026-05-04`, submitted the live IndexNow endpoint, and verified production article HTTP 200, one H1, new title/meta description, new FAQ coverage, blog card update, and sitemap lastmod.

### 2026-05-03 (Codex)
- All-tools link cleanup deployed. Updated category page breadcrumbs/buttons, the organized keyboard tester page breadcrumb, language index breadcrumbs, the mouse/keyboard hub link, legacy language tools-directory metadata, shared tool-list data, and three ignored blog articles so active links point to `/pages/all-tools.php` or the correct localized `/pages/all-tools-*.php` pages instead of `/pages/tools.php`. Kept `pages/tools.php` as a 301/noindex legacy redirect and synced `.htaccess` so root `/tools.php` also resolves to `/pages/all-tools.php`. Local verification passed on category pages, language indexes, `/tools/keyboard-tester/`, and `mouse-and-keyboard-test-tools.php`; live verification passed with zero `pages/tools.php` links on sampled active pages and HTTP 301 redirects from both `/pages/tools.php` and `/tools.php` to `/pages/all-tools.php`.
- Local junk cleanup completed. Removed Python bytecode caches, an old temporary Chrome profile under `seo-audit-2026-05-02/site-screenshots/`, old one-off diagnostic scripts, the old ignored `kbt.zip` archive, stale deploy logs, and the untracked backlink PR helper script. Removed tracked PHP `error_log` files and added `error_log` to `.gitignore`. Removed tracked debug pages `testpage.php` and `test-upload-success.php`, added them to `.gitignore`, and removed exposed live debug endpoints (`test-upload-success.php`, `cache-test.php`, `clear-cache.php`, `ftp-connection-test.php`, `test-ftp-upload.php`, `testpage.php`); post-removal live checks return 404 for the debug files and 410 for `testpage.php`.
- Open-source repo landing page published. Rewrote `README.md` with a cleaner GitHub/GitLab-facing project overview, consistent `90+` tool count, live-site/open-site/GitLab links, setup notes, and project structure. Added GitHub Pages files under `docs/`: `index.html`, `404.html`, `robots.txt`, `CNAME`, and `assets/project-preview.svg`. Committed as `a5f5998 [codex] Add open project page` and pushed to GitHub `main`. Enabled GitHub Pages from `main` `/docs`; Pages build `982673186` completed successfully for commit `a5f5998`.
- `open.keyboardtester.click` subdomain configured for the GitHub Pages landing page. Added DNS CNAME `open.keyboardtester.click -> nasirazizawan009.github.io.` through HostArmada/cPanel UAPI via SSH/paramiko. Verification: local DNS resolves to GitHub Pages IPs and `http://open.keyboardtester.click/` returns HTTP 200 with title `KeyboardTester.click Open Source Project`. HTTPS is not ready yet because the GitHub Pages certificate/trust chain is still pending; do not treat that as a site-code issue unless it remains after GitHub finishes certificate provisioning.
- GitLab mirror handled safely. Pushed the same commit to GitLab as branch `codex/open-project-page` (`https://gitlab.com/nasirazawan/keyboardtester.click/-/merge_requests/new?merge_request%5Bsource_branch%5D=codex%2Fopen-project-page`). The GitLab default branches were not overwritten because `gitlab/master` and `gitlab/main` have unrelated history to the current GitHub `main`; force-sync or default-branch replacement requires explicit user approval.
- GitHub related-tool outreach completed from `nasirazizawan009`. Opened permission-style issues asking maintainers whether they are open to a README "Related tools" link before creating any PRs: `10yard/keyboardtester` issue #2 (`https://github.com/10yard/keyboardtester/issues/2`) and `Mostafa-Abbasi/KeyboardTester` issue #9 (`https://github.com/Mostafa-Abbasi/KeyboardTester/issues/9`). Do not open README PRs unless maintainers explicitly agree or ask for one.
- Live blog ad template deployed. Added shared component `includes/components/blog-article-ads.php` for desktop-only article side rails, using the configured AdSense client and the existing rail slots (`blog_article_left_rail` / `blog_article_right_rail`). Updated all current blog article PHP files so each article renders left/right vertical rail shells outside the article column and only creates the `<ins class="adsbygoogle">` units after a desktop viewport gate (`min-width: 1280px`). Updated `blog/generate-blog.php` so future generated articles inherit the same rail template automatically. Updated `blog/index.php` so the middle pinned/top-performing card is replaced by a prominent boxed AdSense placement while the row remains article/ad/article, and removed the separate below-featured leaderboard on the blog home to avoid stacking ads. Deployed the shared component, AdSense slot config, blog home, generator, and 35 article PHP files via SFTP/paramiko. Verification passed: PHP lint on 40 PHP files, `git diff --check`, localhost HTTP 200 checks for blog home and article samples, local Chrome screenshots for blog home and article rails, and live HTTP 200 checks confirming one H1, `blog-pinned-ad-box` on `/blog/`, two article rail shells on sampled articles, desktop rail gate script, and both rail slot IDs present.
- Live AI GPU blog side-rail ads deployed. Updated ignored blog article `blog/ai-gpu-test-webgpu-browser-ai-readiness.php` with a desktop-only three-column layout: left vertical ad rail, article content, and right vertical ad rail. The actual AdSense `<ins>` elements are created only when `matchMedia('(min-width: 1280px)')` matches, so mobile/tablet readers do not request hidden side ads. Added named rail placements in `includes/adsense-slots.php`: `blog_article_left_rail` reuses slot `6240375832`, and `blog_article_right_rail` reuses slot `3614212490` until dedicated vertical ad units are created. Deployed the article and slot config via SFTP/paramiko. Local verification passed with PHP lint, local HTTP 200, rendered desktop screenshot showing side rails outside the article column, and HTML checks for slots/desktop gate. Live verification passed: article HTTP 200, rail layout present, both rail slot IDs present, vertical ad format script present, and desktop gate present.
- Live keyboard digit alignment pass deployed. Updated `tools/keyboard_tester_english.php` so top-row digit/symbol keys and numpad digit/operator keys center their labels on the key cap, while longer keys such as `Backspace`, `Tab`, `Caps Lock`, and `Enter` keep the existing left-aligned label placement. Deployed the single keyboard partial via SFTP/paramiko. Local verification passed with PHP lint, `git diff --check`, localhost HTTP 200, rendered homepage screenshot showing centered digits, and local HTML rule checks. Live verification passed: homepage and `/tools/keyboard-tester/` returned HTTP 200 and both contained the new digit/numpad center-alignment rules.
- Live homepage keyboard 3D contrast pass deployed. Reworked the homepage-scoped `#keyboard-stage` tray styling in `assets/css/index-modern.css` and `assets/css/index-modern.min.css` from a flat light panel to a raised 3D plastic tray with stronger gradient, beveled top/bottom borders, inset highlights, and deeper shadow. Added homepage-scoped normal-key face styling with higher contrast dark gradients, stronger bottom depth, and active/heat-state exclusions so pressed key feedback remains unchanged. Deployed only the two homepage stylesheets via SFTP/paramiko. Local verification passed with `git diff --check`, localhost HTTP 200, minified CSS checks, and a Chrome screenshot confirming the 3D tray/key contrast while keeping the removed dark frame gone. Live verification passed with homepage HTTP 200 and cache-busted `index-modern.min.css` HTTP 200 containing the 3D tray, 3D key, dark-theme tray, and active-state exclusion rules. A live headless Chrome screenshot attempt hung; only the headless Chrome workers from that attempt were stopped.
- Live homepage keyboard frame cleanup deployed. Removed the remaining dark wrapper/background strip under the homepage keyboard tester by adding homepage-scoped `#keyboard-stage` overrides in `assets/css/index-modern.css` and `assets/css/index-modern.min.css`: transparent keyboard wrapper, no wrapper radius/padding/overflow frame, disabled decorative wrapper pseudo-background, and a light `#eef0f2` keyboard tray with subtle border/shadow for contrast against the dark keys. Deployed only the two homepage stylesheet files via SFTP/paramiko. Local verification passed with `git diff --check`, localhost HTTP 200, and a tall Chrome screenshot confirming the dark strip was gone while key contrast remained clear. Live verification passed: homepage HTTP 200, cache-busted `index-modern.min.css` HTTP 200, transparent wrapper rules present, light tray contrast rules present, and dark-theme tray fallback present.
- Live AdSense centering fix deployed. Root cause: homepage and banner placements were rendered as `data-ad-format="auto"` with `data-full-width-responsive="true"`, allowing Google to expand creatives across the viewport and align them from the left edge instead of the centered site container. Updated homepage hero and after-guide ads, blog index featured ad, and site footer ad to horizontal non-full-width mode. Added stronger centering CSS for `.kbt-ad-slot`, `.adsbygoogle`, and injected ad iframes in `assets/css/global.css`, `global.min.css`, `index-modern.css`, and `index-modern.min.css`, then bumped the shared global CSS URL from `v=2.7` to `v=2.8` in `includes/head-common.php` so normal browsers pick up the footer/bottom ad fix. Deployed `index.php`, `footer.php`, `blog/index.php`, `includes/head-common.php`, and the four CSS files via SFTP/paramiko. Local and live verification passed: homepage HTTP 200, homepage has exactly 2 ad units, both homepage units are `data-ad-format="horizontal"` and `data-full-width-responsive="false"`, blog and footer ad placements also use horizontal non-full-width mode, live pages reference `global.min.css?v=2.8`, cache-busted CSS contains `max-width:970px` and iframe centering, and a live Chrome screenshot showed the homepage ad container centered.
- Live homepage hero banner placement deployed. Moved the first homepage manual AdSense unit from below the keyboard tool into the strip between the hero section and the keyboard tester, using placement `home_hero_banner` with the existing ad slot `8866539179`. Added scoped homepage CSS for the hero banner container and removed the old `home_after_tool` placement so the homepage ad count stays at 2 units total (`home_hero_banner` and `home_after_guide`). Deployed `index.php`, `includes/adsense-slots.php`, `assets/css/index-modern.css`, and `assets/css/index-modern.min.css` via SFTP/paramiko. Local verification passed with PHP lint, `git diff --check`, local HTTP 200, and a Chrome headless screenshot. Live verification passed: homepage HTTP 200, `home_hero_banner` appears before `keyboard-stage`, old `home_after_tool` is absent, two homepage `<ins class="adsbygoogle">` units render, slots `8866539179` and `2022852669` are present, and cache-busted CSS contains the hero banner rules.
- Live homepage hero cleanup deployed. Removed the `Primary tester` / `Keyboard tester` intro band above the homepage keyboard tool and changed the tool section to use `aria-label="Keyboard tester tool"`. Tightened the hero layout so the right-side video starts at the top, kept the language pills and metrics in the left column, and added a third right-column mini card (`Export reports`) so the previously empty hero space is filled. Deployed `index.php`, `help/brief-keyboard-tester.php`, `assets/css/index-modern.css`, and `assets/css/index-modern.min.css` via SFTP/paramiko. Verification passed locally with PHP lint, `git diff --check`, and a Chrome headless screenshot; live verification passed with homepage HTTP 200, no `Primary tester` text, no old `tool-stage-title` ID, 3 hero mini cards, 3 metric cards, 2 homepage AdSense slots, and cache-busted CSS containing the updated spacing rules.
- Live manual AdSense slots activated. Added the user-provided Display ad unit IDs to `includes/adsense-slots.php`: `home_after_tool=8866539179`, `home_after_guide=2022852669`, `blog_index_after_featured=6240375832`, and `site_before_footer=3614212490`. Deployed the full manual-slot implementation live via SFTP/paramiko: `includes/adsense-slots.php`, `includes/components/adsense-slot.php`, `includes/head-common.php`, `index.php`, `footer.php`, `assets/css/global.css`, `assets/css/global.min.css`, and `blog/index.php`. Live verification passed: homepage HTTP 200 with exactly two `<ins class="adsbygoogle">` units and no footer slot, blog index HTTP 200 with blog-index plus footer slots, `mouse-test.php` HTTP 200 with one footer slot, all checked pages load the AdSense library, and no checked page renders local placeholder slots. Important follow-up: Auto ads still must be OFF in the AdSense account UI, because if Auto ads remain enabled at account/site level, the same AdSense library can still inject uncontrolled placements alongside these manual slots.
- Local-only controlled manual AdSense slot system added. Created `includes/adsense-slots.php` for manual Display ad unit IDs and `includes/components/adsense-slot.php` for rendering safe ad placements. Added local-only placeholders for missing slot IDs, while live/prod renders no ad container until slot IDs are configured. Updated `includes/head-common.php` so the AdSense loader no longer loads by default unless manual slots are configured or `$allowAutoAdSense` is explicitly enabled, preventing account-level Auto ads from continuing to inject uncontrolled placements from code alone. Added slot styling to `assets/css/global.css` and `assets/css/global.min.css`, ignored `includes/adsense-slots.local.php`, inserted homepage slots after the main keyboard tester and after the guide, inserted a blog-index slot after the pinned/top section, and added a sitewide before-footer slot through `footer.php` with homepage footer-slot suppression. No live deployment was performed because blank slot IDs would stop current live ads instead of earning; next step is to create/paste real AdSense Display ad unit IDs and then deploy. Local verification: PHP lint passed for touched PHP files, homepage returned HTTP 200 with exactly 2 local ad placeholders and no footer ad, blog index returned HTTP 200 with blog-index plus footer placeholders, `mouse-test.php` returned HTTP 200 with one footer placeholder, and no tested page loaded the AdSense script while slot IDs were blank.

### 2026-05-02 (Codex)
- Bing Webmaster API submission and crawlability check completed. Verified local Bing WMT API access via `.env` key and confirmed quota endpoint returned HTTP 200. Submitted all 864 live sitemap URLs to Bing WMT API in two batches with 0 failed submissions, then submitted the sitemap feed (`https://keyboardtester.click/sitemap.xml`) via Bing `SubmitFeed` HTTP 200. Ran a Bing URL-info audit; Bing throttled part of the audit with `ThrottleHost`, but confirmed several pages have crawl/content data while many newer/low-authority URLs are not yet discovered/crawled by Bing. Ran a full live technical indexability crawl over all 864 sitemap URLs: no `noindex`, no broad canonical mismatch, 863/864 returned HTTP 200 on first pass, and the one timeout (`/languages/portuguese/reaction-time-test.php`) retried as HTTP 200. Fixed two real issues found by the crawl: `gamepad-test.php` now has a single H1, and `keyboard-shortcut-identifier.php` now outputs SEO meta/canonical inside `<head>` with one title. Deployed both files via SFTP/paramiko, verified live HTTP 200/one H1/correct canonical, and resubmitted the fixed URLs plus the transient-timeout URL to Bing WMT API (3/3 submitted, 0 failed). Ignored audit exports are saved under `seo-audit-2026-05-02/`.
- Live homepage language links sync completed. Root cause was not browser cache alone: live homepage HTML lacked the `hero-languages` nav and live CSS lacked `.hero-language-*` rules because the earlier language-button work had remained local-only. Deployed `help/brief-keyboard-tester.php`, `assets/css/index-modern.css`, and `assets/css/index-modern.min.css` via SFTP/paramiko. Live verification: `https://keyboardtester.click/` returned HTTP 200 with `hero-languages`, 9 `hero-language-pill` links, and the `Keyboard tester language versions` nav label; the page's cache-busted `assets/css/index-modern.min.css?v=...` returned HTTP 200 and contained `.hero-languages` and `.hero-language-pill`; all 9 language hrefs returned HTTP 200; all 9 flag SVGs returned HTTP 200.
- Live blog pinned row cleanup deployed. Updated `blog/index.php` so the GPU laptop guide remains the main featured article, while the "Pinned from Search Console" row now shows exactly 3 pinned articles in a 3-column desktop row. The hero metric now reports the 3 pinned articles below instead of counting hidden pinned paths. Deployed only `blog/index.php` via SFTP/paramiko. Local and live verification: `/blog/` returned HTTP 200, the GPU guide remained featured, the pinned grid rendered exactly 3 compact cards, the CSS used `repeat(3, minmax(0, 1fr))`, and the metric rendered `3 pinned articles below`.
- Live good GPU laptop guide promotion deployed. Made `blog/best-laptops-with-good-gpu-2026.php` the featured/main article on `blog/index.php` by placing it first in the pinned top-performer list and adding a `GPU laptops` topic chip. Added contextual links to the guide from `help/seo-content/ai-gpu-test.php`, `help/seo-content/gpu-stress-test.php`, and `help/seo-content/memory-test.php`. Regenerated and deployed `sitemap.xml`, updated/deployed ignored `submit-indexnow.php` with the new blog URL, and deployed the article, six WebP images, `blog/posts-data.php`, blog index, and tool SEO content via SFTP/paramiko. Local verification: PHP lint passed for all changed PHP files; homepage latest posts included the new article automatically from `blog/posts-data.php`; `/blog/` featured the article; AI GPU, GPU Stress, and Memory pages all contained the article link. Live verification: article HTTP 200 with 1 H1, 1 YouTube iframe, price-status wording, unconfirmed-price wording, and disclosure; all six WebP images HTTP 200 as `image/webp`; homepage latest section includes the article; blog homepage features it; AI GPU/GPU Stress/Memory pages link to it; live sitemap HTTP 200 with 864 URLs and the article URL; IndexNow endpoint returned HTTP 200 success and the deployed submit list contains the new article URL.
- Local-only good GPU laptop buyer guide added. Created `blog/best-laptops-with-good-gpu-2026.php` with a source-backed RTX 5090/RTX 5080 laptop comparison, price snapshot checked May 2, 2026, real model images, Jarrod'sTech YouTube embed, Article/FAQ schema, a disclosure note, source list, and internal GPU/keyboard testing links. Updated `blog/posts-data.php` so the post appears in the blog index and homepage latest lists. No live deployment was performed. Local verification: PHP lint passed for the article and `blog/posts-data.php`; `https://localhost/kbt/blog/best-laptops-with-good-gpu-2026.php` returned HTTP 200 with 1 H1, 1 YouTube iframe, the source section, the disclosure note, and the internal GPU test link; all six local WebP laptop images returned HTTP 200; `https://localhost/kbt/blog/` returned HTTP 200 and included the new post; `git diff --check` passed for tracked edits.
- Local-only homepage keyboard tool border cleanup completed. Added scoped `#keyboard-stage` CSS overrides in `assets/css/index-modern.css` and mirrored them in `assets/css/index-modern.min.css` to remove the extra `tool-shell` frame/background/border/shadow/padding around the homepage keyboard tester while leaving the actual keyboard controls and key styling intact. No live deployment was performed. Local verification: `https://localhost/kbt/` returned content with the keyboard stage and tool shell present, loaded `assets/css/index-modern.min.css`, the minified stylesheet includes the scoped overrides, and `git diff --check` passed.
- Live homepage accessibility audit fixes deployed. Updated `includes/components/keyboard-cat-progress.php` to remove the prohibited `aria-label` from the decorative `.cat-body` and mark it `aria-hidden="true"`. Updated `includes/components/homepage-latest-blog.php` so the `Read guide ->` CTA uses accessible contrast colors (`#0369a1` on light cards, `#7dd3fc` in dark mode). Deployed both components via SFTP/paramiko. Local verification: PHP lint passed for both components, homepage HTML no longer rendered `class="cat-body" aria-label=`, rendered `aria-hidden="true"`, rendered three latest-blog cards, and `git diff --check` passed. Live verification: `https://keyboardtester.click/` returned HTML with no bad cat aria-label, hidden decorative cat body present, CTA contrast rules present, three latest-blog cards present, and calculated CTA contrast was 5.93:1 on light and 10.71:1 on dark.
- Live homepage latest blog section deployed. Uploaded `index.php`, `includes/components/homepage-latest-blog.php`, and `blog/posts-data.php` via SFTP/paramiko to `keyboardtester.click`. Live verification passed: `https://keyboardtester.click/` returned HTTP 200, rendered the `homepage-latest-blog` section with three cards and three images, included the `View all guides` link, and all three rendered blog URLs plus all three rendered image URLs returned HTTP 200.
- Local-only homepage latest blog section added. Created `includes/components/homepage-latest-blog.php` to pull the newest three posts from `blog/posts-data.php`, sort by publish date, and render compact image cards right above the footer after the existing related guide CTA. Updated `index.php` to include the component. No live deployment was performed. Local verification: PHP lint passed for `index.php` and `includes/components/homepage-latest-blog.php`; `https://localhost/kbt/` returned HTTP 200 with three latest blog cards, three images, the `View all guides` link, and all rendered card/image targets returned HTTP 200 locally.
- Local-only Pro Test guideline added. Updated `help/keyboard-tester.php` so the Advanced Options section now explicitly explains the Pro Test workflow for advanced users: guided Test All Keys coverage, shortcut/gaming combo checks, Statistics and Heatmap review, Ghost Click hands-off monitoring, Latency sampling, and Export for repair/support evidence. Added matching visible FAQ item and updated homepage FAQ JSON-LD in `includes/schema.php`. No live deployment was performed. Local verification: PHP lint passed for `help/keyboard-tester.php` and `includes/schema.php`; `https://localhost/kbt/` returned HTTP 200, one H1, Pro Test heading/FAQ present, nine visible FAQ items, nine FAQ schema questions, and visible/schema FAQ questions matched exactly.
- Local-only homepage language buttons added. Updated `help/brief-keyboard-tester.php` to render a compact hero language nav from the existing `$keyboardLanguages` config, including 9 flag pills with the current English page marked via `aria-current`. Added responsive light/dark styling in `assets/css/index-modern.css` and mirrored the loaded rules in `assets/css/index-modern.min.css`. No live deployment was performed. Local verification: PHP lint passed for `help/brief-keyboard-tester.php` and `index.php`; `git diff --check` passed; `https://localhost/kbt/` returned HTTP 200 with 9 language pills, 9 flag images, the all-tools CTA still present, and 0 broken/missing internal href/src targets.
- Local-only homepage/tool-list link fix completed. Changed the homepage hero CTA in `help/brief-keyboard-tester.php` from the on-page `#tools` section to the canonical `pages/all-tools.php` page and relabeled it `Explore all testing tools`. Fixed all-tools language-switch links in `includes/components/site-chrome.php` to use `url(...)` instead of root `/pages/...` paths, resolving local `/kbt/` 404s while preserving production root behavior. No live deployment was performed. Local verification: PHP lint passed for `help/brief-keyboard-tester.php`, `index.php`, and `includes/components/site-chrome.php`; `git diff --check` passed; homepage plus all 9 all-tools pages returned HTTP 200 with 0 broken/missing internal href/src targets.
- Local-only keyboard export enhancement completed. Updated `tools/keyboard_tester_english.php` so the exported `.txt` report now includes Advanced Options state, Statistics summary, Test All Keys coverage/untested keys, Heatmap press intensity log, Ghost Click test status/events, Latency test samples/summary, key counts, and key history. Added in-memory ghost-click event tracking for export. No live deployment was performed. Local verification: PHP lint passed for `tools/keyboard_tester_english.php`, targeted `git diff --check` passed, and both inline non-JSON scripts parsed successfully with Node.
- Local-only advanced-options guide expansion completed. Added an `Advanced Options Explained` section to `help/keyboard-tester.php` covering Statistics, Heatmap, Sound, Test All Keys, Export, Ghost Click, and Latency with practical usage notes. Added matching FAQ item `What do the Advanced Options do?` and updated homepage FAQ JSON-LD in `includes/schema.php`; visible FAQ/schema questions still match exactly. No live deployment was performed. Local verification: PHP lint passed for `help/keyboard-tester.php` and `includes/schema.php`; `https://localhost/kbt/` returned HTTP 200, one H1, advanced section/card text present, eight visible FAQ items, eight FAQ schema questions, and visible/schema FAQ questions matched.
- Local-only homepage guide/FAQ improvement completed. Rewrote `help/keyboard-tester.php` into a clearer practical guide covering full-key checks, reading results, stuck/repeating keys, layout/OS labels, compact keyboards, rollover/ghosting checks, export, and privacy. Updated homepage FAQ JSON-LD in `includes/schema.php` to match the seven visible FAQ questions exactly. No live deployment was performed. Local verification: PHP lint passed for `help/keyboard-tester.php` and `includes/schema.php`; `https://localhost/kbt/` returned HTTP 200, one H1, guide heading present, seven visible FAQ items, seven FAQ schema questions, visible/schema FAQ questions matched, and no removed `seo-content` or `tools-list-section` blocks returned.
- Local-only tools-directory consolidation completed. Kept `/pages/all-tools.php` as the canonical English tools directory and changed `/pages/tools.php` plus root `/tools.php` into 301 PHP redirects to it. Also changed localized `/languages/{lang}/tools.php` directory pages into 301 redirects to the matching `/pages/all-tools-xx.php` canonical pages and removed those redirected URLs from generated `sitemap.xml` (now 863 URLs). Updated config canonicalization, shared tool-list routing, related-tools CTA, and SiteNavigation schema to prefer all-tools URLs. No live deployment was performed. Local verification: PHP lint passed for changed PHP files, `git diff --check` passed, old local URLs redirect to the all-tools targets and finish at HTTP 200 with one H1, and sitemap no longer contains `/pages/tools.php`, root `/tools.php`, or localized `/languages/*/tools.php` entries.
- Local-only homepage cleanup completed. Removed the full tools directory include and the long duplicate keyboard SEO article from `index.php`, leaving one concise keyboard guide/FAQ plus a compact popular-tools section. Homepage FAQ JSON-LD now matches the six visible FAQ items in `help/keyboard-tester.php`. Also changed the intent-cluster visible kicker from SEO jargon to a configurable friendly label. No live deployment was performed per user instruction. Local verification: PHP lint passed for touched PHP files; `http://localhost/kbt/` returned HTTP 200, one H1, one FAQPage JSON-LD block with six questions, no `tools-list-section`, no `seo-content` section, and no `Search Intent Cluster` visible text.
- Completed competitor tool-gap audit for `https://www.xbitlabs.com/tools/` against KeyboardTester.click. Extracted 78 XbitLabs tools. Result: 70 covered by existing KeyboardTester tools, 3 covered but without standalone URLs (`Sensitivity Converter`, `Aim Trainer`, `What Is My IP`), 1 partial (`Mouse Latency Test`), and 4 missing/semantic mismatch candidates (`Keyboard Clicker`, cognitive `Memory Test`, `RAM Bandwidth Test`, `WASD Trainer`). Full ignored report and CSV saved at `seo-audit-2026-05-02/xbitlabs-tool-gap-audit-2026-05-02.md` and `.csv`.
- Reviewed delivery failures in `outreach@keyboardtester.click` via IMAP. SMTP accepted 29 outreach emails, but 15 delivery-failure notices were received and matched to sent contacts: 14 recipient spam-filter rejects (`550 High probability of spam`) and 1 invalid/nonexistent mailbox (`support@omegacomputerrepairs.com`). 14 emails remain likely delivered or pending. Created ignored reports under `seo-audit-2026-05-02/outreach/`: `bounce-report-usa-keyboardtester-2026-05-02.csv`, `bounce-summary-usa-keyboardtester-2026-05-02.md`, and `suppression-list-usa-keyboardtester-2026-05-02.csv`. DNS check found SPF and DMARC, but no DKIM TXT record on common selectors, so pause new cold outreach until cPanel Email Deliverability/DKIM is checked.
- Replied on merged PR `nafasebra/awesome-webdesign-tools#32` with a short thank-you comment. Do not add more comments there unless the maintainer replies. Current verified awesome-list state: `StanForever/awesome-websites#49` merged, `nafasebra/awesome-webdesign-tools#32` merged, and `Bakumon/awesome-online-tools#29` submitted/open.
- Registered the outreach operating rules in ignored master tracker `seo-audit-2026-05-02/outreach/outreach-master-tracker-2026-05-02.md`. It covers mailbox/Sent-folder handling, USA batch status, held contacts, email writing rules, sender reputation limits, Russian/localized outreach rules, advertiser prospecting, WhatsApp outreach rules, backlink PR status, and next actions. Future outreach should follow that tracker to avoid duplicate sends or reputation damage.
- Fixed the outreach mailbox Sent-folder visibility issue. SMTP delivery did not automatically save messages to webmail Sent, so Codex appended 29 already-sent message copies to IMAP folder `INBOX.Sent` without resending anything. Sent folder count moved from 0 to 29. Copy log is ignored at `seo-audit-2026-05-02/outreach/imap-sent-copy-log-usa-keyboardtester-2026-05-02.csv`. Future outreach send scripts should append a Sent copy via IMAP, or otherwise BCC/archive explicitly, so webmail reflects sent messages.
- Verified `nafasebra/awesome-webdesign-tools#32` was merged into `main` on 2026-05-01T20:04:26Z. The merged backlink points to `https://keyboardtester.click/online-ruler.php` from `README.md` at merge commit `50f108f02b91ffdea6cec3449e333cd5126e9ec9`. Updated ignored backlink sprint note; practical status is now 2 merged GitHub backlink PRs and 19 open PRs to monitor.
- Sent the approved USA outreach batch from `outreach@keyboardtester.click` after shortening the copy and personalizing each email by company/reference/target URL. Sent 29 validated contacts successfully with 0 SMTP failures; 21 unverified contacts remain on hold. Logs and final drafts are ignored under `seo-audit-2026-05-02/outreach/`: `drafts-short-ready-usa-keyboardtester-2026-05-02.csv`, `drafts-short-ready-usa-keyboardtester-2026-05-02.sent.csv`, `sent-log-usa-keyboardtester-2026-05-02.csv`, and updated `outreach-approval-summary-2026-05-02.md`. Do not resend these 29 unless a manual follow-up is approved later.
- Prepared a 50-prospect USA backlink outreach batch for KeyboardTester.click under ignored `seo-audit-2026-05-02/outreach/`. Files: `prospects-usa-50-keyboardtester-2026-05-02.csv`, `validation-usa-50-keyboardtester-2026-05-02.csv`, `drafts-usa-50-keyboardtester-2026-05-02.csv`, and `outreach-approval-summary-2026-05-02.md`. Validation found 29 contacts ready for user-approved sending and 21 contacts on hold because the exact email was not confirmed on public pages or needs replacement. Drafts vary target URLs across homepage, keyboard tester, keyboard ghosting, N-key rollover, online ruler, and dead-pixel pages. No outreach emails were sent.
- Created `outreach@keyboardtester.click` via cPanel UAPI over SSH for backlink/outreach work. Mailbox quota is 750 MB (host maximum). SMTP/IMAP credentials are stored only in gitignored `.env` under `OUTREACH_SMTP_*` / `OUTREACH_FROM` / `OUTREACH_REPLY_TO`; password value is not documented here. Verified SMTP 587 login and IMAP 993 login successfully. No outreach emails were sent; next step remains prospect list + manual approval before sending small batches.
- Prepared the next practical backlink execution plan at `seo-audit-2026-05-02/backlink-next-actions-2026-05-02.md`. Recommendation: pause new random GitHub PRs because 20 are already open; focus next on manual/directory submissions and editorial outreach. Priority order: AlternativeTo on 2026-05-04 after the account-age block clears, Slant retry, SaaSHub product verification/submission, and three targeted guest/resource pitches to WeTechTitans, Technical Master, and UltraBookGaming.
- Ran a practical backlink sprint focused on GitHub awesome-list opportunities. Opened two new mergeable PRs: `Bakumon/awesome-online-tools#29` adding KeyboardTester.click to the Test section, and `nafasebra/awesome-webdesign-tools#32` adding the Online Ruler to the Utils section. Used the `devicetester/awesome-online-tools` fork for the Bakumon PR because the user account already had a same-name fork from another upstream; cleaned up the unused branch from the wrong fork. Current GitHub backlink PR snapshot: 20 open, 1 merged, 5 closed/not-merged. Saved the working note at `seo-audit-2026-05-02/backlink-sprint-2026-05-02.md`.

### 2026-05-01 (Codex)
- Cleaned up the latest broken-link report: added a 301 redirect from `/keyboard-tester/` to `/tools/keyboard-tester/`, fixed the Minecraft circle related-tool link that emitted the old path, removed the missing `assets/css/tool-components.css` include from Russian DPI, added a compatibility `assets/css/tool-components.css` asset for older references, and removed crawler-hostile Tracker/MSI store source links from two live blog posts. Deployed via SFTP/paramiko and verified live: `/keyboard-tester/` now returns 301 to the canonical tool, `/assets/css/tool-components.css` returns HTTP 200, the updated source pages are HTTP 200, and the removed external URLs no longer appear in the live source pages. The DAA opt-out link returned HTTP 200 in verification and was kept because it is a compliance/helpful privacy reference.
- Completed duplicate/similar page audit across the full sitemap-rendered page set. Initial render audit checked 880 indexable URLs and found no duplicate titles, descriptions, canonicals, or exact rendered-text duplicates. One repeatable high-confidence near-duplicate pattern existed: localized `dead-pixel-test.php` pages overlapped strongly with localized `screen-test.php` pages in all 8 locales.
- Consolidated the 8 localized dead-pixel URLs into their matching localized screen-test URLs via `.htaccess` 301 redirects, PHP renderer fallback redirects, URL helper normalization, localized intent catalog removal, and sitemap generator exclusion. Regenerated and deployed `sitemap.xml`; live sitemap now has 872 URLs and no localized dead-pixel entries.
- Verification passed locally and live: PHP lint passed for changed PHP files; local duplicate audit after the fix rendered 872 URLs with 0 fetch errors, 0 duplicate title groups, 0 duplicate description groups, 0 duplicate canonical groups, 0 exact rendered-text duplicate groups, and 0 high-confidence near-duplicate pairs. Live checks confirmed all 8 retired localized dead-pixel URLs return 301 to localized `screen-test.php`, all 8 targets return HTTP 200 with one H1, and live sitemap is HTTP 200 with 872 URLs.
- Submitted the updated state to discovery systems: IndexNow live endpoint returned HTTP 200 with 858 URLs, Google Indexing API accepted the 8 retired URLs plus 8 target screen-test URLs (16 OK), and Bing WMT accepted the same 16 URL batch.
- Updated the live AI GPU blog article `blog/ai-gpu-test-webgpu-browser-ai-readiness.php` with three additional local WebP stock images, section captions, and an official Chrome Developers YouTube embed about WebAssembly/WebGPU for faster Web AI. Verified locally and live: article HTTP 200, one H1, one iframe, new image assets served as `image/webp`. Submitted the updated article URL to Bing WMT, Google Indexing API, and IndexNow. Note: the blog article and new image assets live under ignored `blog/` paths and were deployed directly via SFTP/paramiko.
- Published the English AI GPU cluster live: deployed `ai-gpu-test.php`, shared AI GPU assets/includes, regenerated `sitemap.xml`, added the new tool to IndexNow submission, and published `blog/ai-gpu-test-webgpu-browser-ai-readiness.php` with research-backed WebGPU/WebNN/browser AI readiness content and internal links to AI GPU, GPU stress, FPS, CPU stress, and memory tests. Submitted updated URLs to IndexNow and Google Indexing API. Note: `submit-indexnow.php` and the new blog article live under ignored paths; local copies were deployed directly via SFTP/paramiko.
- Built local-only English `ai-gpu-test.php` with WebGPU/WebGL2/WebNN detection, AI readiness score, WebGPU matrix benchmark, WebGL graphics FPS sanity check, SEO copy/schema/help content, and tool-list/related-tool wiring. No live deploy, sitemap regeneration, or IndexNow submission was run; user will test locally first.
- Localized the left floating site sidebar across all 9 locales. `includes/components/site-sidebar.php` now uses the existing localized tool/category data from `tool-list-data.php`, localizes sidebar UI labels/search/ARIA text, and sets RTL direction for Arabic. Deployed only this component via SFTP/paramiko. Verified live Spanish, Japanese, Arabic, and German pages render localized sidebar labels and tool names; Arabic sidebar includes `dir="rtl"`.
- DeviceTester GitHub org setup completed after user authorized GitHub CLI as `nasirazizawan009` with `repo,admin:org`. Created `devicetester/.github`, pushed org profile README, transferred `awesome-device-testing-tools` to `devicetester/awesome-device-testing-tools`, set homepage to `https://keyboardtester.click`, kept issues enabled, and verified the old `nasirazizawan009/awesome-device-testing-tools` URL 301-redirects to the new org repo.
- DeviceTester GitHub org follow-up: user created `https://github.com/devicetester` and uploaded the generated avatar. Initial CLI web auth timed out before user authorization; Codex prepared an ignored org profile README draft at `seo-audit-2026-05-01/devicetester-org-profile/profile/README.md`, then completed setup after authorization.
- Checked GitHub awesome/backlink status: `nasirazizawan009/awesome-device-testing-tools` has no open/closed PRs or issues from outside contributors. Outgoing PR status found 1 merged backlink PR (`StanForever/awesome-websites#49`, merged 2026-04-26), 18 open PRs, and 5 closed-not-merged PRs. `mathewlewallen/awesome-free-tools#62` remains open/mergeable with reviewer requested and no new maintainer comment after the user addressed feedback on 2026-04-27.
- Generated Microsoft Store PWA package using Partner Center identity values. Local archive: `microsoft-store/package/KeyboardTester-click-pwabuilder-package.zip`; upload files: `microsoft-store/package/extracted/KeyboardTester.click.msixbundle` and `microsoft-store/package/extracted/KeyboardTester.click.classic.appxbundle`. Verified package manifests contain Package ID `KeyboardTester.Click.KeyboardTester.click`, Publisher `CN=1DE207C4-E7E1-4A04-A664-AEAD22613CAE`, and PublisherDisplayName `KeyboardTester.Click`. `microsoft-store/package/` is ignored because it contains generated upload artifacts.
- Prepared local Microsoft Store PWA submission kit under `microsoft-store/`: preflight report/README, Partner Center submission checklist, ready-to-paste Store listing draft, and four 1366x768 desktop screenshots.
- Confirmed no native Windows rebuild is needed for the first Store submission: existing live PWA has reachable manifest, service worker, offline page, and 512x512 icon. Final `.msixbundle`/`.classic.appxbundle` generation is blocked only on Partner Center Product Identity values: Package ID, Publisher ID, and Publisher display name.
- Follow-up for `keyboard_tester_different_languages.php`: live Japanese flag asset was present at `flags/japanese_flag.svg`, but the browser could still show the old broken `japan_flag.svg` DOM/cache. Added cache-busting `?v=20260501` to the Japanese flag URL and re-uploaded both the page and `flags/japanese_flag.svg`. Live verification: page HTTP 200, versioned Japanese flag URL HTTP 200, old `japan_flag.svg` path absent.
- Fixed and deployed live `keyboard_tester_different_languages.php`: removed the outdated note saying only Keyboard Tester has language support, updated the page title/description/hero copy to reflect the localized tool suite, and corrected the Japanese flag path from `japan_flag.svg` to `japanese_flag.svg`.
- Verified locally and live: page returns HTTP 200, old note is absent, new localized-tool-suite note is present, and the Japanese flag path is correct. Deployed via SFTP/paramiko only.
- Ran a fresh Search Console URL Inspection API audit against the current sitemap URL set: 877 URLs inspected, 167 submitted/indexed, 701 unknown to Google, and 9 confirmed `Crawled - currently not indexed`.
- Evidence for the 9 affected URLs: all were `ALLOWED`, `INDEXING_ALLOWED`, fetch `SUCCESSFUL`, and canonicalized to themselves. Root cause was not robots/noindex/canonical/fetch; fixes targeted weak page value and inconsistent alternate signals.
- Fixed all discovered redirecting/wrong localized `hreflang` English/x-default targets for click-speed and headphone pages. `click-speed-test.php`, `headphone-test.php`, and `tools/headphone_speaker_tester.php` no longer appear as localized alternate targets; Japanese click-speed no longer points alternates at `mouse-test.php`.
- Strengthened the 7 affected localized tool pages with more specific titles/descriptions, expanded FAQ schema, and visible result-interpretation/help content: French mouse, French click-speed, French latency, French typing, German click-speed, Portuguese click-speed, German headphone.
- Strengthened `pages/category-utilities.php` with fuller utility selection content, visible FAQ, ItemList schema, FAQPage schema, and supporting CSS in `assets/css/category-pages.css`.
- Updated the ignored/static blog article `blog/what-is-keyboard-ghosting-anti-ghosting-fix-guide.php` directly because `blog/` is gitignored: shorter anti-ghosting-led title/meta/schema, May 1 modified date, quick-answer block, and a dedicated "anti-ghosting keyboard meaning" section with links to the ghosting and NKRO tests.
- Saved local audit evidence at `seo-audit-2026-05-01/crawled-not-indexed-fix-report.md` plus the API exports under `seo-audit-2026-05-01/` (ignored by git).
- Verified locally: PHP lint passed for modified tracked PHP files, local target pages returned HTTP 200, JSON-LD parsed on all 9 target pages, bad redirecting hreflang scan returned 0, and targeted `git diff --check` passed.
- Deployed live via SFTP/paramiko only. Live verification passed for all 9 target URLs: HTTP 200, updated titles/descriptions present, JSON-LD valid, and corrected alternates visible.
- Submitted the 9 affected URLs to Google Indexing API: 9 OK, 0 failed. Submitted the live IndexNow endpoint: HTTP 200 with "URLs submitted successfully".
- Note: unrelated local changes already existed in `blog/posts-data.php`, `sitemap.xml`, and untracked `scripts/submit-targeted-awesome-prs.py`; they were not deployed as part of this cleanup.

### 2026-04-30 (Codex)
- Fixed and deployed live: Pac-Man/ghost contact now uses swept collision detection instead of only end-of-frame overlap. This fixes cases where a ghost visually crossed Pac-Man but no collision/eat event fired.
- Scope: `assets/js/pacman-game.js` now checks movement path distance between Pac-Man and ghosts, including tunnel wrapping; `assets/js/keyboard-cat-progress.js` now sweeps ghost movement and Pac-Man keypress jumps in the keyboard progress strip; regenerated `assets/js/keyboard-cat-progress.min.js`.
- Verified: `node --check` passed for `pacman-game.js`, `keyboard-cat-progress.js`, and `keyboard-cat-progress.min.js`; local `pacman-game.php` and homepage returned HTTP 200; live JS/page verification returned HTTP 200 and confirmed the collision-fix markers are being served.
- Deployed live via SFTP/paramiko, not FTPS: GSC indexing cleanup content, English keyboard tester, shared keyboard progress/Pac-Man strip assets, all 8 localized keyboard sections, localized keyboard visual/history assets, shared head component, `tools/keyboard-tester-advanced.php`, and the standalone `pacman-game.php` page/assets.
- Verified live: `pacman-game.php`, `assets/js/pacman-game.js`, `assets/css/pacman-game.css`, `assets/js/keyboard-cat-progress.min.js`, `includes/components/keyboard-cat-progress.php`, `assets/js/localized-keyboard-history.js`, Japanese localized keyboard page, homepage keyboard page, and `keyboard-ghosting-test.php` all returned HTTP 200 and contained the expected deployed markers.
- Submitted the updated URL set through the live IndexNow endpoint. Response was HTTP 200 with "URLs submitted successfully" for 863 URLs. Google/GSC does not accept IndexNow; this submission covers Bing, Yandex, Seznam, and Naver-compatible discovery.
- Note: attempted browser smoke via local Playwright, but the workspace does not currently have the `playwright` Node module installed. Live HTTP/content verification passed.
- Done locally only: Improved the standalone Pac-Man game screen behavior. Added a `Fullscreen` control, fullscreen-specific board layout CSS, and automatic vertical viewport follow so the page scrolls down/up to keep Pac-Man visible while playing on shorter screens.
- Verified locally only: `C:\xampp\php\php.exe -l pacman-game.php` and `node --check assets/js/pacman-game.js` passed. Chrome headless tests with short desktop/mobile viewports confirmed auto-scroll activates while playing, fullscreen enters with the game stage filling the viewport, fullscreen exit resets the button label, no horizontal overflow, no console errors, and no failed HTTP responses. No live deploy was run.
- Done locally only: Fixed the standalone `pacman-game.php` movement bug where Pac-Man could feel stuck after the first dot. Root cause was tile-center movement logic: the first pass depended on a loose center threshold and rounded the next tile target too early, causing repeated snap-back near a tile center.
- Done locally only: Reworked `assets/js/pacman-game.js` movement to use direction-aware tile-center crossing (`floor`/`ceil` by direction), exact center snapping, tunnel-safe X normalization, and leftover frame movement so turns and forward movement are smoother. Also set the game board to its native 672px canvas width in `assets/css/pacman-game.css` to avoid non-integer desktop scaling jitter.
- Verified locally only: `node --check assets/js/pacman-game.js`, `C:\xampp\php\php.exe -l pacman-game.php`, and Chrome headless desktop/mobile tests passed. Movement test confirmed score/dot counts keep updating through Left -> Up -> Right -> Down -> Right turns (`00050` to `00190`, dots `220` to `206`), no overlay lock, no horizontal overflow, no console errors, and no failed HTTP responses. No live deploy was run.
- Done locally only: Added `pacman-game.php` plus `assets/css/pacman-game.css` and `assets/js/pacman-game.js` as a full playable retro maze chomp game with pellets, power pellets, chase/scatter ghost behavior, frightened/eaten ghost states, fruit bonus, scoring, high score, lives, levels, pause/reset, sound toggle, palette toggle, speed toggle, keyboard controls, swipe controls, and mobile touch buttons.
- Done locally only: Added a `Full game` link to the existing keyboard Pac-Man progress strip so users can open the standalone game page from the keyboard tester.
- Done locally only: Updated `includes/head-common.php` so local service-worker registration uses the configured base URL (`/kbt/sw.js` locally, `/sw.js` live) and added a page-level AdSense opt-out used by the local noindex game page for clean gameplay/browser validation. No live deploy was run.
- Verified locally only: `pacman-game.php`, `includes/head-common.php`, and `includes/components/keyboard-cat-progress.php` passed PHP lint; `assets/js/pacman-game.js` and `assets/js/keyboard-cat-progress.js` passed `node --check`; Chrome headless tests on desktop and mobile confirmed the canvas renders, Start begins play, score/dot counts update after movement, palette/speed controls work, the mini-strip `Full game` link points to `/kbt/pacman-game.php`, there is no horizontal overflow, and there are no console errors or failed HTTP responses.
- Done locally only: Added `assets/js/localized-keyboard-history.js` and wired it into all 8 localized keyboard pages so the history textarea records the character from the selected language layout instead of relying on the browser/OS `event.key` value. This covers physical key presses and on-screen key clicks.
- Done locally only: Corrected German QWERTZ `Y`/`Z` `data-key` wiring so physical `KeyY` records/highlights `Z` and physical `KeyZ` records/highlights `Y`.
- Verified locally only: Browser tests confirmed localized history output for Arabic (`ض`, `١`), French (`A`, `é`), German (`Z`, `Y`, `ß`), Japanese (`た`, `ぬ`), Korean (`ㅂ`, shifted `ㅃ`/`ㅒ`), Portuguese (`Ç`, `Espaço`), Russian (`Й`, `Б`), and Spanish (`Ñ`, `¡`, shifted `¿`). On-screen key click tests also passed for representative localized keys in all 8 languages. No live deploy was run.
- Done locally only: Corrected localized keyboard legends to use real physical-layout characters instead of ASCII approximations: French AZERTY accents (`É`, `é`, `è`, `ç`, `à`, `ù`), German QWERTZ umlauts/`ß` (`Ä`, `Ö`, `Ü`, `ß`), Portuguese `Ç`/`Espaço`, and Korean Dubeolsik shifted Hangul legends (`ㅃ`, `ㅉ`, `ㄸ`, `ㄲ`, `ㅆ`, `ㅒ`, `ㅖ`).
- Verified locally only: Confirmed exact Unicode legends are present in the source files, all 8 localized keyboard pages return HTTP 200 and include the shared visual CSS, changed pages have no rendered horizontal/text overflow, and `C:\xampp\php\php.exe -l` passed for all 8 localized `languages/*/sections/tool.php` files. No live deploy was run.
- Done locally only: Filled the Japanese JIS empty space between Enter and right Shift by restoring a tall JIS Enter shape, narrowing it to 114px, and shifting it right with a 14px margin so it does not collide with the home-row `}` key.
- Verified locally only: Headless Chrome confirmed zero key overlaps, a normal row gap between Enter and right Shift, and exact right-edge alignment between Enter and right Shift. Reconfirmed all 8 localized keyboard pages return HTTP 200 and include the shared visual CSS. No live deploy was run.
- Verified locally only: `C:\xampp\php\php.exe -l` passed for all 8 localized `languages/*/sections/tool.php` files, and `git diff --check` passed.
- Done locally only: Corrected the follow-up Japanese JIS Enter overlap by removing the tall negative-margin Enter override in `assets/css/localized-keyboard-visual.css`; Enter remains wide for the JIS row but no longer draws over the next row.
- Verified locally only: Headless Chrome overlap check found zero Japanese Enter/key intersections after the fix. Reconfirmed all 8 localized keyboard pages return HTTP 200 and include the shared visual CSS. No live deploy was run.
- Verified locally only: `C:\xampp\php\php.exe -l` passed for all 8 localized `languages/*/sections/tool.php` files, and `git diff --check` passed.
- Done locally only: Fixed the Japanese JIS keyboard layout in `assets/css/localized-keyboard-visual.css` so the shared localized visual theme preserves JIS-specific Enter, right Shift, and Space widths instead of collapsing them to the standard key size.
- Verified locally only: Headless Chrome layout metrics confirmed Japanese number, Q, Shift, and bottom rows now share the same right edge after scaling; a screenshot pass confirmed the tall JIS Enter key renders correctly. Reconfirmed all 8 localized keyboard pages return HTTP 200 and include the shared visual CSS. No live deploy was run.
- Verified locally only: `C:\xampp\php\php.exe -l` passed for all 8 localized `languages/*/sections/tool.php` files, and `git diff --check` passed.
- Done locally only: Updated the shared Pac-Man keyboard progress game so repeated presses of the same key advance Pac-Man. The component now uses total press count for movement, loops back to the start after one full keyboard-length run, clears dots/pellets for the new cycle, and keeps the cumulative score increasing.
- Verified locally only: `node --check` passed for `assets/js/keyboard-cat-progress.js` and `.min.js`; PHP lint passed for English plus all 8 localized keyboard sections; headless Chrome confirmed repeated `KeyA` presses move Pac-Man from 1% to 5%, reach 100% at 103 English presses, then restart at 1% on press 104, with Spanish repeated presses also advancing. No live deploy was run.
- Done locally only: Synced the 8 localized desktop keyboard sections (`languages/*/sections/tool.php`) to the current English dark-key/light-deck visual format by adding shared `assets/css/localized-keyboard-visual.css` and loading it after each language section's inline keyboard CSS.
- Verified locally only: XAMPP routes for all 8 language pages return HTTP 200 and include the new CSS; `C:\xampp\php\php.exe -l` passed for all 8 localized `sections/tool.php` files; headless Chrome checks confirmed all localized keyboards use the English dimensions (`60px` keys, `36px` layout gap, corrected function-row gap, light deck background) and active key colors still render. No live deploy was run.
- Done: Refreshed the local-only English desktop keyboard tester visual in `tools/keyboard_tester_english.php` to match the dark-key/light-deck full keyboard reference more closely.
- Done: Kept existing key `data-key` wiring and added visual-key click handling so mouse/touch clicks on desktop keys record history/counters/cat progress the same way as physical key presses. PrintScreen remains disabled because browsers cannot capture it.
- Done: Follow-up alignment pass: F12 now ends on the same right edge as Backspace, and the desktop keyboard shows visible Caps/Num/Scroll indicator lights above the numpad.
- Done: Fixed local English keyboard OS switching reliability and changed the keyboard sound control to start ON by default on page load.
- Done: Improved Mac/Windows switcher visibility: Mac mode now reorders the bottom modifier row into Mac-style order while Windows mode restores the Windows order.
- Done: Moved the Pro Diagnostics floating "Held" indicator out of the lower-right viewport corner so it no longer overlaps the numpad/keyboard; it now appears as a centered pill above the keyboard while keys are held.
- Done: Stabilized visual key clicks so clicking on-screen keys no longer causes whole-keyboard flicker: keys no longer take pointer focus, counter badges are pre-rendered, active classes are updated with `classList`, and the click sound reuses one audio context.
- Done: Reserved layout space for the Pro Diagnostics "Held" pill and changed it from `display` toggling to opacity/visibility toggling so physical key presses no longer shift/flicker the keyboard deck.
- Done: Shifted the English keyboard "Held" status pill under the desktop keyboard while keeping the shared Pro Diagnostics component compatible with localized keyboard pages.
- Done: Replaced the visible cat progress game locally with a horse pasture progress scene: a small horse moves along the ground, eats grass at 10 milestones, and passes different tree shapes at each step.
- Done: Refined the horse pasture visual after review: replaced the browser-dependent horse emoji with a right-facing CSS horse and added continuous grass across the full track, not only at tree milestones.
- Verified locally only: PHP lint passed, homepage and `/tools/keyboard-tester/` render without horizontal overflow, desktop keypress/click interactions light keys and update history/horse pasture progress, and the mobile keyboard view remains active under mobile viewport. No live deploy was run.
- Done: Re-checked GSC URL Inspection against the earlier indexing cleanup. Current sample shows `mouse-and-keyboard-test-tools.php` and `keyboard-ghosting-test.php` are now indexed; `index.php` is still a normal redirect to `/`; `privacy.php` is stale canonical data from Google's March crawl; `n-key-rollover-test.php`, `stuck-key-test.php`, and `ai-assistant.php` are still unknown to Google.
- Done locally only: removed the remaining raw `index.php` contextual link in `help/seo-content/keyboard-ghosting-test.php` and strengthened homepage keyboard SEO copy with canonical contextual links to `stuck-key-test.php`, `keyboard-ghosting-test.php`, and `n-key-rollover-test.php`. No live deploy was run.
- Done: Replaced the local horse pasture progress strip with a simple Chrome T-Rex-style runner scene: monochrome track, ground dashes, clouds, grass/obstacle checkpoints, and a cleaner CSS horse silhouette that moves forward as unique keys are tested.
- Verified locally only: PHP lint passed for `includes/components/keyboard-cat-progress.php` and `tools/keyboard_tester_english.php`; `node --check` passed for `assets/js/keyboard-cat-progress.js`; desktop and mobile Playwright checks against `https://localhost/kbt/` showed no horizontal overflow, the checkpoint counter advances, and the runner strip renders correctly. No live deploy was run.
- Done: Follow-up local visual pass replaced the remaining CSS horse silhouette with a compact CSS T-Rex and simplified the progress trail further toward the Chrome offline-game look: plain ground line, sparse dashes, cactus/grass/rock checkpoints, and no cloud decoration.
- Verified locally only: PHP lint, JS syntax check, regenerated `keyboard-cat-progress.min.js`, desktop/mobile Playwright checks, and no horizontal overflow. No live deploy was run.
- Done: Follow-up local visual pass used the user's supplied Chrome Dino screenshot as the dinosaur sprite source (`images/keyboard/chrome-dino-runner.png`) and removed all tree/grass/rock/flag objects from the progress strip. The strip is now black with a pixel ground line, score text, cactus obstacles, and pterodactyl-style obstacles only.
- Verified locally only: PHP lint, JS syntax check, regenerated `keyboard-cat-progress.min.js`, desktop Playwright render check, sprite image loaded, no grass/tree/rock/flag selectors in the active progress component/CSS/JS, no horizontal overflow. No live deploy was run.
- Done: Replaced the rough CSS-drawn Chrome Dino progress objects with real image sprites. Cropped orange dino, cactus, and blue cloud sprites from the user's supplied screenshots into `images/keyboard/dino-game/`; used a clean pterodactyl frame from an open Chrome-dino sprite sheet; removed obsolete `images/keyboard/chrome-dino-runner.png`.
- Verified locally only: PHP lint, JS syntax check, regenerated `keyboard-cat-progress.min.css`, desktop/mobile Playwright checks confirmed the sprite images load, old CSS shape selectors are gone, no failed image requests, and no horizontal overflow. No live deploy was run.
- Done: Replaced the whole rejected Chrome Dino progress concept with a local-only Pac-Man-style arcade maze: yellow chomper, maze rails, score label, dots, power pellets, and colored ghosts. Removed the now-unused `images/keyboard/dino-game/` sprite assets.
- Verified locally only: PHP lint, JS syntax check, regenerated `keyboard-cat-progress.min.css` and `keyboard-cat-progress.min.js`, and desktop/mobile Playwright checks confirmed the chomper, ghosts, pellets, score label, and no old Dino selectors render without horizontal overflow. No live deploy was run.
- Done: Improved the local Pac-Man progress visual with a cleaner yellow chomper, continuous mouth animation, and a progress-driven `--maze-eaten` lane so dots disappear behind the chomper as keys are tested.
- Verified locally only: PHP lint, JS syntax check, regenerated minified CSS/JS, no old Dino selectors in active progress assets, and desktop/mobile Playwright checks confirmed continuous mouth animation, dot clearing, score/dot count updates, and no horizontal overflow. No live deploy was run.
- Done: Added local-only moving arcade enemies to the Pac-Man progress strip. Three colored ghosts now continuously travel right-to-left, their pupils look toward the movement direction, Pac-Man starts clear of the mobile floating menu, and the mouth animation was slowed to feel less frantic.
- Verified locally only: PHP lint, JS syntax check, regenerated minified CSS/JS, desktop Playwright check confirmed enemies animate, pupils animate, Pac-Man stays between the maze rails, dots still clear with progress, and desktop/mobile checks showed no horizontal overflow. No live deploy was run.
- Done: Upgraded the local-only Pac-Man strip from CSS-only enemy drift to JS-controlled dynamic ghosts. Static checkpoint ghosts were removed; moving ghosts now randomly reverse between left/right movement, wrap from one side to the other, update pupil direction by movement direction, get eaten on Pac-Man collision, and respawn by falling back from above.
- Verified locally only: PHP lint, JS syntax check, regenerated minified CSS/JS, desktop Playwright check confirmed 0 static ghosts, 3 dynamic ghosts, direction changes, collision `is-eaten`, respawn `is-respawning` from negative top to lane top, dot progress unchanged, and desktop/mobile no horizontal overflow. No live deploy was run.

- Done: Fixed failing GitHub Actions `awesome-lint.yml` check on `nasirazizawan009/awesome-device-testing-tools` after commit `02a607f` rewrote `README.md`.
- Root cause: `README.md` removed the required Awesome badge and added a forbidden `## License` section; `awesome-lint` requires the badge and does not allow a README license section when a `LICENSE` file exists.
- Verified/pushed: Local `npx.cmd awesome-lint README.md` passes. Pushed commit `bfdc9f0` (`Fix awesome lint errors`) to `main`; GitHub Actions run #4 completed successfully.

### 2026-04-29 (Codex)
- Done: Cleaned up the published `awesome-device-testing-tools` README to reduce direct keyboard-tester clone competitors. Removed `KeyboardTester.com`, `Key-Test`, `Keyboard Test Space`, `Hardware Tester Keyboard Test`, `Hardware Tester Mouse Test`, `Hardware Tester Gamepad Test`, and `WebCamMicTest`.
- Done: Added more adjacent authority resources instead: QMK Documentation, QMK Configurator, VIA, Vial, Keyboard Layout Editor, MouseTester, DisplayCAL, MDN WebHID API, W3C Gamepad Standard, and Web Audio API.
- Verified/pushed: `npx.cmd awesome-lint README.md` passes. Pushed commit `b5f5013` directly to `https://github.com/nasirazizawan009/awesome-device-testing-tools`; raw GitHub README confirms removed competitor entries are gone and new authority entries are present.
- Done: Added and deployed `resources.php` as a public "Submit a Device Testing Tool" page that points users to the `awesome-device-testing-tools` GitHub issue form, repo, and pull request flow. Added neutral review standards so it is not framed as a backlink exchange.
- Done: Added a sitewide footer "Submit a Tool" link to `resources.php`, added SEO metadata/config links, regenerated `sitemap.xml`, and included the page in IndexNow submissions.
- Verified/deployed: PHP lint passed for changed PHP files, local/live page has exactly one H1, live `resources.php` returns HTTP 200, live homepage footer links to `/resources.php`, live sitemap includes the page, and IndexNow returned HTTP 200 with the new URL included.
- Done: Installed GitHub CLI via winget. Binary is at `C:\Program Files\GitHub CLI\gh.exe`, version `2.92.0`. It is not authenticated yet; run `gh auth login --web --hostname github.com --git-protocol https` before publishing new repos from CLI.
- Done: Created local starter repo package for `awesome-device-testing-tools` at `github-repo/awesome-device-testing-tools/` with README, contribution rules, CC0 license, PR template, issue form, awesome-lint GitHub Action, and maintainer notes. Local repo initialized on `main` with commit `22bcd0f`.
- Done: Published public GitHub repo at `https://github.com/nasirazizawan009/awesome-device-testing-tools` and kept the URL unchanged per user request. Added repo topics (`awesome`, `awesome-list`, `device-testing`, etc.), homepage URL, and submission review labels. Verified `npx awesome-lint README.md` passes.
- Done: Added supporting homepage copy for Google "People also search for" keyboard tester variants without changing the primary homepage title/meta/H1. New visible coverage includes no-download keyboard tester, laptop keyboard tester, Windows/Mac compatibility, 60 percent keyboard testing, and gaming keyboard tester use cases.
- Done: Updated homepage FAQ schema in `includes/schema.php` to match the new visible FAQ entries.
- Verified/deployed: PHP lint passed with `C:\xampp\php\php.exe`, live homepage returns HTTP 200, existing title/meta stayed unchanged, new visible copy and JSON-LD FAQ entries are present on production. IndexNow returned HTTP 200 after deployment.

### 2026-04-28 (Codex)
- Done: Created a 23.5s vertical YouTube Short package for KeyboardTester.click at `seo-audit-2026-04-28/youtube-short-keyboard-test/`. Files include `keyboardtester-keyboard-test-short.mp4` (1080x1920 H.264/AAC with narration/captions), `keyboardtester-short-thumbnail.jpg`, `upload-title-description.txt`, and `upload-metadata.json`.
- Note: YouTube upload was not completed because the project has no YouTube OAuth/API credentials or upload script. `.env` only contains Bing/DEV.to/FTP/IndexNow keys. The MP4 is ready for manual upload or OAuth-based upload once channel access is provided.
- Done: Fixed `/blog/` dark-mode contrast issue where global dark-mode `!important` text rules made blog card titles/excerpts nearly invisible on white card backgrounds. Added explicit dark-mode blog card/page backgrounds plus high-contrast title, excerpt, date, CTA, placeholder, section, and pagination colors in `blog/index.php`.
- Verified/deployed: PHP lint passed. Local and live Playwright computed-style checks in forced dark mode passed with card background `rgb(17, 24, 39)`, title contrast `16.96:1`, excerpt contrast `11.95:1`, date contrast `6.96:1`, and no horizontal overflow. Uploaded `blog/index.php` via SFTP and verified live `/blog/` HTTP 200 contains the dark-mode CSS.
- Done: Researched a low-competition keyboard timing content cluster from GSC + live SERP checks. Selected `keyboard polling rate test`, `keyboard Hz checker`, `keyboard polling rate checker`, and `how to check keyboard polling rate` because GSC already shows impressions around page-one positions but no clicks, and the SERP is mostly niche tools/explainers rather than high-authority publishers.
- Done: Published `blog/keyboard-polling-rate-test-check-keyboard-hz.php` with stock keyboard images, a related YouTube embed, Article + FAQ schema, internal links to the keyboard polling rate test, latency checker, key repeat rate tester, NKRO/ghosting tools, and relevant blog guides.
- Deployed: Uploaded the new article, `blog/posts-data.php`, two optimized WebP images, and updated `sitemap.xml` via SFTP/paramiko. Live verification passed: article HTTP 200, exactly one H1, images HTTP 200, video embed present, `/blog/` shows the new post, live sitemap contains the new URL, and IndexNow returned HTTP 200 with 862 URLs including the new post.
- Done: Redesigned `blog/index.php` so `/blog/` has a stronger editorial homepage: dark hero panel, large featured article, GSC-pinned "Top performing articles" rail, and a cleaner latest-guides archive below.
- Done: Kept top-performing blog articles on the blog homepage based on GSC data. `best-mechanical-keyboards-for-gaming-2026.php` is now the featured story; keyboard fix, ghosting, shortcuts, WebRTC leak, and crosshair articles are pinned below it. The normal archive excludes those pinned posts to avoid duplicate cards.
- Verified/deployed: Local PHP lint passed. Playwright desktop/mobile checks passed with one H1, 5 pinned rail cards, 9 latest cards, no horizontal overflow, and no duplicate mechanical-keyboard card in the latest grid. Deployed `blog/index.php` via SFTP and verified live `/blog/` and `/blog/?page=2` return HTTP 200 with the expected pinned behavior.
- Done: Replaced visible internal wording on `/blog/` that mentioned "SEO" with reader-facing copy about finding proven guides and new articles quickly. Deployed and verified live.
- Done: GSC CTR batch 1 for `mouse_sensitivity_DPI_tester.php` and `latency-checker.php`. Tightened page titles, meta descriptions, H1s, hero badges, tool-stage copy, feature cards, process copy, and schema/meta config around the actual GSC query language (`dpi tester`, `mouse dpi checker`, `keyboard latency test`, `keyboard delay test`, `input delay`).
- Done: Expanded `help/latency-checker.php` with clearer browser-measurement guidance: what the test measures, what it cannot measure, and how to reduce noisy results without overclaiming hardware-level accuracy.
- Deployed: Uploaded the 8 changed CTR files via SFTP/paramiko and verified production returns HTTP 200 with exactly one title/meta description/H1 on both target URLs. IndexNow endpoint returned HTTP 200 after deployment.
- Done: Counter-checked the Semrush Site Audit PDF from Apr 28 before acting. Live verification showed the reported hreflang, blocked CSS/JS/image resources, and broken image issues are stale/false positives.
- Done: Cleaned real sitemap/indexing problems: excluded non-indexable endpoints (`ai-chat.php`, `ai-config.php`, `desktop-manifest.php`), the old `testpage.php`, `404.php`, and `mouse-trail-index.php` from future sitemap generation.
- Done: Deployed `.htaccess`, `desktop-manifest.php`, `generate-sitemap.php`, and `sitemap.xml`. Production now returns 403 for direct `ai-config.php`, 410 for `testpage.php`, and `X-Robots-Tag: noindex` for `desktop-manifest.php`.
- Verified: Live `sitemap.xml` now contains 875 URLs and no excluded endpoints. Local sitemap URL status sweep found no real bad URLs after retrying one timeout. IndexNow returned HTTP 200 for 861 submitted URLs.
- Done: Updated and deployed `blog/best-mechanical-keyboards-for-gaming-2026.php` for weak GSC CTR: shorter title/meta/H1, refreshed JSON-LD `dateModified`, quick-answer box, jump links, and "How we chose" copy.
- Done: Updated and deployed `blog/posts-data.php` so the blog card uses the new title/excerpt.
- Done: Fixed all static blog PHP pages plus `blog/generate-blog.php` so SEO meta output lives inside `<head>` and duplicate manual `<title>` tags are removed.
- Done: Regenerated/deployed `sitemap.xml`; updated `generate-sitemap.php` so `/blog/` lastmod follows `blog/posts-data.php`, and excluded `blog/posts-data.php` from future sitemap/IndexNow discovery.
- Verified: Production article returns HTTP 200 with exactly one title/meta description, new title, H1, quick-answer section, and `dateModified=2026-04-28`. Blog card appears on `/blog/?page=2`; IndexNow returned HTTP 200 for 861 URLs.

### 2026-04-28 (Claude)
- ✅ Built Pro Diagnostics panel (NKRO counter, combo presets, polling rate, debounce, actuation)
- ✅ Deployed to all 9 language keyboard testers (en + 8 locales)
- ✅ Fixed polling rate calculation bug (interval ref was wrong)
- ✅ Published DEV.to Article 1 (keyboard ghosting + NKRO)

### 2026-04-27 (Claude)
- ✅ Localized Phase 1C: Russian/French/German/Spanish/Portuguese homepage SEO updates
- ✅ Spanish click-speed.php + dpi-tester.php optimizations (Arabic-pattern replica)
- ✅ Internal linking audit + 3 cross-link blocks added (mouse-test, mouse_speed_tester, keyboard_typing_test)
- ✅ All 46 GSC indexing issues addressed
- ✅ DEV.to API setup + Article 1 drafted

### 2026-04-26 (Claude)
- ✅ Survived 12-hour HostArmada outage with maintenance page
- ✅ Fixed `Cannot redeclare url()` PHP fatal in config.php
- ✅ Built GSC API automation (`gsc-traffic.py`, `gsc-coverage-audit.py`)
- ✅ Fixed mouse-trail-index.php duplicate

---

## 🔑 Credentials inventory

All credentials are in the workspace — both agents have access by default.

| Credential | Path | Format | Owner |
|-----------|------|--------|-------|
| FTP/SFTP private key | `.ssh-deploy/kbt_deploy` | OpenSSH ed25519 | Auto-loaded by `paramiko` |
| Bing Webmaster API | `.env` → `BING_API_KEY` | API key string | Read by `submit-bing-wmt.py` |
| GCP service account | `google-credentials.json` | JSON | Used by `submit-google-indexing.py`, `gsc-*.py` |
| DEV.to API key | `.env` → `DEVTO_API_KEY` | API key string | **Action: move from chat to .env** |
| Outreach mailbox SMTP | `.env` → `OUTREACH_SMTP_*`, `OUTREACH_FROM`, `OUTREACH_REPLY_TO` | SMTP/IMAP mailbox credentials for `outreach@keyboardtester.click` | Created via cPanel UAPI; use only for approved low-volume outreach |
| YouTube OAuth client | `youtube-oauth-client.json` + `.env` -> `YOUTUBE_OAUTH_CLIENT_FILE` | OAuth client JSON, gitignored | Used for YouTube Data API uploads |
| YouTube OAuth upload/read token | `youtube-oauth-token-correct-channel.json` | OAuth refresh/access token JSON, gitignored | Correct KeyboardTester channel (`UCwZ16zCK4uA_y4G71GROCuw`) for upload + channel verification |
| YouTube OAuth manage token | `youtube-oauth-token-manage-correct-channel.json` | OAuth refresh/access token JSON, gitignored | Correct KeyboardTester channel metadata updates (`youtube.force-ssl`) |
| YouTube legacy wrong-channel token | `youtube-oauth-token-wrong-channel-legacy.json` | OAuth refresh/access token JSON, gitignored | Personal `@nasirazizawan` upload-only token; do not use for KeyboardTester uploads |
| GitHub PAT | (none persisted) | User generates ad-hoc when needed | Per-task generation, revoke after |
| HostArmada SSH | port 19199, user `keyboar1` | Uses `.ssh-deploy/kbt_deploy` | Documented in CLAUDE.md |

## 📚 Where to find things

| Looking for... | Path |
|----------------|------|
| Project rules/conventions (Claude) | [CLAUDE.md](CLAUDE.md) |
| Project rules/conventions (Codex) | [AGENTS.md](AGENTS.md) |
| Detailed session logs | `seo-audit-{date}/SESSION-TRACKER.md` |
| Deploy scripts | `deploy-*.py` (root) |
| Indexing/SEO scripts | `submit-*.py`, `gsc-*.py` (root) |
| DEV.to publishing | `dev-to-publish.py` + `dev-to-articles/*.md` |
| Tool registry | `includes/components/tool-list-data.php` |
| Schema / SEO meta | `includes/schema.php`, `includes/seo-meta.php` |
| GSC API service account | Domain property `sc-domain:keyboardtester.click` (siteOwner) |
| Past audits | `seo-audit-{date}/` directories |

---

## 🚫 Things NOT to do (mistakes to avoid — historical)

- ❌ **Don't deploy via FTPS** — host's TLS data channel hangs. Use SFTP/paramiko via the SSH key.
- ❌ **Don't add a function in `config.php` without a `function_exists()` guard** — caused 12-hour outage on 2026-04-26.
- ❌ **Don't use `os.system` or shell=True with user input** — use `subprocess.run([...])` with list args.
- ❌ **Don't post promotional comments on Facebook pages** — spam detection, no SEO value (links are nofollow).
- ❌ **Don't try to fix the GSC sitemap "Couldn't fetch" error** — confirmed Google-side bug since Sept 2025, no technical fix exists.
- ❌ **Don't create blog wp-content/wp-admin URLs** — those return 410 Gone via .htaccess.
- ❌ **Don't try to send WhatsApp/IFTTT spam** — not the strategy.
- ❌ **Don't submit dev.to URLs to Google Indexing API** — service account isn't a property owner there; will return 403.

## 💡 Lessons learned (worth keeping in mind)

- Domain property Owner satisfies BOTH Search Console API and Indexing API (Google's docs imply URL Prefix only — but Domain works in practice as of 2026-04).
- Adding service account to GSC Domain property can silently demote URL Prefix permissions. Re-add to URL Prefix as Owner if Indexing API starts failing.
- HostArmada uses lsapi+CageFS, NOT PHP-FPM. Don't ask host to "restart PHP-FPM" — they don't run FPM.
- DEV.to community responds to genuinely educational content; promo angle gets downranked algorithmically.
- 82% of KeyboardTester.click clicks come from non-USA. Korean and Arabic markets ARE searching in native languages; French/German/Spanish/Brazilian markets mostly search in English.

---

## 🤝 Conflict resolution

If both agents need to modify the same file:

1. **Check this file's "Active tasks" first** — if another agent claimed the file, wait
2. **If urgent and other agent unreachable:** make the change, mark in this file as "modified by [agent] while [other] was working — review for conflicts"
3. **For .env / credentials:** never commit. Always gitignored. If you need to add a key, document its existence here (don't paste the value).
4. **For deploys:** only one agent should deploy at a time. Use this file's "Active tasks" to claim.

---

## 📊 Session ending checklist (do this before logging off)

- [ ] Updated "Active tasks" with current state
- [ ] Moved completed work to "Completed today"
- [ ] Documented any new credentials added
- [ ] Updated "Last updated" timestamp at top
- [ ] Committed changes with `[agent-name]` prefix
- [ ] If site was deployed: verified production state via curl

---

## 🚀 Recommended workflow

**Daily start:**
```
1. cd c:/xampp/htdocs/kbt
2. cat AI-COORDINATION.md         # see what's queued + recent context
3. cat seo-audit-2026-04-26/SESSION-TRACKER.md  # detailed state
4. git log --oneline -20          # recent commits
5. Pick a task from "Active tasks" or take a new request from user
6. Work
7. Update this file before ending
```

**For ANY agent:** if you're unsure who's doing what, **ask the user before starting** — they'll know who's currently active.
